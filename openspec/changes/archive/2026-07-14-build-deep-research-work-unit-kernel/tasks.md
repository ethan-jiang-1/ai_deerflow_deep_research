## 1. Domain contracts, state authority, and bundle paths

- [x] 1.1 Add red deterministic tests in `agent/tests/domain/test_work_units.py` for frozen extra-forbid `WorkSpec`, `Attempt`, `OutputRef`, `SourceRef`, `CandidateResult`, `SubmissionRecord`, and `FixtureResultDocument` schemas; exact generation/phase/four-digit work and two-digit attempt id grammar; result-contract ids; ordinal range denial; explicit-null UTC canonical JSON; exact work/candidate/record/failure-detail domain-prefix golden hash vectors including the terminal-code plus ordered-validation-code input object; canonical sorted/unique output and source refs; every field/collection/referenced-byte and 16/48/64-KiB encoded bound; same-id/different-spec conflict; and worker identity/spec mutation denial. @impl WOU-001
- [x] 1.2 Implement `agent/src/deerflow_deep_research/domain/work_units.py` with the exact version-1 bounded field tables, closed work/attempt/validation enums, source/output refs, id regex/parsers, canonical collection validators, domain-separated object hashes, and pure work-unit capability protocols until task 1.1 is green; allocation remains owned by engine task 4.2. @impl WOU-001, WOU-003, WOU-004
- [x] 1.3 Add red state tests for one-way compatible version-2 old-checkpoint loading plus explicit pre-change-reader rejection of newly added fields; frozen extra-forbid compact refs/`TerminalFailureSummary`; model-or-mapping compact input normalized to `model_dump(mode="json")` plain checkpoint mappings; `work_specs_by_id` keyed by work id; `attempts_by_id`, `work_status_by_id`, and `terminal_failures_by_attempt_id` keyed by attempt id; key-derived identity, canonical spec paths, and failure classification omitted from compact values, with paths/classification re-derived from bundle/`FailureCode` authorities; exact failure-detail hash replay; one `active_attempt_by_work_id`; current-epoch work/attempt cursors; retry append without terminal-history rewrite; complete-map current failure projection with at most one selected summary per work while old attempt history remains; exact 32-work/64-attempt/32-failure/64-ref parent bounds; a pessimistic independent-field compact-JSON budget object containing exactly `pending_work_ids`, both cursors, all declared work/attempt/status/active/failure maps, and accepted refs at or below 40,960 bytes; a separate legal all-terminal 32/64/32/64 checkpoint with empty pending/active maps plus a maximum-length single-byte ASCII request at or below 65,536 bytes; the exact six-channel non-checkpointed child state with 32 planned/pending, 16 batch maps, 64 terminal updates, `validation_failed` non-empty versus every-other-terminal empty validation-code tuples, keyed conflict reducers, and `Overwrite` batch clearing; parent reducer terminal monotonicity versus cross-field-validated coalesced child transitions; closed `WriterRole.SUBMIT` plus field-specific controller/worker/submit/gate ownership; and unknown-version denial. @impl WOU-001, WOU-002, WOU-004, WOU-007, REG-007, REG-011
- [x] 1.4 Implement the compact parent ref/failure models and exact six-channel `WorkUnitComponentState` in `domain/work_units.py`, then compatibly extend version-2 `domain/state.py` with optional/defaulted plain-JSON parent projections, model-coercing reducers/load validation, exact work-block serialization, bounds, and field-specific writer enforcement; keep candidate bodies and opaque model objects out of the parent checkpoint and preserve fail-closed rejection of unknown/incompatible versions until task 1.3 is green. @impl WOU-002, WOU-004, WOU-007, REG-006, REG-007, REG-008, REG-011
- [x] 1.5 Add red bundle tests for canonical relative spec/result/output/ledger/lock/staging paths; exact alias probe plus same-token `.lock/.src/.dst` runtime filesystem-probe names and prelaunch equivalents; relative-to-virtual/host boundary conversion; research/attempt containment; absolute/host-path persistence denial; symlink escape denial; temporary probe non-authority; and audit-vs-authority classification. @impl WOU-003, WOU-004, WOU-006, REG-010
- [x] 1.6 Extend `domain/bundle.py` with the work-unit and evidence-ledger paths used by the design, preserving the minimal bundle and rejecting DPT queue/index/status files. @impl WOU-003, WOU-004, REG-010
- [x] 1.7 Add red pure projection tests for `project_work_unit_agent()` whose `NodeAgentContext.attempt_root` is `<research_root>/work/<work_id>/<attempt_id>`, while caller/worker-selected roots, malformed ids, and mismatched work/attempt identity are rejected; do not wire controller capability or `GraphInvocationContext` yet. @impl WOU-001, WOU-003, NOA-001, REG-010
- [x] 1.8 Implement only the trusted work-unit `NodeAgentContext` path projection in `runtime/projection.py`, retaining the existing phase-level projection; controller resolver, `NodeBuildDependencies.work_units`, and invocation-context wiring remain owned by task 5.3. @impl WOU-001, WOU-003, NOA-001, REG-010

## 2. Canonical ledger and runtime-owned atomic store

- [x] 2.1 Add red domain tests in `agent/tests/domain/test_submission_ledger.py` for empty-ledger/genesis-null behavior; exact LF-only canonical JSONL parsing; validator-v1 exact sorted `passed_checks`; 64 KiB record, 4,096-record, and 8 MiB ledger bounds; schema validation; full hash-chain verification; stable candidate replay comparison excluding ledger-assigned fields; duplicate attempt identity and duplicate logical-work detection; partial-tail/blank-line/CRLF/oversize denial; and staging non-authority. @impl WOU-004, WOU-005
- [x] 2.2 Complete the pure ledger codec and replay comparison in `domain/work_units.py`; runtime code SHALL import these domain functions directly and SHALL NOT import `engine/`. @impl WOU-004, WOU-005
- [x] 2.3 Add red runtime/doctor tests for `ReadinessDiagnostic.work_unit_storage`; exact shared reason codes; package-export and implementation paths for every built-in provider; no suffix guessing; LocalSandbox and AIO-without-provisioner ready candidates; provisioner-backed AIO/E2B/BoxLite not ready; custom/unrecognized unknown but runtime-not-ready; exact same-token `.lock/.src/.dst` probe families; real flock contention, descriptor-relative replace, file/directory fsync, no-follow regular-file and cleanup failures despite misleading capability introspection; prelaunch placement directly under existing gateway-visible `get_paths().base_dir` with no invented user/thread or `host_base_dir`; runtime `uses_thread_data_mounts is True` and `provider.get(parent_sandbox.id) is parent_sandbox` contradictions; bidirectional base64url alias failure; `asyncio.to_thread` use for synchronous sandbox reads/writes/cleanup; host-created 0600 and sandbox-created host-fchmod behavior; runtime diagnostics-subtree placement; host descriptor cleanup plus exact trusted-path shell-quoted sandbox cleanup/absence verification on alias mismatch; caller-text exclusion; and host-path redaction. @impl WOU-006, DEC-005
- [x] 2.4 Implement `runtime/work_unit_storage.py` as the non-cyclic shared module containing the pure config classifier and I/O runtime verifier used by diagnostics and store construction: exact provider-mode table, tri-state diagnostic, `runtime_ready` conjunction, executable host flock/replace/fsync/no-follow probe rather than feature-set inference, installed-provider/sandbox identity check, bounded bidirectional host/sandbox alias round-trip with sync APIs off-thread, dual host descriptor-relative plus trusted generated sandbox-command cleanup with residue mapped to `probe_cleanup_failed`, stable redacted reasons, and failure before research/ledger/checkpoint mutation. @impl WOU-006, DEC-005
- [x] 2.5 Add red `DeepResearchControlResult`/reflected-tool tests for required/forbidden `infrastructure_reason`, every closed storage reason and exact unavailable/busy pairing, dedicated first `WorkUnitStoreError` catch, provider/checkpointer closure, final redacted `Exception -> checkpoint_inconsistent` for work-unit invariant failures without catching cancellation, start/resume parent-sandbox initialization versus status/cancel no-sandbox adapter selection, optional envelope parent sandbox, and no storage-error collapse, graph checkpoint mutation, free-form detail, host-path leakage, or resource leakage. Cover unsupported modes, mount/alias/POSIX/probe-cleanup defects, `ledger_corrupt`, `accepted_artifact_diverged`, lock deadline, `work_unit_gate_view_inconsistent`, and `node_gate_write_conflict`. @impl WOU-005, WOU-006, RUI-006
- [x] 2.6 Implement `InfrastructureResultCode.WORK_UNIT_STORAGE_UNAVAILABLE`, `WORK_UNIT_STORE_BUSY`, domain `WorkUnitStorageReason`, optional validated `DeepResearchControlResult.infrastructure_reason`, `WorkUnitStoreError` with the complete closed pairing, ordered dedicated/known/final-redacted tool projection, optional `TrustedRuntimeEnvelope.parent_sandbox`, and explicit `RuntimeAdapter.adapt(..., initialize_parent_sandbox=...)` selection in `tool.py` until task 2.5 is green; status/cancel remain checkpoint-only and `CancelledError` still propagates. Do not construct or wire `WorkUnitControllerDependencies` yet; task 5.3 owns that capability boundary. @impl WOU-005, WOU-006, RUI-006
- [x] 2.7 Add red runtime-store tests for trusted host-root derivation, verified provider/sandbox identity and host aliasing, exact ledger lock/staging/alias-probe and same-token filesystem `.lock/.src/.dst` names, descriptor-relative no-follow and regular-file checks, `O_CREAT|O_RDWR|O_NOFOLLOW` stable lock mode 0600, rejection of group/world bits on existing lock/ledger, exclusive mode-0600 staging/probes plus host fchmod of sandbox-created alias files, real two-fd flock contention, a two-second deadline with 25 ms nonblocking retries, exact ledger size/count bounds, logical-work plus attempt indexes, one accepted winner across sibling attempts, file/directory fsync, real same-directory dir-fd atomic replace preserving ledger 0600, lock release/dual cleanup on every failure/cancellation, and no host-path leakage. @impl WOU-004, WOU-006
- [x] 2.8 Implement `runtime/work_unit_store.py` using the verified `TrustedRuntimeEnvelope.workspace_host_path`, `LOCK_NB` deadline retries, `asyncio.to_thread`, atomic whole-file publication, staging cleanup, and injected clock/fault hooks until task 2.7 is green, without exposing the store to model-facing capabilities. @impl WOU-004, WOU-006
- [x] 2.9 Add blocking-I/O and cancellation tests proving ledger reads/hashes/locks/commits do not block the event loop, lock acquisition terminates at its deadline, cancellation waits only for a size-bounded acquired commit, and a committed-ledger/missing-checkpoint outcome remains replayable. @impl WOU-005, WOU-006, WOU-007

## 3. Deterministic submit validation

- [x] 3.1 Add red table-driven tests in `agent/tests/engine/test_work_unit_validation.py` for the exact ordered/deduplicated `SubmissionValidationCode` registry; valid `fixture.work-unit` documents; and denial of wrong research/generation/phase/work/attempt/role ids, spec mutation, unknown result contract/version/fixture marker, missing/extra/unsorted output-set mismatch, unsupported schemas, missing/empty files, non-canonical required/full paths, path traversal, absolute paths, symlink escape/path swap, result/output hash mismatch, and invalid source refs including scheme/userinfo/default-port/host-case/trailing-slash/fragment canonicalization cases. @impl WOU-001, WOU-003
- [x] 3.2 Implement `engine/work_units/validation.py` as an ordered collect-all validator with closed stable failure codes, pure validation plans, a closed result-contract registry containing only `(fixture.work-unit, 1)` in this change, canonical source URLs, exact required/result/candidate output-set equality, result/output/source schema checks, and no success inference from summaries, finish reasons, file names, or run events. @impl WOU-003
- [x] 3.3 Add red runtime read tests for descriptor/no-follow containment, symlink/path-swap denial, stat verification, byte bounds, hashing, virtual-ref projection, and host-path redaction. @impl WOU-003, REG-009, REG-010
- [x] 3.4 Extend the runtime store with the contained read plan until task 3.3 is green; keep all host path and blocking file access inside `runtime/`. @impl WOU-003, REG-009, REG-010
- [x] 3.5 Add red role-ownership fixtures proving controller alone writes canonical `work-spec.json`, the assigned worker receives only a narrowed `AttemptArtifactWriter` for the exact non-research `FixtureResultDocument` and declared `outputs`, change-04 fixtures use empty `source_refs`, submit alone writes ledger mechanics, and no persisted hash contains virtual-absolute or host paths. @impl WOU-001, WOU-003, REG-002, REG-010
- [x] 3.6 Implement the narrowed deterministic fixture artifact capability and boundary path conversions until task 3.5 is green, with no model, web, MCP, ACP, DeerFlow `task`, or sandbox research-tool call. @impl WOU-001, WOU-003, REG-002, REG-010

## 4. Work-unit allocation, reducers, retry, and drain kernel

- [x] 4.1 Add red engine tests for exact stable work/attempt ids and cursor scopes; retry with a new attempt entry and unchanged spec hash only for unaccepted failed/timed-out work; cancelled/superseded and accepted-work retry denial plus new-logical-work quality repair; one-active-attempt enforcement; exact `1..16` construction limits and fixture limit 3; ordinal ordering, cursor slice/advance-before-dispatch, completion-order independence, and bounded refill; the complete internal pending/running/terminal transition table; UTC/created-started-expiry-terminal ordering and expiry boundary; exact same-status replay versus detail conflict; canonical same-hash idempotency; different-hash conflict; terminal downgrade/replacement denial; exact first-validation-code/non-validation-terminal mapping to one parent `FailureCode` with full ordered detail hashing; separate acceptance of a coalesced parent projection only after cross-field attempt/status/active/failure/accepted validation; rejection of an equivalent unvalidated direct parent jump; deterministic full-map replacement of only the selected per-work failure summary while attempt history remains; accepted-ref dedupe; cancellation/expiry/superseded late-submit denial; and structural drain behavior. @impl WOU-001, WOU-002, WOU-007, WOU-008
- [x] 4.2 Implement `engine/work_units/ids.py`, `reducers.py`, and `kernel.py` for controller allocation, typed internal pending/in-flight/terminal transitions, construction-time concurrency policy, candidate reduction, retry/replay decisions, exact primary-validation/non-validation failure projection, generic phase drain, separately writer-authorized controller/submit partial operations, and one cross-field final parent-projection validator whose coalesced output retains terminal monotonicity without pretending intermediate child transitions were checkpointed. @impl WOU-001, WOU-002, WOU-007, WOU-008
- [x] 4.3 Add sole-writer tests proving planner/worker/repair/gate updates cannot allocate ids, mutate specs, set submitted status, append accepted refs, or access ledger commit capability; ordinary `CONTROLLER` can materialize but cannot append accepted refs; closed `SUBMIT` alone can publish accepted refs/current failure projection and its allowed attempt/active-map transition; and gate retains only route/feedback/budget fields. @impl WOU-001, WOU-004, REG-007, REG-009
- [x] 4.4 Add red gate-rule tests for the exact five-field/32-entry `WorkUnitGateView` and separate exact ephemeral `WorkUnitGateFailure(work_id, attempt_id, already-mapped failure_code, derived classification, detail_hash)`, forbidden payload/path/control fields, and non-checkpointing; prove the component validator checks exact child planned ids, canonical terminal selection, full reconciled `SubmissionRecord` work/attempt/hash association (including swapped-hash denial), and the final parent projection; prove the separate wrapper validator checks only sorted current-phase planned membership, terminal attempt/status, accepted-hash membership, and selected failures and raises `work_unit_gate_view_inconsistent` without claiming hash-to-work reconstruction; prove the shared work-completion rule reads only the validated drain/terminal/accepted/failure view, performs no I/O or remapping, preserves valid fixture routes, and prevents fixture `pass` from masking failed/unaccepted work. @impl WOU-003, WOU-008, GAK-002, GAK-006
- [x] 4.5 Implement the exact frozen bounded `WorkUnitGateView`, non-checkpointed `WorkUnitGateFailure`, both pure view validators, and reserved `__work_unit_gate_view__` preview-only transport constant in `domain/work_units.py`, plus `WorkUnitCompletionRule` in `engine/work_units/kernel.py`, until task 4.4 is green; do not yet register the rule on Wave0/Wave1, add a `ResearchState` field, or checkpoint the view. @impl WOU-008, GAK-005, GAK-006

## 5. Reusable LangGraph component and Wave integration

- [x] 5.1 Add red graph tests in `agent/tests/graph/test_work_unit_component.py` for at least three concurrent `Send` workers, arbitrary completion order, exact keyed candidate retention/conflict, `Overwrite` clearing without duplicate reducer output, multiple refill batches, the 32-work/concurrency-1 path under internal recursion limit 128, no double winner, explicit whole-Wave replay from the last parent checkpoint, exact component-level gate-view validation before transient detail is discarded, and a gate-ready return only after drain. @impl WOU-002, WOU-005, WOU-008, REG-002
- [x] 5.2 Implement `graph/components/work_units.py` as the reusable load-ledger -> deterministic materialize/allocate -> reconcile -> bounded Send -> keyed reduce -> deferred stable submit/Overwrite clear -> refill -> drain child graph with typed bounded child state, exact component-level gate-view validation against its child plan/reconciled records/final projection before return, explicit `checkpointer=False`, and internal `recursion_limit=128`; do not claim inherited child durability or caller-controlled recursion under manual invocation. @impl WOU-002, WOU-005, WOU-008
- [x] 5.3 Add red capability-projection tests, then extend `NodeSpec` with default-empty closed `NodeCapability`, add optional `NodeBuildDependencies.work_units`, and extend `domain/invocation.py`, `runtime/projection.py`, `runtime/research.py`, `GraphInvocationContext`, and async runtime resolution so only synthetic/declaring controllers receive `WorkUnitControllerDependencies`; construct the verified store/controller context asynchronously only immediately before eligible start/resume graph invocation, while status/cancel receive no work-unit dependency and malformed cancel routing into a declaring controller fails before factory construction. Implement the per-work resolver here with fully validated `WorkSpec`/active `Attempt` input, nested controller authority forced null, and the task-1.8 `work/<work>/<attempt>` context. Prove replay derives and validates `a00/work-spec.json` against compact role/hash, writes/verifies a byte-identical retry copy, and never reconstructs scope/output contracts from the compact ref. Assert declaration/runtime mismatch fails before factory construction and host/store authority never enters other nodes, `NodeAgentContext`, prompts, tool schemas, or checkpoint state. Do not mark production Wave specs yet. @impl WOU-001, WOU-004, NOA-001, REG-009, REG-010
- [x] 5.4 Add red wrapper tests using a synthetic declaring controller: its typed `__work_unit_gate_view__` is copied, popped, wrapper-validated, injected only into the in-memory gate mapping, and absent from the final update/checkpoint; non-controller use, missing/wrong-type view, or view/preview divergence fails before gate evaluation. Prove the gate also receives a pure preview of exactly `work_specs_by_id`, `attempts_by_id`, `work_status_by_id`, `active_attempt_by_work_id`, `terminal_failures_by_attempt_id`, and `accepted_submission_refs` through their real reducers, while phase/route/trace/cursors/gate-owned fields, the input checkpoint, and the stripped original node delta remain untouched; assert the final return contains that original reducer delta rather than the merged preview, overlapping node/gate keys fail as `node_gate_write_conflict`, and `FixtureSequenceRule` keeps the prior current-visit index. @impl WOU-004, WOU-008, GAK-003, GAK-005, REG-002
- [x] 5.5 Implement reserved-view extraction plus the explicit non-reflective reducer-preview path in `domain/state.py`/`graph/builder.py` until task 5.4 is green, without marking or changing Wave0/Wave1 yet. @impl WOU-008, GAK-003, REG-001, REG-002
- [x] 5.6 Add a red Wave0 integration test requiring its production `NodeSpec` to declare `WORK_UNIT_CONTROLLER`, three controller specs/attempts, fixture candidates, validated records/accepted refs, exact component and wrapper gate-view validation, replay, drain, the prepended completion rule, and a repair visit that allocates new work ids for already accepted fixture work through the shared component. @impl WOU-001, WOU-002, WOU-003, WOU-004, REG-002
- [x] 5.7 Mark Wave0 as `WORK_UNIT_CONTROLLER`, replace its fixed `BranchResult` join with the shared component, and prepend `WorkUnitCompletionRule` only to its fixture `GateDefinition` until task 5.6 is green. @impl WOU-001, WOU-002, WOU-003, WOU-004, WOU-008, REG-002
- [x] 5.8 Add a red Wave1 integration test with distinct deterministic fixture scope/content, production `WORK_UNIT_CONTROLLER` declaration, the same validated view/completion-rule path, and an assertion that no second kernel, store, reducer, or submit path exists. @impl WOU-001, WOU-002, WOU-003, WOU-004, REG-002
- [x] 5.9 Mark Wave1 as `WORK_UNIT_CONTROLLER`, replace its fixed join with the same shared component, and prepend the same `WorkUnitCompletionRule` to its fixture `GateDefinition` until task 5.8 is green. @impl WOU-001, WOU-002, WOU-003, WOU-004, WOU-008, REG-002
- [x] 5.10 Add topology/gate regressions proving both and only Wave0/Wave1 declare controller capability, accepted work identity is never reconstructed by wrapper I/O, every reducer is applied exactly once, internal nodes never enter `LOGICAL_NODES`, the snapshot/route labels remain identical, and `_route()` is unchanged. @impl WOU-008, GAK-003, REG-001, REG-002

## 6. Crash replay, process races, lifecycle, and E2E coverage

- [x] 6.1 Add red fault-injection tests for every named boundary: from the last parent checkpoint before accepted-ref publication, before staging write, after staging fsync, after atomic ledger replace, after directory fsync, before submit-node return, and after returned state update. @impl WOU-005, WOU-006
- [x] 6.2 Add red reconcile-matrix tests for state/ledger absent regeneration; active/no-record execution; replay rehydration from derived canonical `a00/work-spec.json` plus role/hash verification and retry-copy equality; matching ledger/no-ref catch-up; matching ledger/ref no-op; divergent candidate returning `candidate_conflict`; submitted/accepted checkpoint-ahead with no matching ledger record returning `work_unit_storage_unavailable/ledger_corrupt`; matching fingerprint with missing/mutated accepted files returning `accepted_artifact_diverged`; sibling-attempt accepted record superseding a retry; duplicate-logical-work/broken-chain returning `ledger_corrupt`; no checkpoint repair for any authority defect; and staging-only cleanup. @impl WOU-005
- [x] 6.3 Complete kernel/store replay orchestration until tasks 6.1 and 6.2 are green, without adding a second checkpoint or evidence authority. @impl WOU-005, REG-009
- [x] 6.4 Add a real multiprocessing/shared-temp-workspace contract test where independent store instances race the same candidate, divergent candidates for one attempt, and candidates from sibling attempts of one logical work, proving one logical-work winner, one valid hash chain, no partial line, and no claim that a newer ordinal wins under split-brain callers. @impl WOU-006
- [x] 6.5 Add restart/cancel/expiry tests proving accepted logical work skips every sibling attempt, an unaccepted non-terminal attempt may replay unchanged, failed/timed-out work requires a fresh retry entry/id, cancelled/superseded work cannot be redispatched, expired/cancelled/superseded attempts reject late results before store access, exact terminal replay is idempotent, cancellation is never projected as completion, and a storage-unavailable fixture still permits checkpoint-only cancel without initializing the parent sandbox or constructing/exposing the store. @impl WOU-005, WOU-007
- [x] 6.6 Update full-fake and mixed-graph tests to cover happy completion, repair, rerun, stop, cancel, stale-response denial, controlled Wave0/Wave1 fixture files, and absence of model/web/subagent/cache/synthesis/report side effects. @impl REG-002, REG-005, WOU-004
- [x] 6.7 Add a worker-completion-without-result integration fixture plus wrong-id, out-of-root path, hash-mismatch, same-hash replay, and different-hash conflict paths through the real component and submit boundary. @impl WOU-003, WOU-005

## 7. Structure, documentation, and configuration invariants

- [x] 7.1 Add the exact design-listed new production paths (`domain/work_units.py`, `engine/work_units/{__init__,ids,reducers,validation,kernel}.py`, `graph/components/{__init__,work_units}.py`, and `runtime/{work_unit_storage,work_unit_store}.py`) to `openspec/governance/project-structure.toml`; narrowly permit only node-local `subgraph.py` to import reusable `graph/components/` orchestration; update architecture contract fixtures; render the generated `agent/AGENTS.md` block; and pass `check_project_architecture.py`. Do not enumerate every test file. @impl PRS-001, PRS-002, PRS-003, PRS-004
- [x] 7.2 Update human-authored `agent/AGENTS.md`, `agent/README.md`, and the numbered/master backlog plan status with the work-unit authority chain, compact state budgets, JSONL/lock decision, compatible version-2 state extension, whole-Wave replay unit, mounted-workspace limitation, status/cancel no-parent-sandbox boundary, controlled fixture side effects, and later-node reuse rule. @impl WOU-004, WOU-005, WOU-006, REG-009
- [x] 7.3 Verify `openspec/governance/req-registry.yaml` contains the `WOU` prefix and WOU-001 through WOU-008 descriptions, and that active delta req headers own WOU-001..008 plus modified PRS-002/003, REG-002/009/010, and DEC-005, with every modified requirement copied in full. @impl WOU-001, WOU-002, WOU-003, WOU-004, WOU-005, WOU-006, WOU-007, WOU-008, PRS-002, PRS-003, REG-002, REG-009, REG-010, DEC-005
- [x] 7.4 Run the existing configure/doctor contract tests and project doctor in the configured environment; verify its bounded capability-probe files are removed with no persistent config/authority mutation, add the work-unit storage readiness result, and confirm no `config.yaml`, `extensions_config.json`, skill, Agent/SOUL, mount, dependency, startup-only field, `backend/`, or `frontend/` edit is needed. @impl DEC-002, DEC-005

## 8. Verification and hard done conditions

- [x] 8.1 Run focused domain/engine/graph/runtime work-unit tests, then `cd agent && make test-unit && make test-contract`; fix all failures.
- [x] 8.2 Run `cd agent && make test-viability && make test-durability && make test-blocking-io`; include the reflected `infra_probe` provider-reopen/subprocess-restart path as the environment-appropriate end-to-end infrastructure check. @impl RUI-001, RUI-005, WOU-005, WOU-006
- [x] 8.3 Run `cd agent && make test` and verify the complete agent-owned suite, including full-fake and mixed lifecycle paths, is green.
- [x] 8.4 Run `cd agent && make format && make lint && make lock-check`; fix formatting, lint, or lock drift without adding an unused dependency.
- [x] 8.5 Run `python3 openspec/governance/check_project_architecture.py`, `python3 openspec/governance/check_project_reqs.py`, and `python3 openspec/governance/check_project_specs.py`; all three must pass with no structural drift, duplicate/unregistered/orphan/reused-retired requirement IDs, delta headers in main specs, or missing purpose/requirements/req headers.
- [x] 8.6 Inspect `git diff -- backend frontend config.example.yaml extensions_config.example.json` and verify it is empty; inspect the final change diff for one submit authority, one ledger path, one retry path, and complete `@impl` traceability before archive.
