## 1. Domain contracts: marker, store protocol, and pure binding validation

- [x] 1.1 Add red deterministic tests in `agent/tests/domain/test_bootstrap.py` for a frozen extra-forbid `BootstrapMarker` (literal `schema_version=1`, bound `research_id`/`start_message_id`/`request_digest`/`state_schema_version`, canonical sorted compact JSON, and rejection of malformed identity, unsupported marker schema/version, and oversize payloads); a runtime-checkable `BootstrapBundleStoreProtocol` (`establish_bundle`, `read_marker`); and a pure `validate_bootstrap_binding(marker, checkpoint) -> Failure | None` that returns a typed `FailureCode` (identity mismatch, schema/version unsupported, request-digest divergence) and `None` only when the marker is bound to the checkpoint, performing no I/O and no mutation. @impl BON-001, BON-002, GAK-002, GAK-006
- [x] 1.2 Implement `agent/src/deerflow_deep_research/domain/bootstrap.py` with the frozen `BootstrapMarker` (`schema_version=1`, `research_id`/`start_message_id`/`request_digest`/`state_schema_version`), `BootstrapBundleStoreProtocol`, a `marker_path(research_id)` helper → `workspace/deep-research/<research_id>/request/marker.json` added to `domain/bundle.py` (alongside `REQUEST_SUBTREE`), and the pure `validate_bootstrap_binding` reusing `domain/failure_codes.FailureCode` until task 1.1 is green; runtime code SHALL import these domain contracts directly and SHALL NOT import `engine/`. @impl BON-001, BON-002, GAK-002, GAK-006
- [x] 1.3 Add red tests in `agent/tests/domain/test_node_spec.py` for a new `NodeCapability.BOOTSTRAP_BUNDLE` member and an optional `NodeBuildDependencies.bootstrap_bundle: BootstrapBundleStoreProtocol | None = None`; prove declaring and non-declaring nodes are constructible and the field defaults to `None`. @impl BON-001, BON-002
- [x] 1.4 Extend `domain/node_spec.py` (`NodeCapability` enum + `NodeBuildDependencies` field) and `domain/invocation.py` (`GraphInvocationContext.bootstrap_bundle: BootstrapBundleStoreProtocol | None = None` with type validation) until task 1.3 is green; do not wire the wrapper or runtime resolver yet. @impl BON-001, BON-002
- [x] 1.5 Add red contract tests in `agent/tests/contract/test_bootstrap_contracts.py` for the extended `graph/nodes/bootstrap/contracts.py` `BootstrapResult` (route plus optional binding/marker ref) and rejection of a caller-supplied authority field; the public `NODE_SPEC` surface stays one export. @impl BON-002, PRS-003
- [x] 1.6 Extend `graph/nodes/bootstrap/contracts.py` with the binding/marker contract fields until task 1.5 is green, keeping the package exporting exactly `NODE_SPEC`. @impl BON-002, PRS-003

## 2. Runtime-owned atomic bundle store

- [x] 2.1 Add red runtime tests in `agent/tests/unit/test_bootstrap_bundle.py` for trusted host-root derivation from the virtual `workspace_root`; atomic establishment of the minimal bundle directory tree plus a schema/version marker under the canonical `request` subtree, written last via same-directory atomic replace + file/directory fsync under the research-scoped POSIX lock; marker bound to the supplied checkpoint identity; `read_marker` returning the validated marker; rejection of group/world bits and symlink/path-swap/escape; `asyncio.to_thread` for all synchronous sandbox reads/writes/cleanup with no event-loop blocking; and no host-path leakage in errors. @impl BON-001, BON-003
- [x] 2.2 Add red recovery tests proving a partial establishment (missing or mismatched marker, interrupted subtree) is detected on re-entry, the partial tree is cleaned up, and establishment re-runs atomically; that recoverable transient lock contention retries internally up to a bound; and that an unsupported provider surfaces `work_unit_storage_unavailable` through the change-04 `runtime/work_unit_storage.py` probe before any write, with cleanup on every failure/cancellation path. @impl BON-001, BON-003
- [x] 2.3 Implement `agent/src/deerflow_deep_research/runtime/bootstrap_bundle.py` (`BootstrapBundleStore.create` async factory + `establish_bundle`/`read_marker`) reusing the verified `TrustedRuntimeEnvelope` workspace host path, the change-04 `verify_runtime_work_unit_storage` probe (idempotent; may run alongside the work-unit store's own probe in `_context`), `LOCK_NB` deadline retries, `asyncio.to_thread`, atomic marker publication, descriptor-relative no-follow checks, partial-tree cleanup, and injected clock/fault hooks; establish only the `request/` subtree + marker (all other subtrees remain lazy) until tasks 2.1 and 2.2 are green; the store never exposes a real host path to the node. @impl BON-001, BON-003, WOU-006
- [x] 2.4 Add red blocking-I/O and cancellation tests proving establishment reads/writes/locks/fsync do not block the event loop, lock acquisition terminates at its deadline, cancellation cleans up without a partial bundle, and a cancellation during establishment leaves no marker observable as active. @impl BON-001, BON-003

## 3. Real bootstrap node, capability injection, and terminal routing

- [x] 3.1 Add red node tests in `agent/tests/graph/test_bootstrap_node.py` for `build_real`: on a bound marker it routes `needs_input` toward `hitl1` and makes no `run_agent`/model/web/MCP/ACP/DeerFlow-`task` call; on a binding divergence (identity mismatch, unsupported marker schema/version, request-digest mismatch) it produces a typed `FailureCode` and does not route onward to research; it ignores `fixture_plan` for its route; and on unrecoverable failure it sets `phase_status=TERMINAL`, `terminal_status=BLOCKED`, `terminal_reason=GATE_BLOCKED`, `route=exhausted` and publishes no inconsistent bundle or route, using a focused fake `BootstrapBundleStore`. @impl BON-002, BON-003, GAK-002
- [x] 3.2 Implement `graph/nodes/bootstrap/node.py` `build_real` consuming `dependencies.bootstrap_bundle` to establish, read, and validate the marker, then return the route/terminal update through `engine/fake_control.node_update` until task 3.1 is green; the fake factory in `fake.py` is unchanged. @impl BON-002, BON-003
- [x] 3.3 Add red wrapper tests in `agent/tests/graph/test_node_wrapper.py` for a declare/forbid branch mirroring work units: a node declaring `BOOTSTRAP_BUNDLE` receives `context.bootstrap_bundle` and fails before factory construction when it is missing; a non-declaring node fails when a bootstrap bundle is attached; the attached capability never enters other nodes, `NodeAgentContext`, prompts, tool schemas, or checkpoint state. @impl BON-001, BON-002, NOA-001, REG-009
- [x] 3.4 Extend `graph/builder.py:_node_wrapper` with the `BOOTSTRAP_BUNDLE` declare/forbid attach branch (copied from the proven `declares_work_units` checks); add the `bootstrap` terminal route in BOTH topology representations — `builder.py` maps `exhausted → END` and `graph/topology.py:NORMALIZED_EDGES` adds `TopologyEdge("bootstrap","exhausted","blocked")` (matching the other phases' `exhausted` target in `TERMINALS`) — while keeping `needs_input`/`profile_complete` and the normalized node order unchanged until task 3.3 is green. @impl BON-003, BON-005, REG-001, REG-005
- [x] 3.5 Add red runtime tests in `agent/tests/runtime/test_research_handlers.py` that `ResearchActionHandler._context` constructs `BootstrapBundleStore` for start/resume and attaches it to `GraphInvocationContext`, while status/cancel (`include_work_units=False`) construct neither the work-unit store nor the bootstrap store and remain checkpoint-only; an unavailable workspace surfaces as `work_unit_storage_unavailable` without checkpoint mutation and cancellation still propagates. @impl BON-001, BON-003, RUI-006
- [x] 3.6 Wire `BootstrapBundleStore.create` into `runtime/research.py:_context` (gated by the existing `include_work_units` flag so status/cancel stay checkpoint-only) until task 3.5 is green. @impl BON-001, BON-003, RUI-006

## 4. Mixed-graph integration and same-thread start edge cases

- [x] 4.1 Add red tests in `agent/tests/graph/test_implementation_map.py` that the mixed map (`bootstrap=real`, every other phase `fake`) selects the real bootstrap factory (not the unavailable sentinel) and compiles, the full-fake map (all `fake`) is unchanged, and the normalized node order plus all non-bootstrap conditional edges are identical — mirroring the existing `test_explicit_mixed_override_preserves_identical_graph_shape` pattern (which today substitutes a `_fake_factory` as `real_factory` because no real factory exists yet). @impl BON-005, REG-001
- [x] 4.2 Extend `runtime/research.py:ResearchGraphRecipe` to accept an `implementation_modes` override (default all-fake) so a mixed recipe can select `bootstrap=real` without changing the production full-fake recipe until task 4.1 is green. @impl BON-005, REG-001
- [x] 4.3 Add red lifecycle regression tests in `agent/tests/integration/test_bootstrap_lifecycle.py` extending the existing `test_research_lifecycle_tool.py` edge-case patterns (duplicate same-message start, different-message `thread_research_exists`, already-terminal resume/cancel) to the real-bootstrap mixed recipe — proving the REG-004/RUI-003 invariants are preserved, not re-implemented — plus new research-scoped establishment (a start for research A never touches research B's bundle) and marker `schema_version`-mismatch / cross-research binding rejection, all through the real handlers with focused fakes and no model call. @impl BON-004, REG-004, RUI-003
- [x] 4.4 Add a red mixed-graph end-to-end test (through the handlers using the `implementation_modes` recipe override) that runs real bootstrap → `hitl1` → remaining fake phases to completion, asserts bootstrap writes `workspace/deep-research/<rid>/request/marker.json` and routes `needs_input`, no research model/web/subagent/synthesis/report side effect occurs, and the lifecycle result remains `implementation_mode=full_fake`; add the parallel full-fake regression proving the fake bootstrap path (and the existing `test_research_graph.py` "no diagnostics subdir" assertion) is unchanged. @impl BON-001, BON-004, BON-005, REG-002, REG-005
- [x] 4.5 Add a red partial-directory recovery integration test that interrupts establishment mid-creation, re-enters, and asserts the partial bundle is cleaned up and re-established without being observed as active or completed. @impl BON-001, BON-003

## 5. Structure, documentation, and configuration invariants

- [x] 5.1 Add the exact new production paths (`domain/bootstrap.py` and `runtime/bootstrap_bundle.py`) to `openspec/governance/project-structure.toml` required-current paths; render the generated `agent/AGENTS.md` block with `python3 openspec/governance/check_project_architecture.py --render-guide`; and pass `check_project_architecture.py`. Do not enumerate every test file. @impl PRS-001, PRS-004
- [x] 5.2 Update human-authored `agent/AGENTS.md` (Current Status / Node Packages), `agent/README.md`, and the numbered/master backlog plan status (`_backlog/plans/deep-research-05-bootstrap-node.md`) with the real bootstrap, atomic bundle establishment + marker, non-gated binding-validation replacing the fixture pass, `BOOTSTRAP_BUNDLE` capability injection, the `bootstrap --exhausted--> blocked` terminal edge, and the unchanged full-fake/mixed-graph boundary; regenerate the rendered topology snapshot (`agent/docs/deep-research-topology.md` via `scripts/render_topology.py`) to include the new edge. @impl BON-001, BON-002, BON-003, BON-005, REG-005
- [x] 5.3 Verify `openspec/governance/req-registry.yaml` contains the `BON` prefix and BON-001 through BON-005 descriptions, and that the active `specs/bootstrap-node/spec.md` delta owns `> req: BON-001, BON-002, BON-003, BON-004, BON-005` with no modified-capability delta. @impl BON-001, BON-002, BON-003, BON-004, BON-005
- [x] 5.4 Run the existing configure/doctor contract tests and project doctor in the configured environment; verify bootstrap reuses the change-04 `work_unit_storage` readiness classification with no new diagnostic dimension, and confirm no `config.yaml`, `extensions_config.json`, skill, Agent/SOUL, mount, dependency, startup-only field, `backend/`, or `frontend/` edit is needed. @impl DEC-005

## 6. Verification and hard done conditions

- [x] 6.1 Run focused domain/runtime/graph/bootstrap tests, then `cd agent && make test-unit && make test-contract`; fix all failures.
- [x] 6.2 Run `cd agent && make test-viability && make test-durability && make test-blocking-io`; include the reflected `infra_probe` provider-reopen/subprocess-restart path as the environment-appropriate end-to-end infrastructure check. @impl RUI-001, RUI-005, BON-001
- [x] 6.3 Run `cd agent && make test` and verify the complete agent-owned suite, including full-fake and mixed-bootstrap lifecycle paths, is green.
- [x] 6.4 Run `cd agent && make format && make lint && make lock-check`; fix formatting, lint, or lock drift without adding an unused dependency.
- [x] 6.5 Run `python3 openspec/governance/check_project_architecture.py`, `python3 openspec/governance/check_project_reqs.py`, and `python3 openspec/governance/check_project_specs.py`; all three must pass with no structural drift, duplicate/unregistered/orphan/reused-retired requirement IDs, delta headers in main specs, or missing purpose/requirements/req headers.
- [x] 6.6 Inspect `git diff -- backend frontend config.example.yaml extensions_config.example.json` and verify it is empty; inspect the final change diff for one bundle-establishment authority, one binding-validation path, one bootstrap route authority, and complete `@impl` traceability before archive.
