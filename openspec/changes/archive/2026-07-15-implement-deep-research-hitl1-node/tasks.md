## 1. Domain contracts: profile, parser, request-bundle protocol, and state

- [x] 1.1 Add red deterministic tests in `agent/tests/domain/test_profile.py` for frozen extra-forbid `StructuredBrief`, `PartialResearchProfile`, and `ResearchProfile` contracts with `schema_version=1`; closed enums `ResearchDepth`, `TargetAudience`, `OutputFormat`, `CostTolerance`, and `TimeBudget`; bounded `must_answer` (1-8 items, each <= 256 chars), `scope_boundaries` (<= 2,048 chars), and `custom_notes` (<= 1,024 chars); canonical JSON serialization; final-profile rejection of missing dimensions unless `degraded_profile=True`; invalid enum/extra field/malformed JSON rejection; and deterministic `parse_profile_response(text) -> PartialResearchProfile` for JSON and free text with unknown enum values treated as unset. @impl HIN-001, HIN-003
- [x] 1.2 Implement `agent/src/deerflow_deep_research/domain/profile.py` with the profile contracts, enums, canonical JSON helpers, missing-dimension helper, merge helper, and deterministic parser until task 1.1 is green. @impl HIN-001, HIN-003
- [x] 1.3 Add red tests for request-bundle profile path/protocol: `domain/bundle.py` exposes `profile_path(research_id) -> workspace/deep-research/<rid>/request/profile.json`, rejects malformed research ids, classifies the path as content/request-bundle owned, and a runtime-checkable pure `RequestBundleStoreProtocol.write_profile(profile: ResearchProfile) -> ContentRef` is available without host path fields. @impl HIN-004, REG-010
- [x] 1.4 Extend `domain/bundle.py` and the appropriate domain capability module (`domain/profile.py` or `domain/node_spec.py` import under `TYPE_CHECKING`) with the request-profile path helper and pure write protocol until task 1.3 is green. @impl HIN-004, REG-010
- [x] 1.5 Add red state tests in `agent/tests/domain/test_state.py` for new checkpoint fields: `profile_ref: ContentRef | None`, `research_depth: str`, `target_audience: str`, `output_format: str`, `cost_tolerance: str`, `time_budget: str`, `must_answer_questions: tuple[str, ...]`, `degraded_profile: bool`, `pending_profile: dict[str, Any] | None`, and `profile_followup_round: int`; prove defaults preserve version-2 checkpoint compatibility, fields appear in `ResearchState`, `ResearchCheckpoint`, `OWNERSHIP_TABLE`, controller-authorized reducer ownership, and the local authority-writer guard if required by `domain/state.py`; prove `RESEARCH_STATE_SCHEMA_VERSION` is not bumped. @impl HIN-003, HIN-004, REG-006, REG-007, REG-011
- [x] 1.6 Extend `domain/state.py` with final and transient profile fields, bounds, ownership entries, default validation, serialization support, and controller writer authorization until task 1.5 is green. @impl HIN-003, HIN-004, REG-006, REG-007, REG-011

## 2. Runtime capabilities: request-bundle writer and real node-agent injection

- [x] 2.1 Add red runtime tests for `agent/src/deerflow_deep_research/runtime/request_bundle.py::RequestBundleStore` that writes canonical `profile.json` atomically under the established `request/` subtree, computes the `ContentRef.content_hash` from canonical JSON bytes, fsyncs/cleans staging files, rejects symlink/path escape and cross-research paths, uses `asyncio.to_thread` for blocking I/O, and never exposes host paths in errors. @impl HIN-004, REG-010
- [x] 2.2 Implement `runtime/request_bundle.py` until task 2.1 is green, reusing the change-04/05 shared workspace and POSIX primitives where possible without extending bootstrap authority, creating a second phase cursor, or creating an evidence authority. @impl HIN-004, REG-009, REG-010
- [x] 2.3 Add red tests in `agent/tests/domain/test_node_spec.py` and `agent/tests/unit/test_invocation_context.py` for an optional `NodeBuildDependencies.request_bundle` / `GraphInvocationContext.request_bundle` field, plus a `NodeCapability.REQUEST_BUNDLE` member that can be declared by HITL1 real and is rejected when attached to undeclared nodes. @impl HIN-004, PRS-003
- [x] 2.4 Extend `domain/node_spec.py`, `domain/invocation.py`, `graph/builder.py`, and runtime context validation so only real factories declaring the request-bundle capability receive it; non-declaring nodes fail on accidental injection. @impl HIN-004, PRS-003
- [x] 2.5 Add red tests in `agent/tests/unit/test_research_runtime_capabilities.py` proving `ResearchGraphRecipe` detects `hitl1=real`, rejects `hitl1=real` unless `bootstrap=real`, constructs a real `RuntimeNodeAgentBridge` with zero tools and one model call per bridge invocation for real HITL1, attaches the request-bundle writer only for real HITL1 start/resume contexts, and leaves full-fake recipes on unavailable capabilities with no request-bundle writer. @impl HIN-001, HIN-004, HIN-005, NOA-001, NOA-002
- [x] 2.6 Wire `runtime/research.py` to construct the real node-agent bridge and request-bundle writer for the mixed real-HITL1 recipe until task 2.5 is green. @impl HIN-001, HIN-004, HIN-005, NOA-001, NOA-002

## 3. Brief prompt and HITL1 node behavior

- [x] 3.1 Add red tests in `agent/tests/graph/test_hitl1_prompts.py` for `build_brief_prompt(question: str) -> NodeExecutionRequest`, `build_followup_context(partial, missing) -> str`, and `parse_brief_output(text: str) -> StructuredBrief`; verify the original question is present, expected output demands exactly one JSON object, context JSON stays <= 2,048 chars with stable enum options, and invalid/missing/non-JSON brief output is rejected. @impl HIN-001, HIN-002
- [x] 3.2 Implement `agent/src/deerflow_deep_research/graph/nodes/hitl1/prompts.py` until task 3.1 is green. @impl HIN-001, HIN-002
- [x] 3.3 Add red node tests in `agent/tests/graph/test_hitl1_node.py` for first visit: `build_real` calls `dependencies.capabilities.run_agent()` with the brief request, validates `StructuredBrief`, retries once on invalid output, calls `interrupt()` with a `PendingResearchInterrupt` containing compact structured context, and suspends without writing profile fields. Use fake capabilities returning `NodeExecutionResult.summary`, not `FakeToolCallingModel` directly. @impl HIN-001, HIN-002
- [x] 3.4 Implement the first-visit brief-generation and interrupt path in `graph/nodes/hitl1/node.py`; keep `fake.py` unchanged. @impl HIN-001, HIN-002
- [x] 3.5 Add red node tests for resume paths: complete valid `AcceptedHumanResponse` writes `profile_ref` from `request_bundle.write_profile()`, sets all final short fields, consumes response ids, clears `pending_profile`, and routes `accepted`; `InternalCancelDecision` routes `cancel` with `terminal_status=CANCELLED` / `terminal_reason=USER_CANCELLED` and writes no profile fields; request-id mismatch raises `ValueError("response_mismatch")`. @impl HIN-003, HIN-004
- [x] 3.6 Implement complete-response, cancel, mismatch, and final profile storage paths until task 3.5 is green. @impl HIN-003, HIN-004
- [x] 3.7 Add red tests for restart-durable follow-up: incomplete response writes `pending_profile`, increments `profile_followup_round`, consumes ids, and routes `needs_followup`; a new invocation with that checkpoint emits a follow-up interrupt with ordinal 2 asking only for missing fields; a later valid response merges checkpointed progress and routes `accepted`; no closure/local variable is required. @impl HIN-002, HIN-003
- [x] 3.8 Implement durable follow-up logic in `build_real` using checkpoint state and the explicit `needs_followup` route until task 3.7 is green. @impl HIN-002, HIN-003
- [x] 3.9 Add red tests for bounded/error paths: third answer round still incomplete records degraded final profile with `degraded_profile=True`; brief validation fails twice -> `route=exhausted`, `terminal_status=BLOCKED`, no interrupt; `run_agent` raises or returns failure -> fail closed before interrupt and no partial/final profile write; unrecognizable enum values remain unset and trigger follow-up. @impl HIN-001, HIN-003, HIN-005
- [x] 3.10 Implement bounded follow-up, exhausted, and run-agent failure handling until task 3.9 is green. @impl HIN-001, HIN-003, HIN-005

## 4. Topology, implementation map, and E2E

- [x] 4.1 Add red topology/contract tests that `Hitl1Result` permits `accepted`, `cancel`, `needs_followup`, and `exhausted`; `NORMALIZED_EDGES` and `build_research_graph()` include `hitl1 --needs_followup--> hitl1` and `hitl1 --exhausted--> END/blocked`; the topology snapshot changes only by those two edges; and full-fake route behavior remains unchanged. @impl HIN-005, REG-001, REG-002
- [x] 4.2 Extend `graph/nodes/hitl1/contracts.py`, `graph/topology.py`, `graph/builder.py`, and the committed topology snapshot/tests until task 4.1 is green. @impl HIN-005, REG-001, REG-002
- [x] 4.3 Add red implementation-map tests that mixed mode (`bootstrap=real`, `hitl1=real`, every other phase `fake`) selects both real factories and compiles; `hitl1=real` without `bootstrap=real` fails closed before graph invocation; full-fake map is unchanged; real HITL1 declares the request-bundle capability but no work-unit/bootstrap capability; fake HITL1 declares no request-bundle capability. @impl HIN-005, PRS-003
- [x] 4.4 Extend the HITL1 `NODE_SPEC`, implementation map fixtures, and recipe override support until task 4.3 is green. @impl HIN-005, PRS-003
- [x] 4.5 Add a red mixed-graph lifecycle E2E through handlers that runs real bootstrap -> real HITL1 -> interrupt -> resume valid profile -> remaining fake phases to completion using fake node-agent capabilities and a temporary request-bundle writer; assert `profile.json`, `profile_ref`, final short fields, consumed response ids, `implementation_mode=full_fake`, and no web/subagent/synthesis/report side effects. @impl HIN-001, HIN-004, HIN-005
- [x] 4.6 Add red E2E coverage for incomplete response -> restart/recompile -> follow-up resume -> completion, plus brief-generation double-invalid -> terminal blocked with no pending interrupt. @impl HIN-002, HIN-003, HIN-005, REG-005
- [x] 4.7 Implement remaining lifecycle/runtime wiring until tasks 4.5 and 4.6 are green. @impl HIN-001, HIN-002, HIN-003, HIN-004, HIN-005
- [x] 4.8 Add a full-fake regression test proving fake HITL1 and existing lifecycle E2E remain unchanged and do not construct `RuntimeNodeAgentBridge` or request-bundle writer. @impl HIN-005, REG-002

## 5. Structure, documentation, and governance

- [x] 5.1 Verify the active delta specs for `hitl1-node`, `research-graph-lifecycle`, and `project-structure` still own the two new HITL1 route labels, the real-HITL `langgraph.types.interrupt` exception, request-bundle profile storage, profile checkpoint fields, version-2 compatibility, and unchanged `PendingResearchInterrupt` / resume wire schemas after implementation edits. @impl HIN-002, HIN-004, HIN-005, REG-001, REG-002, REG-003, REG-006, REG-007, REG-008, REG-009, REG-010, REG-011, PRS-002, PRS-003
- [x] 5.2 Add exact new production paths (`domain/profile.py`, `graph/nodes/hitl1/prompts.py`, and `runtime/request_bundle.py`) to `openspec/governance/project-structure.toml`; update the architecture checker fixtures/rules for real-HITL interrupt imports and request-bundle capability surfaces; render the generated `agent/AGENTS.md` block with `python3 openspec/governance/check_project_architecture.py --render-guide`; pass `check_project_architecture.py`. @impl PRS-001, PRS-002, PRS-003, PRS-004
- [x] 5.3 Update human-authored `agent/AGENTS.md` (Current Status / Node Packages / state authority), `agent/README.md`, and `_backlog/plans/deep-research-06-hitl1-node.md` with real HITL1, structured profile contracts, node-agent brief generation, durable follow-up, request-bundle `profile.json`, explicit HITL1 self/blocked routes, and the unchanged `backend/`/`frontend/` boundary. @impl HIN-001, HIN-002, HIN-003, HIN-004, HIN-005
- [x] 5.4 Verify `openspec/governance/req-registry.yaml` contains the `HIN` prefix and HIN-001 through HIN-005 descriptions, and that active delta specs own all HIN ids plus modified REG-001/002/003/005/006/007/008/009/010/011 and PRS ids with no duplicate/unregistered/orphan/reused-retired requirements. @impl HIN-001, HIN-002, HIN-003, HIN-004, HIN-005, REG-006, REG-007, REG-008, REG-009, REG-010, REG-011, PRS-004
- [x] 5.5 Run configure/doctor contract tests and project doctor; verify HITL1 adds no `config.yaml`, `extensions_config.json`, public/custom skill, Agent/SOUL, mount, dependency, startup-only field, `backend/`, or `frontend/` changes. @impl DEC-005

## 6. Verification and hard done conditions

- [x] 6.1 Run focused domain/runtime/graph/HITL1 tests, then `cd agent && make test-unit && make test-contract`; fix all failures.
- [x] 6.2 Run `cd agent && make test-viability && make test-durability && make test-blocking-io`; verify mixed real-HITL1 E2E and restart-follow-up paths use existing infrastructure boundaries.
- [x] 6.3 Run `cd agent && make test` and verify the complete agent-owned suite, including full-fake, mixed-HITL1, blocked, and restart-follow-up lifecycle paths, is green.
- [x] 6.4 Run `cd agent && make format && make lint && make lock-check`; fix formatting, lint, or lock drift without adding unused dependencies.
- [x] 6.5 Run `python3 openspec/governance/check_project_architecture.py`, `python3 openspec/governance/check_project_reqs.py`, and `python3 openspec/governance/check_project_specs.py`; all three must pass.
- [x] 6.6 Run `openspec validate implement-deep-research-hitl1-node --strict`; fix any artifact/schema issue.
- [x] 6.7 Inspect `git diff -- backend frontend config.example.yaml extensions_config.example.json` and verify it is empty; inspect final diff for one profile authority, one brief-generation path, no closure-held progress, one request-bundle profile writer, explicit HITL1 follow-up/blocked routes, and complete `@impl` traceability before archive.
