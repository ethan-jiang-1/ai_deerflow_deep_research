## 1. Batch 1 Red Tests And Current-Asset Audit

- [x] 1.1 Build a current test inventory from pytest collection, classify each asset by stable seam and authenticity level, and record which historical recommendations are already covered, stale, duplicated, or tautological. Add red contract tests for inventory schema and stale selector detection. @impl EVH-006, EVH-007
- [x] 1.2 Create the machine-readable thirteen-incident mapping with risk family, lowest responsible seam, deterministic pytest node ids, and stable invariant/error; make the mapping checker red for missing or incorrectly selected coverage. @impl EVH-006
- [x] 1.3 Add red tests for all-real graph compilation/imports, non-interactive policy forwarding, model/tool readiness diagnostics, legal sandbox identity, mounted-workspace store creation, and unique demo thread/run/research identity. @impl EVH-006
- [x] 1.4 Add red tests for distinct per-node capability/policy resolution, allowed-tool-to-typed-spec completeness, budget admission with large tool results and call boundaries, structured-output repair/fallback, and mixed success/failure gate fatigue. @impl EVH-006, EVH-008
- [x] 1.5 Add red contract tests proving deterministic, live, release, and Postgres selector sets have the required inclusion/exclusion behavior and that the deterministic lane denies external network access. @impl EVH-009
- [x] 1.6 Add red governance contract tests for unknown test-side `@impl`, an alive requirement without a collected deterministic test, and package-only references that do not identify a collected test. @impl EVH-009, EVH-010

## 2. Batch 1 Test Control Plane And Incident Closure

- [x] 2.1 Implement the test-owned incident inventory schema/checker, connect every incident to current collected deterministic selectors, and replace tautological regression claims with tests at the named real seam. @impl EVH-006
- [x] 2.2 Add shared fixtures for a valid `TrustedRuntimeEnvelope`, real local sandbox mappings, temporary mounted workspace paths, unique thread/run/research identity, deterministic time/randomness, and redacted diagnostics without adding production test hooks. @impl EVH-006, EVH-007
- [x] 2.3 Extend the scripted model fixture for ordered responses, tool calls, usage metadata, malformed output, timeout, and provider failure; add small typed scripted web tools with fixed results and failures. @impl EVH-007, EVH-008
- [x] 2.4 Implement the missing incident regressions and any narrowly required production fixes using red-before-green slices; keep `backend/`, `frontend/`, production topology, state schema, and sandbox layout unchanged. @impl EVH-006
- [x] 2.5 Add `workflow`, `requires_llm`, and `release_e2e` marker semantics plus `make test-fast`, `make test-integration`, and a complete network-free `make test`; retain existing focused targets as compatible subsets. @impl EVH-009
- [x] 2.6 Implement the requirement-to-collected-test checker, add it to permanent governance commands, and annotate owning test surfaces with exact `@impl EVH-001..010` references. @impl EVH-009, EVH-010
- [x] 2.7 Add an agent-owned PR workflow for lint, governance, fast tests, and deterministic integration/workflow tests with no model credentials or public-network dependency. @impl EVH-005, EVH-009
- [x] 2.8 Run the Batch 1 gate: incident checker, requirement coverage checker, selector contracts, `make test-fast`, `make test-integration`, `make lint`, and existing viability/durability/blocking-I/O targets; record the passing selectors in the inventory. @impl EVH-006, EVH-009, EVH-010

## 3. Batch 2 Typed Scenario Corpus

- [x] 3.1 Add red tests for the test-owned scenario model covering stable id, risk family, requirement/regression ids, entrypoint, authenticity, preconditions, scripted/live inputs, expected outcomes/artifacts, hard invariants, metrics, and permitted degradation. @impl EVH-001, EVH-007
- [x] 3.2 Implement the minimal scenario model and explicit scripted/live/release runner interfaces; reject unsupported authenticity claims and include scenario identity in every failure. @impl EVH-001, EVH-007
- [x] 3.3 Port quick factual, claim verification, insufficient evidence, prompt injection, malformed output, tool unavailable/timeout, budget exhaustion, partial success, checkpoint control, and filesystem failure into reusable scenario families. @impl EVH-001, EVH-003, EVH-004, EVH-007
- [x] 3.4 Replace state-dictionary/string-only eval tests with scenarios that traverse the real lifecycle, untrusted-data, ledger, gate, bridge, checkpoint, or filesystem seam they claim to cover; retain pure metric tests only for independent worked examples. @impl EVH-002, EVH-003, EVH-004, EVH-008
- [x] 3.5 Expand pure metrics to citation completeness, contradiction recall, and unsupported-major-claim counts, and separate hard-invariant results from quality scores. @impl EVH-002

## 4. Batch 2 Deterministic Agent Workflow Conformance

- [x] 4.1 Inventory every available real node and add a parameterized conformance matrix requiring one success and one highest-risk failure/repair/degradation/exhausted scenario at the `NodeSpec`/capabilities seam. @impl EVH-007, EVH-008
- [x] 4.2 Add zero-tool model-node conformance through the real runtime bridge and policy path for HITL1, topic planning, synthesis, HITL2, rerun, readiness, and final delivery where applicable. @impl EVH-008
- [x] 4.3 Add worker conformance scenarios that traverse real resolver, bridge, scripted web tool, middleware, policy, budget, structured result, attempt-scoped filesystem, submit validator, ledger, gate, and checkpoint for Wave0, targeted evidence, and Wave1. @impl EVH-003, EVH-004, EVH-008
- [x] 4.4 Add mixed-graph real-prefix scenarios through handlers for each high-risk prefix, keeping unrelated later nodes fake and asserting route, checkpoint, accepted submissions, artifacts, and implementation-mode honesty. @impl EVH-001, EVH-008
- [x] 4.5 Add real-seam fault injection for duplicate resume, cancel, timeout, partial publication, stale checkpoint, conflicting worker result, and restart recovery; require idempotent recovery or explicit terminal outcome. @impl EVH-003, EVH-010
- [x] 4.6 Add adversarial source scenarios through scripted tools and the real worker/gate path for prompt injection, forged submission text, route-like content, path traversal, duplicate/SEO sources, and unavailable/paywalled sources. @impl EVH-004, EVH-008
- [x] 4.7 Run the Batch 2 gate: the entire deterministic suite under network denial, scenario authenticity/coverage contracts, full-fake regressions, all mixed-prefix scenarios, viability, durability, blocking-I/O, lint, and governance. @impl EVH-001, EVH-003, EVH-004, EVH-008, EVH-009
  - Verified 2026-07-17: asset coverage (13 incidents / 11 real nodes / 1205 collected deterministic tests), requirement-to-test coverage, strict change validation, architecture governance, lock check, and lint pass. Fast passes (1109); integration passes (91, plus 4 Gateway-unavailable skips and 1 Postgres deselection); complete deterministic union passes (1200, plus the same 4 skips and 1 deselection). Full-fake/mixed-prefix passes (26), scenario/coverage contracts pass (24), viability passes (5), durability passes (8 plus 1 deferred Postgres skip), and blocking-I/O passes (9). Repository-wide requirement/spec main-file drift predates this change and remains explicitly pending under final gate 7.3.

## 5. Batch 3 Live Canaries And Behavioral Reports

- [x] 5.1 Add red preflight and selection tests proving explicitly selected live runs fail clearly on missing credentials/config and never silently skip, while normal deterministic tests remain credential-free. @impl EVH-005, EVH-009
  - Verified 2026-07-17: preflight tests went red before implementation for missing model and web configuration, then pass (7 focused tests). Live collection selects only the two `tests/live` probes while the deterministic expression selects none; an empty-credential `make test-live` exits non-zero with `live_model_credentials_missing` / `live_web_credentials_missing` rather than skipping.
- [x] 5.2 Implement live scenario execution and redacted reporting for attempts, hard invariants, quality metrics, model/tool identity, tokens and cost when available, tool calls, retries, and wall time. @impl EVH-002, EVH-005, EVH-007
  - Verified 2026-07-17: `tests.scenarios.live.LiveScenarioRunner` was implemented red-first and its 5 focused tests pass. Reports expose bounded attempts/retries, independent hard invariants, pure quality metrics, provider/tool ids, optional tokens/cost, tool-call totals, wall time, and redacted diagnostics; mismatched route/terminal/artifacts fail with stable scenario/lane/authenticity identity.
- [x] 5.3 Add `@requires_llm` canaries for start-to-HITL1, HITL1-to-topic-planning, and one-topic Wave0 using unique run identities and bounded time/cost/attempts. @impl EVH-005, EVH-009
  - Verified 2026-07-17: live collection contains exactly the three public-entry mixed-prefix canaries plus two preflight probes, while deterministic collection contains none. Canary manifests and harness contracts pass; each invocation has unique thread/run/research identity, one outer attempt, a 32K token budget, and a 300-second deadline. Model-only prefixes allow one model call; Wave0 allows at most three web tool calls plus one final structured-output model call, and additionally requires an observed Tavily call and exactly one Wave0 accepted ledger record. Provider execution is tracked separately by 5.5.
- [x] 5.4 Add `make test-live` and a scheduled/manually dispatched agent live workflow using repository secrets, strict credential preflight, redacted logs, and uploaded scenario reports. @impl EVH-005, EVH-009
  - Verified 2026-07-17: `make test-live` clears/creates `.reports/live`, runs the explicit strict model+web preflight, executes only the live marker lane, and writes redacted per-scenario JSON. The scheduled/manual `agent-live-evaluation.yml` uses repository secrets, a 20-minute job bound, strict preflight, and `upload-artifact`; 13 focused tests, Ruff, workflow YAML parsing, and both empty/local preflight behaviors pass.
- [x] 5.5 Run the live canaries in an appropriately credentialed environment, record the first non-blocking quality baseline and hard-invariant results, and document provider-only behaviors that cannot be faithfully replayed. @impl EVH-002, EVH-005, EVH-010
  - Verified 2026-07-17: strict preflight passed with `model=deepseek, web=tavily`; all three canaries were run. Start-to-HITL1 and HITL1-to-topic-planning passed hard invariants; one-topic Wave0 failed closed after three bounded internal attempts with no accepted ledger record. The first non-blocking baseline, four deterministic regression descents, and remaining provider-only model/tool distribution behavior are recorded in `agent/docs/live-evaluation-baseline-2026-07-17.md`. This is not a Batch 3 pass; 6.6 remains blocked on all canaries passing.

## 6. Batch 3 Full-Real Release Acceptance

- [x] 6.1 Add red tests for release preflight, unique thread/run/research identity per invocation, bounded visible retries, checkpoint isolation, and required terminal/artifact assertions. @impl EVH-005, EVH-009
  - Verified 2026-07-17: release preflight/invocation/runner contracts were implemented red-first and 5 focused tests pass. Explicit confirmation plus model/web credentials are required; thread/run/research/checkpoint identities are unique; at most two attempts are visible; terminal completion, full lifecycle trace, accepted evidence, both final artifacts, citation bindings, containment, cleanup, and checkpoint isolation are independent hard invariants.
- [x] 6.2 Implement `@release_e2e` full-real acceptance from the real public entry through final delivery, asserting lifecycle trace, accepted submissions, report artifacts, citation bindings, containment, and cleanup without reusing prior checkpoints. @impl EVH-004, EVH-005, EVH-009
  - Verified 2026-07-17: the test-owned full-real executor starts at `run_deep_research`, uses an all-real recipe and fresh invocation/checkpoint identity, handles both HITL boundaries, reads accepted evidence from the authoritative store, validates the normal 9-node trace, both final artifacts and citation bindings, proves path containment, and restricts cleanup to the invocation root. Release collection selects exactly one test while deterministic collection selects none; 17 release/live control tests pass. Credentialed execution/pass remains owned by 6.6.
- [x] 6.3 Add `make test-release-e2e` and a manual/reusable release workflow that depends on deterministic gates and fails preflight rather than skipping when its environment is incomplete. @impl EVH-005, EVH-009
  - Verified 2026-07-17: `make test-release-e2e` creates an isolated report directory, runs strict confirmation/model/web preflight, and selects only `release_e2e`. The manual/reusable workflow depends on deterministic lint/assets/requirement/test gates, has no skip/continue-on-error path, uses repository secrets, and uploads the release report. Twelve focused tests, Ruff, YAML parsing, and an empty-environment Make invocation pass their expected success/failure contracts.
- [x] 6.4 Add the live/E2E regression-descent workflow and documentation: every discovered defect records risk/seam classification and either a red-before-green deterministic scenario or an explicit provider-only live rationale. @impl EVH-006, EVH-010
  - Verified 2026-07-17: `agent/docs/regression-descent.md` records all five live discoveries with risk family, lowest stable seam, and either an exact collected deterministic selector or an explicit provider-only rationale. Four defects descended to deterministic regressions; Wave0 model/tool-selection distribution remains honestly live. Complete deterministic collection succeeds (1227 selected / 7 excluded), and the regression-descent governance contract passes.
- [x] 6.5 Produce the DPT invariant parity and release report listing achieved and outstanding quality, resilience, cost, and security evidence without treating a single successful demo as proof. @impl EVH-002, EVH-003, EVH-004, EVH-005
  - Verified 2026-07-17: `agent/docs/dpt-invariant-parity-release-report-2026-07-17.md` records DPT authority/control parity and achieved versus outstanding quality, resilience, cost, and security evidence. Its mechanical contract passes and the verdict remains `NOT READY`: only 2/3 live canaries pass, monetary cost is unavailable, and full-real acceptance has not run. The later release regression now proves deterministic final-file materialization and matching content hashes; full-real evidence remains outstanding.
- [x] 6.6 Run the Batch 3 gate in the appropriate credentialed environment: all live canaries and one isolated full-real acceptance pass their hard invariants; archive reports contain no secrets or raw host paths. @impl EVH-005, EVH-009, EVH-010
  - Completed: deterministic final-file publication is fixed and focused/fast regressions pass. The isolated Wave0 canary and complete credentialed live lane pass (`5 passed`, including all three canaries); every live hard invariant is true and archive scanning found no secrets or raw host paths. Full-real descent has fixed the minimal-profile topic bound plus Wave0, Wave1, and synthesis model-boundary/repair defects, each with a deterministic regression recorded in `agent/docs/regression-descent.md`.
  - Latest run (2026-07-17): isolated full-real attempt 13 ended fail-closed after 319.30s at `first_resume:blocked`. It did not reach synthesis/HITL2 because the provider returned multiple topics despite the prompt-only one-topic instruction; the expanded Wave1 workload exhausted when one source URL was invalid. No checkpoint was reused.
  - Latest run: isolated full-real attempt 14 ended after 219.22s and reached second resume/readiness-final processing. Four of eight release hard invariants passed (`accepted_evidence_present`, `paths_contained`, `cleanup_complete`, `checkpoint_isolated`); four remain false (`terminal_completed`, `lifecycle_trace_complete`, `report_artifacts_present`, `citation_bindings_valid`). This is the first run past HITL2 and narrows the remaining defect to readiness/final delivery.
  - Latest run: isolated full-real attempt 15 ended fail-closed after 160.39s before HITL2. Single-topic, Wave0, and Wave1 passed; synthesis failed only because a provider gap used `severity` while the contract requires integer `priority`, and the bounded repair repeated that shape. The report remained redacted and recorded only `gaps.0.priority:missing` / `gaps.0.severity:extra_forbidden`.
  - Latest run: isolated full-real attempt 16 ended fail-closed after 174.01s in Wave0. A repair response contained several valid sources plus a minority of invalid URLs/unknown status labels; current tuple validation rejected the entire source batch, so no evidence was accepted. The redacted report contains only item error locations/types.
  - Latest run: isolated full-real attempt 17 ended fail-closed after 155.99s at readiness with a fresh identity and cleanup complete. Wave0 accepted after bounded visible retries, Wave1 passed, and synthesis repair returned schema-valid but empty `findings`/`gaps` despite two accepted evidence records. Four invariants passed (`accepted_evidence_present`, `paths_contained`, `cleanup_complete`, `checkpoint_isolated`); four remain false (`terminal_completed`, `lifecycle_trace_complete`, `report_artifacts_present`, `citation_bindings_valid`). The raw lifecycle trace records three consecutive `wave0` visits before Wave1 and remains preserved for retry observability.
  - Fixed and verified: attempt 17 descended into three red-before-green deterministic regressions (`RELEASE-20260717-13..15`). Synthesis now reads accepted canonical result documents through the authoritative store under per-entry/total byte bounds, verifies ledger hash and byte-count commitments, supplies them as untrusted data to both initial and repair calls, and rejects empty or unbacked findings. Readiness now accepts the canonical `h_...` ledger hashes required by `ResearchState`; release lifecycle completeness collapses only consecutive retry visits for ordered-phase comparison while preserving the raw trace in reports. The combined synthesis/readiness/release/store/zero-tool/invocation suite passes (`49 passed`) and targeted Ruff checks pass.
  - Attempt 18 has not entered execution and is not counted yet. The first preflight failed closed on missing explicit confirmation; after adding confirmation, command-level preflight loaded local dotenv and passed, but pytest's own strict preflight received no Tavily credential because the Make entrypoint did not propagate dotenv into the test process. Both failures happened before identity/checkpoint/model/web creation and produced no report. The release entry now shares an optional `.env` argument between preflight and pytest, with explicit environment values taking precedence; the redacted response-shape reporter also retains raw item-level `loc:type` diagnostics before normalization. Descent selectors `RELEASE-20260717-16..17` pass.
  - Attempt 18 execution (2026-07-17) then ran with a fresh identity for 360.69s and failed closed at `first_resume:blocked`. Wave0 reached schema-valid output after bounded retries; Wave1 later exhausted after provider source IDs with invalid characters survived its repair response (`source_id:string_pattern_mismatch`), leaving no accepted evidence. No synthesis/readiness/final artifact phase was reached. A red deterministic regression now normalizes provider source IDs and rebinds claim support/counter refs (`RELEASE-20260717-18`, `16 passed` Wave1 focused suite, Ruff clean).
  - Attempt 19 execution (2026-07-17) ran with another fresh identity for 281.78s and reached Wave2 synthesis, then failed closed as `first_resume:checkpoint_inconsistent` with `suspended_without_interrupt`. Both synthesis responses were schema-invalid in the same provider shape: `findings[].id/description` plus string `gaps[]`; bounded repair repeated it. The normalizer now maps description to statement, converts non-empty gap strings to typed medium-priority gap records, and maps source/URL/support-ref aliases from authoritative evidence back to accepted ledger hashes (`RELEASE-20260717-19`, synthesis focused `9 passed`, Ruff clean).
  - Attempt 20 execution (2026-07-17) ran with a fresh identity for 349.24s, passed Wave0 and Wave1 after bounded repairs, then failed closed as `first_resume:checkpoint_inconsistent` because synthesis repair returned sparse findings without id/priority/confidence and used `source_ids`/`evidence_refs`. Those provider fields now normalize to a deterministic statement-derived finding id, medium defaults, and evidence-backed refs that are resolved to accepted ledger hashes (`RELEASE-20260717-20`, synthesis focused `10 passed`, Ruff clean).
  - Attempt 21 execution (2026-07-17) ran with a fresh identity for 186.66s and ended `first_resume:blocked` before synthesis. Wave0 and the only Wave1 final response were both schema-valid, but the Wave1 submit/gate path still failed and exhausted after six total web calls. The current report has no safe checkpoint gate summary for a non-`checkpoint_inconsistent` terminal result, so the active slice is adding a redacted structural checkpoint summary (phase/trace, accepted count, work status and typed failure codes only) and reproducing the submit/gate failure deterministically before another credentialed run.
  - Attempt 21 descent proved that multi-source Wave1 output preserved provider order while `CandidateResult.source_refs` requires canonical `(source_id, canonical_url)` order; the candidate constructor failed after model schema validation, and the component correctly reduced it to `WORKER_FAILED`. Wave1 now sorts sources and claim refs before writing result/candidate artifacts (`RELEASE-20260717-21`). The safe release checkpoint summary now records only phase/status/route, trace, accepted count, work-status counts, typed failure codes, and interrupt count. Combined Wave1/release tests pass (`28 passed`), with Ruff/format clean.
  - Attempt 22 execution (2026-07-17) ran with a fresh identity for 200.43s. The new checkpoint summary proves Wave0 and Wave1 both passed authoritatively (`accepted_count=2`, two `submitted`, no failure codes, `route=pass`, trace through Wave1). It then failed closed in Wave2 because provider relations used `finding_id_a`/`finding_id_b`; those fields now normalize to `source_finding`/`target_finding`, and response diagnostics include only safe finding/relation/gap counts, item keys/types, and raw/normalized error counts (`RELEASE-20260717-22`, combined focused `35 passed`, Ruff clean).
  - Attempt 23 execution (2026-07-17) ran with a fresh identity for 155.84s. Wave0/Wave1 again passed authoritatively (`accepted_count=2`, no failures); both initial and repaired synthesis responses had zero findings and one typed gap and failed on exactly one normalized schema error: singular `affected_topic` was not mapped to `affected_topics`. That gap alias is now normalized (`RELEASE-20260717-23`, synthesis focused `12 passed`, Ruff clean); gap-only insufficient-evidence synthesis remains a valid bounded outcome.
  - Attempt 24 execution (2026-07-17) ran with a fresh identity for 220.57s. It retained two accepted records and reached synthesis; initial and repair responses repeated the same five-finding/three-relation/four-gap object. Safe diagnostics show all finding and gap normalization succeeded, leaving one root normalization error because relations used `from_finding`/`to_finding`/`type`/`description` and omitted a relation id. That alternate relation shape now maps endpoints/type and derives a deterministic relation id (`RELEASE-20260717-24`, synthesis focused `13 passed`, Ruff/format clean).
  - Attempt 25 execution (2026-07-17) ran with a fresh identity for 239.21s. Wave1's initial output used unknown open-question states but its bounded repair passed; two authoritative submissions remained accepted. Synthesis then repeated a gap-only response and failed on one root normalization error caused by omitted gap id/priority. The normalizer now derives a stable gap id from description/question, applies medium priority, and safe diagnostics record gap keys without values (`RELEASE-20260717-25`, combined focused `28 passed`, Ruff/format clean).
  - Verified 2026-07-17: attempt 26 passed the isolated full-real release lane (`1 passed`, 1279 deselected, 287.55s) with a fresh identity and no outer retry. All eight hard invariants are true: terminal completion, ordered lifecycle completeness with three raw visible Wave0 visits preserved, accepted evidence, both final artifacts, valid citation bindings, path containment, cleanup, and checkpoint isolation. The report records two accepted submissions, two citation-bound claims/refs, 37,597 input tokens, 18,591 output tokens, and 12 web calls without raw content. All three live canaries remain green (`5 passed` complete live lane), and scans of `.reports/live` plus `.reports/release` found no secret patterns or raw host paths. Batch 3 is complete.
  - Remaining for 6.6: a fresh isolated full-real run must complete, include all required ordered lifecycle phases, publish both final artifacts with valid citation bindings, retain accepted evidence, prove containment/checkpoint isolation/cleanup, and pass archive redaction. The checkbox stays open until that evidence exists.

## 7. Documentation, Governance, And Archive Gates

> Current progress (2026-07-17): **40/40 complete**. `RUO-001`, `RUO-003`, and `RUO-004` now have genuine collected deterministic coverage; all required governance, deterministic, viability, durability, blocking-I/O, report-redaction, diff, and repository-boundary gates pass.
> Active slice: `RUO-001` has a real tool/action-input test but still needs checkpoint-injection proof; `RUO-004` is genuinely covered by the complete nine-phase full-fake graph test. `RUO-003` is a confirmed historical implementation gap: the archived task was checked off without a store/controller change or orphan-recovery test. A red-before-green recovery test and the smallest writer-authorized checkpoint transition are now in progress.
> Red verified: `test_recovery_fails_running_attempt_from_prior_generation_with_orphan_reason` runs the real shared Wave0 component from generation 1 after a generation 0 attempt was checkpointed as `running`; current code completes new work but omits the stale attempt from its parent update (`KeyError` on the old attempt id). The green slice is implementing explicit `failed/orphaned` recovery through the shared controller update.
> Green verified: the shared controller now emits reducer-authorized `failed` + `orphaned` + `work_failed` updates for prior-generation running attempts while current-generation work still completes; the focused recovery test plus adjacent domain/kernel/state contracts pass (`66 passed`). `RUO-003` and the complete full-fake `RUO-004` mapping are resolved; `RUO-001` checkpoint-injection proof remains before rerunning governance.
> Traceability unblocked: a real Start handler test now proves `RUO-001` is allowed through action input and persisted in checkpoint state, the shared recovery test owns `RUO-003`, and the complete nine-phase graph test owns `RUO-004`. The combined focused suite passes (`13 passed`) and `make test-req-coverage` passes. No known blocker remains; the complete final governance and deterministic test matrix is now running.
> Shared regression gate passed: Ruff lint/format is clean and the adjacent work-unit domain/state/kernel/store/component, full-fake graph, and non-interactive suites pass (`114 passed`). OpenSpec/project governance, asset coverage, lock, and repository-boundary checks are the active run; the complete deterministic suite follows.
> Governance gate passed: strict OpenSpec validation; project requirements (`128` registered, `0` retired/orphan, `156` occurrences); project specs (`23`, `0` violations); architecture; requirement coverage; assets/incidents (`13` incidents, `11` real nodes, `1275` deterministic tests collected); lock; diff whitespace; and unchanged `backend/`/`frontend/`. Progress remains **39/40** while `make test`, viability, durability, blocking-I/O, and final report redaction rechecks run.
> Full deterministic gate run 1: `1269 passed`, `4 skipped` (Gateway stack unavailable), `7 deselected`, and `2 failed` in 54.94s. Active blockers are `test_duplicate_seo_sources_fail_before_ledger_and_gate_cannot_pass` (one duplicate-source candidate was accepted) and `test_handlers_run_real_prefix_through_wave2_and_keep_later_nodes_fake` (resume returned `blocked` instead of HITL2 suspension). Isolation/order-dependence diagnosis is active; 7.3 remains **39/40**.
> Diagnosis: both failures reproduce deterministically in an isolated two-selector loop (`2 failed` in 1.20s), so they are not suite-order pollution. Duplicate URLs were incorrectly treated like discardable malformed provider records by Wave0 normalization; valid duplicate records must instead reach the independent-source validator and fail closed. The mixed-prefix synthesis fixture is stale after the accepted-evidence semantic hardening: it supplies empty findings and gaps, correctly triggering bounded repair/blocking. The active fix preserves fail-closed duplicate semantics and updates that fixture to an explicit no-fabrication gap.
> Focused fixes verified: duplicate-source normalization again preserves valid duplicate URLs for fail-closed independent-source validation. Mixed-prefix diagnosis further showed its Wave1 fixture forged four runtime-owned source fields; after removing them and representing synthesis as an explicit evidence gap, both original failing selectors pass (`2 passed` in 0.79s). Temporary diagnostics were removed; adjacent worker/synthesis regressions and then the complete deterministic suite are rerunning.
> Full deterministic gate passed on rerun: `1271 passed`, `4 skipped` (the documented unavailable real Gateway app stack), `7 deselected`, and `2` existing Pydantic warnings in 44.15s. The remaining active commands are the explicit viability, durability, and blocking-I/O targets, followed by final governance/report-redaction/boundary rechecks before 7.3 can close.
> Specialized gates passed: viability `5 passed` (with the same 2 existing warnings), durability `8 passed` plus the explicitly deferred Postgres case, and blocking-I/O `9 passed`. Only final report archive redaction plus governance/diff/boundary rechecks remain before marking 7.3 and the change **40/40**.
> Report archive gate passed: all three live reports and the release report are valid JSON; credential-value and raw-host-path scans return no matches; all live hard invariants are true; and release remains 8/8 hard invariants true with `accepted_count=2`, two citation claims/two refs, one outer attempt, and zero outer retries. Final governance/lock/lint/diff/boundary rechecks are running now.

- [x] 7.1 Update `agent/README.md` and `agent/AGENTS.md` with the four asset classes, authenticity ladder, five stable seams, scenario rules, commands, CI cadence, and E2E regression-descent policy. @impl EVH-007, EVH-009, EVH-010
  - Verified 2026-07-17: README operator guidance and AGENTS development policy now define the four asset classes, five-level authenticity ladder, five stable seams, required scenario manifest fields, non-overlapping deterministic/live/release commands, actual workflow names and cadence, strict preflight semantics, hard-invariant versus quality-threshold policy, and red-before-green regression descent. Mechanical content/diff checks pass and related scenario/live/release/inventory contracts pass (`33 passed`).
- [x] 7.2 Update the master strategy with final command/workflow names and close the four absorbed test-asset source plans according to `_backlog/plans/README.md`; close the master plan only after all three batches pass. @impl EVH-006, EVH-010
  - Verified 2026-07-17: after all three batches passed, the master records the final deterministic/live/release commands, governance commands, workflow names/cadence, full-real eight-invariant pass, archive scan, and regression-descent policy. The four absorbed source plans and master moved unchanged by filename to `_backlog/_done/_closed_plans/` as `CLS-003..007`; active/closed/root indexes now agree on seven closed plans and next id `CLS-008`, change links point to the archived master, old active paths have no remaining references, and diff checks pass.
- [x] 7.3 Run `openspec validate evaluate-harden-deep-research-graph --strict`, `python3 openspec/governance/check_project_reqs.py`, `python3 openspec/governance/check_project_specs.py`, `python3 openspec/governance/check_project_architecture.py`, the new requirement-coverage and incident-coverage checkers, `cd agent && make lock-check && make lint && make test && make test-viability && make test-durability && make test-blocking-io`, and verify `backend/`/`frontend/` are unchanged. @impl EVH-001, EVH-003, EVH-004, EVH-005, EVH-006, EVH-007, EVH-008, EVH-009, EVH-010
  - In progress 2026-07-17: strict change validation, architecture governance, incident/scenario coverage (`13 incidents`, `11 real nodes`, `1273 deterministic tests collected), requirement-to-collected-test coverage, lock check, Ruff lint, diff whitespace check, and unchanged `backend/`/`frontend/` all pass. Ruff format requires four touched files to be normalized. Project req/spec governance exposes the previously recorded main-spec drift: `evaluation-hardening` remains in delta shape, while final-delivery/readiness/rerun/runtime-operations main specs lack `> req:` headers, causing their 25 registry ids to appear orphaned. The active slice is repairing only those main-spec governance shapes before rerunning the complete final gate.
