# Test Asset Rebalancing Tasks

> Current progress: **51/51 complete**. Deterministic implementation, governance, documentation, and repository boundaries are green. The dependent production change is archived, complete-lane red evidence is preserved, and further credentialed retries plus nightly restoration are explicitly deferred to `_backlog/plans/deep-research-six-case-live-closure.md`. Completing tasks 6.7/6.8 records that honest evidence/cadence handoff, not a six-case green claim. The change is apply-complete and ready for archive review.
>
> Progress rule: update this summary and the active task after every focused red/green slice. Before checking a task, record its focused selector or deterministic governance command, observed result, and any honest scope reduction directly below that task; manual live tasks additionally record the bounded report evidence named by the task.
>
> Hard boundary: this change modifies test-owned `agent/` assets, OpenSpec, documentation, Make targets, and agent workflows only. `backend/` and `frontend/` remain unchanged. Any required change to production behavior or interfaces under `agent/src/` returns to proposal/design review before implementation and triggers a fresh release-proof policy assessment.

## 1. Governance Bootstrap, Canonical Evidence, And Lane Contract

- [x] 1.1 Before any implementation or governance mutation, record `apply_base_revision`, initial worktree status, and protected Git tree hashes for `agent/src`, `backend`, `frontend`, the release runner, and the public entry; record read-only deterministic baseline probes for the empty `workflow` collection and placeholder metrics without snapshotting raw aggregate test counts, and append the exact commands/results plus any pre-existing user changes below this task before checking it. @impl EVH-001, EVH-002, EVH-005, EVH-008, EVH-009
  - Apply base: `1a15040dd0bc3cef4840b32946a4ba42701b4a1a` (`chore(openspec): update rebalance-deep-research-test-assets change artifacts`). Initial `git status --porcelain=v1 -uall` was empty; no pre-existing tracked or untracked user changes were present before the first apply write.
  - Protected Git objects from `git rev-parse HEAD:<path>`: `agent/src` tree `32f747773f31de7d767e90ad52015e06011316b7`; `backend` tree `e033d4751b5f9a517b9fb249d25eceb8092ef1be`; `frontend` tree `b5e77197a08602fe194ded9d8045468d7ec9de3a`.
  - Release control-plane blobs: `agent/tests/scenarios/release.py` `bb2667fd2bcac2ed30de9eca41c18c917a2cc556`; `agent/tests/e2e/test_release_acceptance.py` `51df58d866b899bc275dc3499818b288f6e73d87`; `.github/workflows/agent-release-e2e.yml` `aea955cc0a5815ed37628c2ea6cbe2a870e3cdc6`; `agent/scripts/release_preflight.py` `b45e88e30f15433d77bafca6c81bc1af3b66eb05`.
  - Public-entry blobs: `agent/src/deerflow_deep_research/tool.py` `160380cfaa9c787da10df2d7c5f5978d30b81450`; committed skill source `agent/config/public-skill/deep-research-controller/SKILL.md` `d9114d905ce4c0e2e13037280d2880cc9625a247`; Agent SOUL source `agent/config/agent-template/SOUL.md` `8aeccdf8dba9096c5464a347abb306ff71fb3882`.
  - Checkout-only entry state: `skills/public/deep-research-controller/SKILL.md` was absent and ignored by `.gitignore`; `agent/config/agent-template/config.yaml` existed but was ignored by the repository-wide `config.yaml` rule. Neither has a HEAD object and neither is represented as a protected Git hash.
  - Workflow probe: `cd agent && uv run --extra operations pytest --collect-only -q -m workflow` exited `5` with `no tests collected`; the raw aggregate/deselection count is intentionally not retained as a baseline contract.
  - Metric probes: `cd agent && uv run --extra operations pytest -q tests/eval/test_eval_corpus.py tests/eval/test_quality_metrics.py` passed. `compute_metrics` still produced placeholder evidence: ref syntax `2/3` precision and diversity `3`; one accepted ref for two questions yielded coverage `1/2`; empty denominators yielded precision `0`, diversity/unsupported count `0`, and completeness/coverage/contradiction recall `1`. Both uv commands emitted the pre-existing non-blocking warning that active `VIRTUAL_ENV=backend/.venv` differs from `agent/.venv`.
- [x] 1.2 Establish `openspec/governance/test-evidence-policy.md` as an authority/lifecycle and synchronized-change policy: the `evaluation-hardening` main spec owns approved semantics, its one active delta owns pending modifications until archive/sync, archived artifacts are historical, test-owned registries hold exact enumerable metadata, and agent-owned checkers execute enforcement; register the permanent policy path under the existing exact-enumeration contract in `project-structure.toml`, regenerate the bounded `agent/AGENTS.md` structure block, update structure-governance fixtures, add concise human-authored bootstrap pointers to `openspec/config.yaml` and `agent/AGENTS.md`, list the concern in the governance README without duplicating requirements/catalogs, correct the OpenSpec deterministic-scenario and three-check archive wording, and pass strict OpenSpec plus all three root governance checks before later slices use the policy. @impl EVH-001, EVH-005, EVH-009, EVH-010, PRS-004
  - Red: `cd agent && uv run --extra operations pytest -q tests/contract/test_architecture_governance.py::ArchitectureGovernanceContractTests::test_valid_pending_project_passes` failed with `ERROR [path.missing] required file is missing: openspec/governance/test-evidence-policy.md` after the fixture registered the path and before the policy existed.
  - Green: the same focused selector passed after adding the policy and fixture path; the full `tests/contract/test_architecture_governance.py` suite passed `17` tests. `python3 openspec/governance/check_project_architecture.py --render-guide` exactly matched the bounded block in `agent/AGENTS.md`.
  - Final gates: `openspec validate rebalance-deep-research-test-assets --strict`, `check_project_reqs.py`, `check_project_specs.py`, `check_project_architecture.py`, and `git diff --check` passed. A policy-scope scan confirmed the policy contains only `Authority`, `Lifecycle`, and `Synchronized Changes` routing with no independent `SHALL`, pytest selector, or authenticity catalog.
- [x] 1.3 Define the central frozen `TestEvidenceClaim` contract with stable claim id, exact selector, expected focused selection, requirement ids, one of four closed asset classes, one of five canonical stable seams, optional one of five authenticity levels, at most one scenario-case id, and discovery ids; use synthetic case registries to prove each supplied case resolves to exactly one claim/node id containing its stable case id, reject aliases, unknown or contradictory vocabulary, selector globs/prefixes, and add bounds, validation, and duplicate tests without requiring the not-yet-migrated real scenario catalog. @impl EVH-006, EVH-007, EVH-008
  - Red 1: `tests/contract/test_evidence_claim_contract.py` failed collection with `ModuleNotFoundError: tests.assets.evidence` before the test-owned contract existed. Green 1: the first bounded/identity/case-binding contract passed `25` tests.
  - Red 2: six focused cases then exposed missing requirement/discovery cardinality bounds and asset-class/focused-selection contradictions. Green 2: the completed closed vocabularies, frozen tuple fields, exact-selector rules, bounds, compatibility matrix, duplicate checks, and synthetic case/collection joins passed `34` focused tests.
  - Compatibility: the focused file plus existing incident, node, fault, and lane contracts passed `56` tests. Ruff check, Ruff format check, strict OpenSpec/root governance from task 1.2, and `git diff --check` passed; no real scenario catalog was imported or enforced.
- [x] 1.4 Migrate incident, real-node, and critical-fault inventories to reference central claim ids and canonical seams, preserve replaced/duplicate incident-recommendation status and the complete historical incident-category inventory, remove duplicated selector/authenticity declarations, and add contradictory-reference failure tests. @impl EVH-006, EVH-008
  - Red 1: the three inventory contract files failed collection because `EVIDENCE_CLAIMS`, `claim_index`, and `validate_inventory_claim_references` did not yet exist. Red 2: focused contradictory/unknown-reference cases failed collection for the same missing join API. Red 3: the historical-status contract failed because the old inventory had no explicit `DUPLICATE` state.
  - Green: the central catalog now contains `39` claim ids for `39` unique exact selectors. Incident, real-node, and critical-fault records contain claim ids plus their own domain meaning only; selector/seam/authenticity metadata resolves centrally. RM-09 was honestly downgraded to code correctness with no workflow authenticity. RM-11 shares the topic-planning risk claim, RM-04/RM-13 share the demo-adapter claim, duplicate-resume/cancel share the lifecycle claim, and the HITL2 stale-checkpoint node/fault entries share one claim.
  - Historical inventory: all `13` incident categories remain, distributed as `9 new-regression`, `2 covered`, `1 replaced`, and `1 duplicate`; RM-11 remains replaced and RM-13 is explicitly the duplicate of RM-04 rather than relying on an implicit shared selector.
  - Validation: the four focused claim/inventory contracts passed `55` tests; the wider claim/inventory/lane/regression-descent set passed `60` tests. `scripts/check_test_assets.py` joined `13` incidents, `11` real nodes, and `7` critical faults against `1312` collected deterministic tests. Ruff check/format, strict OpenSpec, `git diff --check`, and the protected-tree diff check passed. One earlier wider pytest command named nonexistent `test_test_lane_contract.py` and collected nothing; it was discarded and replaced by the passing command using the real `test_test_lane_selection.py`.
- [x] 1.5 Use Python syntax-tree inspection across production node packages to freeze and verify loaded `run_agent` calls or method-reference owners (`hitl1`, `topic_planning`, `wave0`, `wave1`, `wave2_synthesis`, `targeted_evidence`); add `workflow` only to the existing qualifying focused selectors, register their real-bridge deterministic workflow claims, and make ownership drift or missing/downgraded coverage fail with the node id; do not infer model use from `NODE_SPEC.capabilities`, require a real bridge loop for `SCRIPTED_REAL_WORKFLOW`, and add positive/negative classification plus a known-violation smoke case proving a mis-scoped/empty discovery cannot pass. @impl EVH-008, EVH-009
  - Red: `tests/contract/test_workflow_node_inventory.py` first failed collection because `tests.assets.workflow_nodes` did not exist; after the AST validator existed it failed on an empty `workflow` collection and six absent central claims.
  - Discovery: Python AST inspection of loaded `.run_agent` attributes, including direct calls and method references but excluding same-name functions and store targets, finds exactly `hitl1`, `topic_planning`, `wave0`, `wave1`, `wave2_synthesis`, and `targeted_evidence`. Positive/negative syntax fixtures and a deliberately empty/mis-scoped node root prove the detector cannot pass vacuously.
  - Workflow proof: six existing selectors now carry `workflow` and central `SCRIPTED_REAL_WORKFLOW` claims. HITL1/topic-planning/Wave2 use the real zero-tool `RuntimeNodeAgentBridge` at the node interface; Wave0/Wave1/targeted-evidence use the real worker bridge/tool-policy/artifact/ledger path at runtime integration. Each selector asserts observable agent/model/tool/store facts; no claim is inferred from capabilities, and RM-09 remains ordinary correctness.
  - Validation: `pytest --collect-only -q -m workflow` collected exactly `6/1326`; `pytest -q -m workflow` passed all `6`. The ownership contract passed `7`, the combined claim/inventory/ownership and six-node test set passed `70`, and the asset checker passed with `13` incidents, `11` real nodes, `7` critical faults, `6` model-workflow nodes, `1319` deterministic aggregate tests, and `6` workflow tests. Ruff check/format, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 1.6 Define the `workflow` marker as a temporally ordered real lifecycle/mixed-graph transition, real bridge-driven agent loop, or real worker submit/gate sequence rather than agent-loop shorthand; make `make test-fast`, `make test-integration`, and `make test-workflow` pairwise disjoint, keep live/release/Postgres excluded, add collection tests proving their exact union equals `make test` deterministic collection and rejects overlap, set live to `requires_llm and not release_e2e`, set release to `requires_llm and release_e2e`, and reject any `release_e2e` test missing `requires_llm`. @impl EVH-008, EVH-009
  - Red: the expanded lane contract failed collection because `FAST_EXPRESSION`, `INTEGRATION_EXPRESSION`, `WORKFLOW_PATHS`, and `WORKFLOW_EXPRESSION` did not exist.
  - Green: `workflow` now means an ordered real lifecycle/mixed-graph transition, bridge-driven agent loop, or worker submit/gate sequence with external adapters replaced. `test-fast` and `test-integration` exclude workflow; `test-workflow` selects zero-API workflow tests while excluding live/release/Postgres. Live is exactly `requires_llm and not release_e2e`; release is exactly `requires_llm and release_e2e` in both the selection registry and Make target.
  - Collection proof: the contract performs real pytest collection for deterministic aggregate, fast, integration, and workflow, proving all three focused deterministic selections are non-empty, pairwise disjoint, and their exact union equals the aggregate. It separately proves `release_e2e and not requires_llm` is empty. The six lane tests passed in `16.16s`; actual `make test-workflow` selected and passed `6` tests with `1322` deselected.
  - Compatibility: lane plus live/release control-plane contracts passed `19`; the release control-plane focused suite passed `11`. Make dry-run showed all five focused expressions correctly. Ruff check/format, strict OpenSpec, corrected residual-expression scans, `git diff --check`, and protected-tree checks passed. An earlier residual scan placed `rg -g` after paths and emitted option errors; it was discarded and rerun correctly.
- [x] 1.7 Update the asset checker to collect deterministic aggregate plus fast/integration/workflow/live/release focused selectors, validate every central claim in its expected selection, directly test each validation rule with minimal invalid fixtures, and add a known collected-selector smoke case proving an empty/mis-scoped subprocess collection cannot pass. @impl EVH-006, EVH-009
  - Red: `tests/contract/test_asset_checker_contract.py` failed collection because the generic `collect_pytest_selectors` and pure `validate_claim_selections` interfaces did not exist.
  - Green: one fail-closed collector now owns aggregate and all five focused subprocess collections; nonzero collection or an empty selector set is an error. The pure claim validator requires all five collections, requires each central claim in its declared focused selection, and rejects the same exact selector in any other focused selection. Minimal tests cover missing expected selector, wrong lane, missing lane collection, and changed expected-lane drift.
  - Detector smoke: a temporary known `@pytest.mark.workflow` test is collected by its correct scope, while `not workflow` produces pytest code `5` and is rejected with the named mis-scoped lane. This proves an empty subprocess discovery cannot satisfy the checker.
  - Real result: the full checker passed with `45` central claims and collections `deterministic=1327`, `fast=1230`, `integration=91`, `workflow=6`, `live=5`, `release=1`; incident/node/fault and AST workflow joins remain green. The new checker contract passed `6`; combined asset/claim/ownership/lane/regression contracts passed `75`. Ruff check/format, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 1.8 Update PR CI to run all three focused deterministic selections and fail when workflow collection is empty or any selector overlaps. @impl EVH-009
  - Red: `tests/contract/test_agent_pr_workflow.py` failed because `.github/workflows/agent-tests.yml` contained `make test-fast` and `make test-integration` but no `make test-workflow`.
  - Green: the agent PR/push deterministic job now runs `make test-assets`, requirement coverage, and exactly one each of `make test-fast`, `make test-integration`, and `make test-workflow`; it does not substitute the overlapping aggregate `make test`. The asset step fails on empty focused collections and claim/lane drift, while the fast lane's collection contract fails on overlap or incomplete deterministic union.
  - Validation: workflow/partition/checker contracts passed `13`; the real asset checker passed with `45` claims and `deterministic=1328`, `fast=1231`, `integration=91`, `workflow=6`, `live=5`, `release=1`. The workflow YAML parsed through `ruamel.yaml`; Ruff, strict OpenSpec, `git diff --check`, and protected-tree checks passed.

## 2. Scenario Families, Cases, And Assertions

- [x] 2.1 Define frozen `ScenarioFamily` and `ScenarioCase` contracts with stable ids, family risk/requirements/permitted-degradation policy, and case lane/entrypoint/asset class/seam/authenticity/bounds/expectations/hard invariants/applicable metrics; reject descriptive-only families and case degradation outside family policy using synthetic fixtures, and keep legacy `Scenario`/`ScenarioRunner` only as the unchanged migration source until task 3.11 rather than weakening the new schema for string pseudo-scripts. @impl EVH-001, EVH-007
  - Red: `tests/contract/test_scenario_case_contract.py` failed collection because the parallel `tests.scenarios.contracts` module did not exist.
  - Green: frozen `ScenarioFamily`, `ScenarioCase`, `ScenarioBounds`, `ScenarioExpectations`, and closed `ScenarioLane` contracts now separate lane-neutral risk/policy from lane-specific entrypoint, canonical evidence vocabulary, bounded execution, outcomes, invariants, and metrics. They reuse the central asset/seam/authenticity enums rather than creating aliases.
  - Synthetic governance: minimal fixtures reject invalid identity/requirements/vocabulary, incomplete cases, unbounded attempts/model/tool/time, duplicate/unknown family/case bindings, descriptive-only families, and degradation outside family policy. The focused contract passed `25` tests.
  - Migration honesty: legacy `tests/scenarios/model.py` and `catalog.py`, including `Scenario`, `ScenarioRunner`, string pseudo-scripts, and all ten `SCENARIOS`, remain unchanged as the migration source; their adjacent eval/fault/adversarial tests passed `15`. Ruff check/format, strict OpenSpec, `git diff --check`, protected-tree checks, and explicit legacy-source checks passed. No real family completeness enforcement was enabled.
- [x] 2.2 Define bounded typed deterministic inputs for model turns, tool results/failures, lifecycle actions, and supported fault points; retain exhausted-response failure in shared scripted adapters, expose only the model/tool calls and bound-tool observations needed by evidence-bearing workflow cases, assert each case's declared call order/bounds through its observation, and reject string pseudo-scripts, credentials, raw host paths, external URLs where forbidden, and oversize inputs without adding persisted LLM trace replay unless a reviewed case proves short scripts insufficient. @impl EVH-001, EVH-007
  - Red: `tests/contract/test_scenario_input_contract.py` failed collection because `tests.scenarios.inputs` did not exist.
  - Green: frozen `ScenarioInputs` now composes bounded typed `ModelTurn`, `ToolStep` with result/timeout/unavailable/error outcomes, `LifecycleAction`, and closed supported `FaultPoint` values. Deterministic cases require `ScenarioInputs`; live/release cases require bounded `LiveRequirements`. No `Any`, string pseudo-script, or universal execution dispatcher was introduced.
  - Bounds/sensitivity: contracts reject wrong collection/item types, oversize cardinality/content/results/arguments, credential-like text, raw macOS/Linux/Windows user paths, and non-synthetic external URLs where URL inputs are used. Model/tool call observations validate exact declared order/count, case call bounds, and bound-tool membership.
  - Adapter continuity: existing `ScriptedChatModel` and `ScriptedTool` remain the execution fixtures and still fail loudly when responses are exhausted; the new contract observes their public call facts rather than reimplementing them. Focused 2.1/2.2 contracts passed `43`; combined contracts plus node-agent bridge and six workflow cases passed `70`. Asset checker passed with `1371` deterministic tests and unchanged focused workflow/live/release counts. Ruff, strict OpenSpec, `git diff --check`, and protected-tree checks passed. No persisted LLM trace replay was added because no migrated case demonstrated that short typed scripts were insufficient.
- [x] 2.3 Define the test-owned `ScenarioObservation` projection and closed invariant registry over observable calls plus checkpoint, validated-ledger, and contained-sandbox facts; add unknown-invariant and redaction tests. @impl EVH-007
  - Red: `tests/contract/test_scenario_observation_contract.py` failed collection because `tests.scenarios.observation` did not exist.
  - Green: frozen `CheckpointFacts`, `LedgerFacts`, `SandboxFacts`, and `ScenarioObservation` now project route/terminal/identity/attempt control, accepted/conflict/idempotency ledger authority, and contained artifact-hash/citation facts. The closed `InvariantName` registry computes route, terminal, identity, attempt, accepted authority, conflict, replay, containment, artifact-hash, and citation-binding results from those facts rather than trusting execution success.
  - Fail-closed coverage: contracts reject unknown/duplicate invariants, mutable or duplicate ledger refs, traversal/absolute artifact facts, credential/host-path/free-form diagnostics, and citations not bound to accepted ledger refs. The diagnostic surface is bounded typed codes only.
  - Validation: focused observation contracts passed `9`; the 2.1-2.3 combined contracts passed `52`. Asset checker passed with `1380` deterministic tests and unchanged focused workflow/live/release counts. Ruff, strict OpenSpec, `git diff --check`, and protected-tree checks passed. Legacy live/release runners remain unchanged until the shared assertion work in task 2.4.
- [x] 2.4 Implement one shared assertion interface for family/case route, terminal, artifacts, support/citations, every applicable invariant, and permitted degradation without adding a universal execution dispatcher. @impl EVH-001, EVH-007
  - Red: `tests/contract/test_scenario_assertions_contract.py` failed collection because `tests.scenarios.assertions` did not exist.
  - Green: pure `assert_scenario(family, case, observation)` checks family binding, route, terminal, artifact paths, accepted support refs, citation claim ids, degradation against both family and case policy, and every declared closed invariant. It returns a frozen per-invariant result and aggregates all failures with stable family/case/lane identity.
  - Honesty: invariant outcomes are computed from task 2.3 checkpoint/ledger/sandbox facts; a conflict fact, missing accepted authority, unbound citation, or failed containment cannot become true because execution returned normally. The interface imports no executor and dispatches no production interface.
  - Validation: focused assertion/observation contracts passed `17`; combined 2.1-2.4 plus legacy scenario/live compatibility passed `77`. Asset checker passed with `1388` deterministic tests and unchanged focused workflow/live/release counts. Ruff, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 2.5 Build reusable local runtime, scripted model/tool, checkpoint, ledger, and contained-artifact fixtures that focused pytest cases can compose while continuing to call existing production interfaces explicitly; isolate effective home/config paths and filesystem state, make every owner restore the sandbox provider, cached AppConfig, or default GraphHost state it actually mutates through public reset/restore APIs on success and failure, add an order-independent cross-case cleanup regression for the current `_LiveAdapter` provider installation, and do not add speculative global singleton resets. @impl EVH-001, EVH-008
  - Red: `tests/contract/test_scenario_fixture_contract.py` failed collection because the composable fixture bundle did not exist. A later ownership red test proved the first broad cleanup attempt incorrectly reset an unowned provider when live preflight failed before adapter creation.
  - Green: `ScenarioFixtureBundle` composes existing isolated runtime envelope/identity, `ScriptedChatModel`, `ScriptedTool`, and contained checkpoint/ledger/artifact roots without wrapping production dispatch. `_LiveAdapter` owns an explicit context-manager reset, and the actual live executor resets through public `reset_sandbox_provider()` only after adapter installation, covering success, early return, exception, and cancellation.
  - Cleanup proof: preflight failure before provider installation performs no reset; failure after installation resets once; two adapters run in reverse name order produce exact `set/case/reset` pairs. This removes the existing cross-case process-global provider leak.
  - Scope honesty: live AppConfig is passed explicitly and the GraphHost is locally constructed, so neither cache/default is mutated and no speculative AppConfig or default-host reset was added. Focused fixture/live tests passed `19`; combined 2.1-2.5 and six workflow cases passed `87`. Asset checker passed with `1393` deterministic tests. Ruff, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 2.6 Add synthetic scenario-governance fixtures requiring stable case ids as pytest parameter ids and rejecting unbound cases, cross-asset/seam authenticity substitution, insufficient same-scope authenticity, missing authority observations, or deterministic credentials/network access; defer enforcement that every real family has a collected deterministic case/claim to task 3.11. @impl EVH-001, EVH-007, EVH-009
  - Red: `tests/contract/test_scenario_governance_contract.py` failed collection because the synthetic join validator did not exist.
  - Green: `validate_scenario_evidence` joins only supplied synthetic families/cases/claims/observations. It requires exactly one claim per case, exact collected selector with `[case-id]`, identical asset class/seam/authenticity, compatible focused lane, and an authority observation. It rejects unbound/multiply-bound cases, missing observations, and cross-class/seam/authenticity substitution.
  - Hermetic policy: deterministic cases structurally require `ScenarioInputs`; attempts to substitute credential/network `LiveRequirements` or embed credential-like input fail at construction. No real `SCENARIOS` import or completeness check exists in this validator, so ten-family closure remains deferred to task 3.11.
  - Validation: focused governance passed `8`; complete Group 2 contracts plus legacy scenario/live compatibility passed `90`. Asset checker passed with `1401` deterministic tests and unchanged focused workflow/live/release counts. Ruff, strict OpenSpec, `git diff --check`, and protected-tree checks passed.

## 3. First-Wave Replay Cases

> Slice contract: each conversion task records its stable case id, exact central claim/node id, focused selector, and observed result before it is checked; task 3.11 enables completeness only after those slices are individually bound.

- [x] 3.1 Convert `quick-factual` into a focused real worker/ledger workflow case that proves accepted evidence and structurally bound support at the lowest applicable seam. @impl EVH-001, EVH-008
  - Stable binding: family/case `quick-factual`; central claim `workflow-wave0-worker-bridge`; exact selector `tests/integration/test_worker_bridge_conformance.py::test_scripted_worker_traverses_real_bridge_tool_policy_artifacts_and_ledger[quick-factual]`; focused selection `workflow`; seam `runtime-integration`; authenticity `SCRIPTED_REAL_WORKFLOW`.
  - Migration: the existing Wave0 real bridge/tool-policy/artifact/ledger parameter case was renamed from `[wave0]` to the stable case id rather than duplicating its setup. Its typed script declares three model turns and one `web_search` call under exact bounds.
  - Observed result: one production Wave0 worker call traversed the real `RuntimeNodeAgentBridge`, bounded tool policy and repair turn, published one validated `SubmissionRecord`, and returned its record hash as accepted authority. The shared assertion bound `quick-factual-answer` to that accepted hash and verified attempts bounded, accepted authority, path containment, result artifact hash, and citation binding.
  - Validation: target replay plus real registry/ownership/governance contracts passed `17`; workflow collection remained exactly `6`, now containing `[quick-factual]`. Asset checker passed with `45` claims and `1402` deterministic tests. Ruff, strict OpenSpec, `git diff --check`, and protected-tree checks passed. Real-catalog completeness remains incremental until task 3.11.
- [x] 3.2 Convert `claim-verification` into a labeled replay case with supported, contradicted, and uncertain evidence states and authoritative finding/ref observations. @impl EVH-001, EVH-002
  - Stable binding: family/case `claim-verification`; claim `workflow-wave1-worker-bridge`; selector `tests/integration/test_worker_bridge_conformance.py::test_scripted_worker_traverses_real_bridge_tool_policy_artifacts_and_ledger[claim-verification]`; focused selection `workflow`; seam `runtime-integration`; authenticity `SCRIPTED_REAL_WORKFLOW`.
  - Migration: the existing Wave1 real bridge/tool/artifact/validator/ledger parameter case was renamed from `[wave1]` to the stable case id. Its production `Wave1WorkerOutput` now includes one supported claim (`support_refs`), one contradicted claim (`counter_refs`), and one uncertain claim with no refs plus a real `TARGETED_SEARCH` open question; no new production enum or schema was invented.
  - Observed result: the persisted validated result preserved all three claim ids, the runtime-bound source id, and the targeted-search question. Test-owned `LabeledEvidenceFact` projected supported/contradicted refs and uncertain absence from that artifact; `LABELED_EVIDENCE_COMPLETE` passed only when all three typed states were present. One accepted `SubmissionRecord` and its result hash remained authoritative.
  - Validation: both migrated replays plus registry/ownership/observation contracts passed `20`; workflow remained exactly `6`, and old `[wave1]` references were absent. Asset checker passed with `1403` deterministic tests. Ruff/format, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 3.3 Convert `insufficient-evidence` into a real validation/ledger/artifact workflow case that reaches typed honest degradation without accepted unsupported claims or fabricated citations. @impl EVH-001, EVH-002, EVH-008
  - Stable binding: family/case `insufficient-evidence`; claim `workflow-insufficient-evidence-wave0`; selector `tests/integration/test_adversarial_worker_path.py::test_low_quality_or_unavailable_sources_are_explicitly_degraded[insufficient-evidence]`; focused selection `workflow`; seam `runtime-integration`; authenticity `SCRIPTED_REAL_WORKFLOW`.
  - Migration: the existing real Wave0 `503 Service Unavailable` adversarial parameter was renamed from `[unavailable]`, marked workflow only for that case, and bound to typed two-model/one-tool inputs. SEO and paywall siblings remain correctness cases.
  - Observed result: production worker parsing/materialization and submit validation published one degraded-source `SubmissionRecord` with explicit `Source unavailable` limitation, empty `baseline_facts`, and a hashed result artifact. Shared assertions recorded typed `insufficient-evidence`, accepted the limitation record as authority, and required no labeled claim facts or citation bindings; `HONEST_DEGRADATION` fails if unsupported facts/citations are introduced.
  - Validation: full adversarial worker plus registry/ownership/observation suite passed `26`; workflow increased honestly to `7`, integration correctness decreased to `90`, and the exact deterministic partition remained green. Asset checker passed with `46` claims and `1403` deterministic tests. Ruff/format, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 3.4 Convert `prompt-injection` into a real tool/worker/validator/ledger/gate workflow case proving route, gate, ledger, checkpoint, and path authority cannot be forged. @impl EVH-001, EVH-004, EVH-008
  - Stable binding: family/case `prompt-injection`; claim `workflow-prompt-injection-wave0`; selector `tests/integration/test_adversarial_worker_path.py::test_authority_forging_source_text_cannot_control_ledger_or_gate[prompt-injection]`; focused selection `workflow`; seam `runtime-integration`; authenticity `SCRIPTED_REAL_WORKFLOW`.
  - Migration: the existing prompt-injection parameter now carries workflow and a stable case id; forged-submission and route-text sibling parameters remain correctness. The hostile tool result simultaneously claims `route=exhausted`, `gate=pass`, forged accepted refs/checkpoint completion, and `../../../outside` path authority.
  - Observed result: the real bridge/worker/validator/ledger/gate path accepted only the runtime-created record hash, produced gate route `pass` from authoritative state, kept paths contained, and omitted the hostile source text from the validated result artifact. `AUTHORITY_NOT_FORGED` plus route/accepted/artifact/containment invariants are computed from observations, not from the input text.
  - Validation: full adversarial/registry/ownership suite passed `16`; workflow increased to `8`, integration correctness decreased to `89`, and the deterministic partition remained exact. Asset checker passed with `47` claims and `1403` deterministic tests. Ruff/format, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 3.5 Convert `malformed-output` into a real node/worker workflow case that consumes bounded repair responses and fails closed without partial ledger or artifact authority. @impl EVH-001, EVH-008
  - Stable binding: family/case `malformed-output`; claim `workflow-malformed-output-wave2`; selector `tests/integration/test_zero_tool_node_conformance.py::test_wave2_malformed_output_consumes_repair_and_leaves_no_partial_authority[malformed-output]`; focused selection `workflow`; seam `node-interface`; authenticity `SCRIPTED_REAL_WORKFLOW`.
  - Migration: a stable parameter case was added beside the existing Wave2 zero-tool bridge conformance, reusing its real `RuntimeNodeAgentBridge` setup. Two scripted malformed model turns are declared and exactly bounded; no tool is available.
  - Observed result: production Wave2 invoked the real bridge twice (initial plus repair), then raised the typed `synthesis_output_json_invalid` failure. No synthesis artifact, accepted ledger ref, or citation binding existed. `NO_PARTIAL_AUTHORITY` passes only when all three authority surfaces are empty.
  - Validation: full zero-tool/registry/ownership/observation suite passed `25`; workflow increased to `9` and the deterministic partition remained exact. Asset checker passed with `48` claims and `1405` deterministic tests. Ruff/format, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 3.6 Convert `tool-unavailable-timeout` into a real bridge/policy workflow case with typed timeout/unavailable results, cancellation semantics, call bounds, and no partial publication. @impl EVH-001, EVH-003, EVH-008
  - Stable binding: family/case `tool-unavailable-timeout`; claim `workflow-tool-unavailable-timeout-bridge`; selector `tests/eval/test_fault_injection.py::test_tool_unavailable_timeout_bridge_fails_closed[tool-unavailable-timeout]`; focused selection `workflow`; seam `runtime-integration`; authenticity `SCRIPTED_REAL_WORKFLOW`.
  - Honest typed boundaries: missing eligible policy tools fail before agent construction with `NodeAgentConfigurationError.code=tools_unavailable`; a real blocking model under the bridge deadline returns `BUDGET_EXHAUSTED/error_code=wall_time`; outer cancellation propagates `CancelledError`. A read-only probe confirmed a bound tool handler exception propagates raw `RuntimeError`, so this case does not falsely label that execution-time behavior as a typed unavailable result.
  - Bounds/authority: unavailable builds `0` agents, timeout and cancellation each build exactly `1`, and no tool dispatch or publication store is involved. `FAULT_OUTCOMES_COMPLETE` requires all three diagnostic codes and `NO_PARTIAL_AUTHORITY` verifies no accepted refs, artifacts, or citations; the contained authority root remains absent.
  - Validation: full fault/registry/ownership/observation suite passed `23`; workflow increased to `10` with an exact deterministic partition. Asset checker passed with `49` claims and `1406` deterministic tests. Ruff/format, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 3.7 Convert `budget-exhaustion` into a real bridge/policy workflow case with typed exhaustion, exact model/tool call bounds, and no accepted partial publication. @impl EVH-001, EVH-003, EVH-008
  - Stable binding: family/case `budget-exhaustion`; claim `workflow-budget-exhaustion-bridge`; selector `tests/unit/test_node_agent_bridge.py::test_real_bridge_enforces_exact_model_tool_budget_without_publication[budget-exhaustion]`; focused selection `workflow`; seam `runtime-integration`; authenticity `SCRIPTED_REAL_WORKFLOW`.
  - Red correction: the first focused run failed before execution because `max_parallel_tool_calls=2` contradicted the reduced per-response limit `1`; the valid policy fixture was corrected to all exact tool bounds `1` rather than weakening policy validation.
  - Observed result: one scripted model turn issued one allowed `web_search`; the real bridge/tool-policy executed exactly one tool call; the next model call was rejected before its handler by `BudgetMiddleware(max_model_calls=1)`. Result was typed `BUDGET_EXHAUSTED/error_code=policy`, with `model.calls=1`, `tool.calls=1`, one agent build, and no ledger/artifact/citation authority or host publication.
  - Validation: full bridge/registry/ownership/observation suite passed `39`; workflow increased to `11` with exact deterministic partition. Asset checker passed with `50` claims and `1407` deterministic tests. Ruff/format, strict OpenSpec, `git diff --check`, and protected-tree checks passed.
- [x] 3.8 Convert `partial-worker-success` into a real fan-out/submit/gate workflow case distinguishing accepted, failed, repair, fatigue, and exhausted outcomes from authoritative work state. @impl EVH-001, EVH-003, EVH-008
  - Stable binding: family/case `partial-worker-success`; claim `workflow-partial-worker-submit-gate`; selector `tests/integration/test_wave0_work_units.py::test_partial_worker_success_projects_distinct_gate_outcomes_from_authoritative_state[partial-worker-success]`; focused selection `workflow`; seam `runtime-integration`; authenticity `REAL_NODE_FAKE_CAPABILITIES` (real controller/store/submit/gate sequence, deliberately no agent-loop claim).
  - Red/green: observation contracts first failed because work/gate authority had no closed projection; `WorkStateFacts` and `WorkGateOutcome` now retain accepted work ids, failed attempt ids, route, and closed diagnostic codes. The registry then failed with `partial-worker-success: no evidence claim` until the exact collected claim was registered.
  - Observed result: production `run_fixture_work_unit_component` fanned out three Wave0 works through the real artifact writer and `WorkUnitStore`; one resolver-bound worker failed while two candidates became validated ledger records. The validated gate view and parent projection preserved two accepted work ids, one failed attempt, and one `work_failed` summary. Production `evaluate_gate` over that same authority produced repair, a separately fingerprinted fatigue exhaustion, and a separately budget-driven repair exhaustion; the invariant rejects a single conflated fatigue/budget outcome.
  - Validation: affected scenario/work-unit suites passed `45`; the exact workflow selector passed and collected once. Asset governance passed with `51` central claims and `1410` deterministic tests (`fast=1309`, `integration=89`, `workflow=12`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty `agent/src`/`backend`/`frontend` protected-tree diff passed.
- [x] 3.9 Convert `checkpoint-control` into a real lifecycle workflow case covering duplicate resume, cancel idempotency, terminal monotonicity, and restart-safe checkpoint identity without claiming an agent loop. @impl EVH-001, EVH-003
  - Stable binding: family/case `checkpoint-control`; claim `workflow-checkpoint-control-restart`; selector `tests/integration/test_provider_durability.py::test_checkpoint_control_survives_process_restarts[checkpoint-control]`; focused selection `workflow`; seam `lifecycle-mixed-graph`; authenticity `REAL_NODE_FAKE_CAPABILITIES` (real lifecycle/checkpointer transitions with fake graph nodes, no agent-loop claim).
  - Red/green: observation contracts first failed because lifecycle authority had no projection; bounded `LifecycleFacts` now retains opaque research/request ids, typed cancel/status codes, and durability only. The subprocess workflow then failed because its test driver supported only start/resume; extending the test-owned driver to cancel/status exposed the existing production handlers without changing production. Registry governance finally failed with `checkpoint-control: no evidence claim` until the exact claim was bound.
  - Observed result: seven fresh Python processes opened one file-SQLite checkpoint in order: start, resume, duplicate resume, cancel, duplicate cancel, post-terminal resume, status. Every process recovered the same research identity with `restart_durable`; duplicate resume reprojected the same next request id, both cancels returned cancelled, the terminal resume returned `invalid_transition`, and final status remained cancelled.
  - Validation: provider durability/public lifecycle/scenario suites passed `54` with the existing deferred Postgres case skipped; the focused registry/observation/workflow set passed `6`. Asset governance passed with `52` central claims and `1415` deterministic tests (`fast=1313`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty `agent/src`/`backend`/`frontend` protected-tree diff passed.
- [x] 3.10 Convert `sandbox-filesystem-failure` into code-correctness cases over real store atomic-publication/replay seams with containment and prior-or-single-new authority guarantees, without falsely marking them workflow. @impl EVH-001, EVH-003
  - Stable binding: family/case `sandbox-filesystem-failure`; claim `store-prior-authority-fault-matrix`; selector `tests/unit/test_work_unit_store.py::test_prior_authority_survives_atomic_publication_fault_matrix[sandbox-filesystem-failure]`; focused selection `fast`; asset class `CODE_CORRECTNESS`; seam `runtime-integration`; authenticity omitted. It is intentionally absent from workflow collection.
  - Red/green: observation contracts first failed because filesystem fault authority had no closed projection; `FilesystemFaultOutcome` now records the supported fault point, post-fault/post-replay record counts, replay disposition, containment, and staging residue. The real store case then surfaced a missing test workspace precondition before running green. Registry governance finally failed with `sandbox-filesystem-failure: no evidence claim`; the registry collector was honestly widened from workflow-only to the deterministic aggregate so this code-correctness case could retain its fast claim.
  - Observed result: for each of `before_staging_write`, `after_staging_fsync`, `after_ledger_replace`, and `after_directory_fsync`, the real store first committed prior authority, faulted a second work, reopened, and replayed. Fault state contained either the prior record only or prior plus one new record; replay ended with exactly two distinct work records, correct appended/replayed disposition, canonical contained result refs, and no staging residue. The invariant requires all four points and rejects duplicate or leftover staging authority.
  - Validation: store/scenario/claim/lane suites passed `99`; focused migration tests passed `4`. Fast collection included the exact case and workflow collection of its file was empty. Asset governance passed with `53` central claims and `1418` deterministic tests (`fast=1316`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty `agent/src`/`backend`/`frontend` protected-tree diff passed.
- [x] 3.11 Remove the legacy `Scenario`/`ScenarioRunner` and catalog only after all ten migrations, then enable real-catalog governance proving every family id has a collected deterministic case/claim at its declared lowest seam and only qualifying cases enter the workflow selection. @impl EVH-001, EVH-007, EVH-008, EVH-009
  - Removal/migration: deleted `tests/scenarios/model.py`, `tests/scenarios/catalog.py`, and their old runner-only unit test after all ten families/cases were present. Eval lookups now use `FIRST_WAVE_FAMILIES/CASES`; live execution owns the narrower `LiveScenario`/`LiveOutcome` data it actually consumes; release uses the central authenticity enum. A repository scan found no legacy `Scenario`, `ScenarioRunner`, `SCENARIOS`, model, or catalog references.
  - Real closure: `validate_real_scenario_catalog` fixes the required ten-family id set, requires at least one deterministic case per family, exactly one matching central claim per case, collection in the deterministic aggregate, exact asset/seam/authenticity agreement, workflow collection for every workflow-class case, and exclusion of code-correctness cases from workflow. `scripts/check_test_assets.py` now runs this validator in the normal CI asset path.
  - Validation: migration/live/release/scenario compatibility passed `49`; the full deterministic related matrix passed `130`; lane/claim/node governance passed `49`. Asset governance passed with `53` claims and `1417` deterministic tests (`fast=1315`, `integration=89`, `workflow=13`, `live=5`, `release=1`). The net deterministic count change is the removal of three legacy-runner tests plus two real-closure contracts, not a coverage target or regression.
  - Release honesty: one mistakenly broad explicit pytest command named the credentialed release test and correctly failed strict preflight with `release_confirmation_missing`; that result was discarded. The corrected deterministic command excluded `requires_llm/release_e2e/postgres` and passed `130`; release was collection-only and selected exactly one case. Ruff/format, strict OpenSpec, `git diff --check`, zero legacy scan, and the empty protected-tree diff passed.
- [x] 3.12 Re-run the syntax-discovered six-node workflow audit after the first-wave migration, add or upgrade the smallest explicit production-interface case for any newly exposed gap, and make the combined node/scenario/workflow inventory pass without weakening a required seam or authenticity. @impl EVH-008, EVH-009
  - Combined audit: syntax-aware discovery still finds exactly `hitl1`, `topic_planning`, `wave0`, `wave1`, `wave2_synthesis`, and `targeted_evidence`; each has its existing real-bridge claim at node-interface or runtime-integration. Real-catalog closure binds all ten families, with nine qualifying workflow cases and the one store-only code-correctness case excluded from workflow.
  - Workflow structure: the 13 exact selectors comprise six owner proofs plus first-wave risk cases without inflating authenticity: HITL1/topic-planning/Wave2 zero-tool bridge, Wave0/Wave1/targeted worker bridge, malformed output, insufficient evidence, prompt injection, unavailable/timeout, budget exhaustion, partial worker submit/gate, and checkpoint lifecycle restart. Shared owner/case selectors remain one test when they prove both concerns.
  - No gap addition: the combined inventory exposed no missing owner, family, responsible seam, or authenticity level, so no duplicate production-interface case was added merely to increase volume. `sandbox-filesystem-failure` remained fast code correctness as required.
  - Validation: combined ownership/registry/governance/lane contracts passed `24`; `make test-workflow` passed all `13` with `1411` deselected. Asset governance passed with `53` claims and `1417` deterministic tests (`fast=1315`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.

## 4. Provider-Shape Provenance

- [x] 4.1 Define the minimized provider-shape case schema/loader plus a closed `DiscoveryDisposition` contract for exactly one of `uniform_shape` with intended case id, `existing_regression` with claim id, or `provider_only` with bounded live case/rationale; reject mixed/unknown disposition fields and validate focused node, input schema version, bounded synthetic payload, expected normalized/fail-closed result, and sensitivity metadata. @impl EVH-010
  - Contract: frozen `ProviderShapeCase` requires stable case/discovery ids, one of `wave0`/`wave1`/`wave2_synthesis`, schema version `1..16`, a structured top-level object payload, exactly one normalized-object or fail-closed error expectation, and reviewed synthetic sensitivity metadata. Payloads are recursively frozen after `json.loads`; no ad hoc string parsing or provider trace format was added.
  - Bounds: corpus `<=256 KiB`, at most `128` cases, each payload `<=16 KiB`, recursion depth `<=8`, synthetic domain grammar, duplicate case/discovery protection, and exact loader field sets. Top-level scalar/string pseudo-payloads, malformed/unknown fields, unreviewed sensitivity, oversize payloads, and mixed expected outcomes fail closed.
  - Disposition: `DiscoveryDisposition` is exactly one of `uniform_shape(intended_case_id)`, `existing_regression(claim_id)`, or `provider_only(live_case_id, rationale)` with separate reference grammars and bounded nontrivial rationale. Missing, mixed, unknown, or malformed branch fields fail at construction.
  - Validation: focused provider-shape contract passed `11`; adjacent evidence/input/scenario contracts passed `74`. Asset governance passed with `53` claims and `1428` deterministic tests (`fast=1326`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed. No historical discovery was classified early; tasks 4.2-4.4 own those rows.
- [x] 4.2 Classify `LIVE-20260717-01..06` as uniform shape, existing focused regression, or provider-only behavior; bind existing-regression claims immediately and record a stable intended shape-case id or bounded live rationale for the other dispositions. @impl EVH-006, EVH-010
  - Existing regressions: LIVE-01 workspace cleanup -> `live-discovery-workspace-cleanup`; LIVE-02 model construction -> `live-discovery-model-construction`; LIVE-03 canary parser precondition -> `live-discovery-canary-precondition`; LIVE-04 Wave0 prompt/contract -> `live-discovery-wave0-prompt-contract`; LIVE-06 required-tool direct-answer rejection -> `live-discovery-tool-required`. Each central correctness claim owns its exact selector and matching discovery id; none was duplicated into a uniform shape case because their setup/responsible seams differ.
  - Provider-only: LIVE-05 remains `provider_only` with bounded live case `live-one-topic-wave0` and central claim `live-wave0-provider-tool-selection`. Rationale records that model web-tool selection is a provider decision distribution: scripted execution proves the policy path but cannot reproduce the probability of selecting the tool.
  - Governance: `LIVE_DISCOVERY_DISPOSITIONS` is exactly LIVE-01..06. The join rejects missing/extra/duplicate discoveries, unknown claims, claims lacking reverse discovery provenance, unknown live cases, or provider-only discoveries without a central live claim. A real collection caught and corrected the initial handwritten function-name mismatch in the live selector (`test_live_prefix_canary`, not `test_live_canary`).
  - Validation: disposition/evidence contracts passed `49`; all five deterministic selectors passed, and live collection exposed the three bounded canaries with the Wave0 exact id. Asset governance passed with `59` claims and `1430` deterministic tests (`fast=1328`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 4.3 Classify `RELEASE-20260717-01..14` as uniform shape or existing focused regression; bind existing-regression claims immediately and record a stable intended shape-case id for cases migrated in tasks 4.5-4.7. @impl EVH-006, EVH-010
  - Existing focused regressions: RELEASE-01 final publication -> `release-discovery-final-publication`; 02 profile budget -> `release-discovery-profile-budget`; 06 Wave1 worker/ledger -> existing `wave1-worker-ledger-success` with added provenance; 08 Wave2 repair -> `release-discovery-wave2-structured-repair`; 10 topic bound -> `release-discovery-topic-bound`; 13 synthesis semantic floor -> `release-discovery-synthesis-semantic-floor`; 14 readiness provenance -> `release-discovery-readiness-provenance`.
  - Intended uniform shapes: RELEASE-03 `shape-wave0-url-canonicalization`; 04 `shape-wave0-fetch-status-alias`; 05 `shape-wave0-limitations-list`; 07 `shape-wave1-provider-identifiers`; 09 `shape-wave2-provider-field-aliases`; 11 `shape-wave2-gap-priority-alias`; 12 `shape-wave0-partial-source-degradation`. These remain intended ids until tasks 4.5-4.7 add their collected cases; 4.8 owns closure.
  - Classification rationale: publication/store, prompt-budget, worker-ledger, repair-loop, node-bound, semantic-floor, and readiness cases retain their full focused setup. Single provider URL/status/list/id/field aliases are uniform payload shapes and are not duplicated as bespoke tests before the shared corpus exists.
  - Validation: exact RELEASE-01..14 disposition contract plus evidence contracts passed `50`; all seven existing selectors passed. Asset governance passed with `65` claims and `1431` deterministic tests (`fast=1329`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 4.4 Classify `RELEASE-20260717-15..25` as uniform shape or existing focused regression; bind existing-regression claims immediately and record a stable intended shape-case id for cases migrated in tasks 4.5-4.7. @impl EVH-006, EVH-010
  - Existing focused regressions: RELEASE-15 visible retry trace -> `release-discovery-visible-retry-trace`; 16 dotenv handoff -> `release-discovery-dotenv-handoff`; 17 redacted synthesis diagnostics -> `release-discovery-redacted-schema-diagnostics`. Each keeps its release-control/reporting setup and reverse discovery provenance.
  - Intended uniform shapes: RELEASE-18 `shape-wave1-source-id-ref-rewrites`; 19 `shape-wave2-description-string-gaps`; 20 `shape-wave2-sparse-findings`; 21 `shape-wave1-source-order`; 22 `shape-wave2-relation-endpoints`; 23 `shape-wave2-singular-affected-topic`; 24 `shape-wave2-alternate-relation`; 25 `shape-wave2-missing-gap-identity`. These are parser/materializer payload shapes and remain intended until 4.6/4.7.
  - Classification rationale: retry visibility, dotenv transfer, and redacted report diagnostics are control-plane behavior, not uniform provider payloads. Wave1 id/ref/order and Wave2 sparse/relation/gap variants share parser/materializer scaffolding and are reserved for the shared shape corpus rather than retaining eight provenance-specific bespoke setups.
  - Validation: exact RELEASE-15..25 disposition/evidence contracts passed `51`; the three existing selectors passed. Asset governance passed with `68` claims and `1432` deterministic tests (`fast=1330`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 4.5 Add or migrate Wave0 URL, fetch-status, limitations, partial/duplicate-source, and direct-answer shape cases through production normalization/validation seams without duplicating non-uniform focused tests. @impl EVH-006, EVH-010
  - Shared corpus: committed minimized synthetic `tests/fixtures/provider_shapes/wave0.json`; one parameterized runner calls production `parse_wave0_worker_output` for `shape-wave0-url-canonicalization`, `shape-wave0-fetch-status-alias`, `shape-wave0-limitations-list`, `shape-wave0-partial-source-degradation`, and `shape-wave0-duplicate-source-fail-closed`.
  - Production semantics: URL/status/list/partial cases assert normalized stable projections. Partial invalid records retain the valid source and append a typed dropped-record limitation. Duplicate canonical URLs are not silently deduplicated: production `validate_independent_sources` fails closed with `sources_not_independent`, so the corpus records that separate expected outcome honestly.
  - Provenance/claims: all five stable parameter ids have exact fast claims. RELEASE-12 owns the intended partial-degradation case and also links the complementary duplicate fail-closed case. LIVE-06 direct-answer behavior remains the existing real bridge/policy regression `live-discovery-tool-required`; it was not duplicated as a payload shape because tool-selection enforcement needs bridge execution.
  - Validation: shared corpus plus original Wave0/parser contracts passed `40`; all five shape cases passed. Asset governance passed with `73` claims and `1437` deterministic tests (`fast=1335`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 4.6 Add or migrate Wave1 provider/source ids, ref rewrites, question states, source ordering, and malformed-submit shape cases through production contract/materializer/submit seams. @impl EVH-006, EVH-010
  - Shared corpus: `wave1.json` owns four stable cases. `shape-wave1-provider-identifiers` normalizes claim/question ids and question state; `shape-wave1-source-id-ref-rewrites` normalizes provider source ids, URL, and claim refs through `Wave1WorkerOutput`. `shape-wave1-source-order` and `shape-wave1-malformed-submit` drive the existing real materializer/submit/ledger integration setup from the same corpus.
  - Production semantics: source-order persists canonical `source:a, source:z` order and rewrites claim support refs before candidate validation. Malformed submit uses a structured contradictory `source_ids` payload, consumes the real repair path, becomes typed failed work, and leaves the ledger empty; it is not a string pseudo-script or parser-only claim.
  - Loader correction: the first Wave1 run exposed that the frozen JSON representation could not distinguish empty arrays from empty objects. The loader now uses tagged immutable `FrozenJsonArray`/`FrozenJsonObject`, making `{}` and `[]` exactly reversible while preserving bounds and ordinary tuple fixture compatibility. Wave0 and Wave1 corpus tests both pass after the correction.
  - Validation: Wave0/Wave1/provider/contract/integration suites passed `36` (focused corpus recheck `22`). Asset governance passed with `76` claims and `1439` deterministic tests (`fast=1337`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 4.7 Add or migrate Wave2 finding, gap, relation, endpoint, priority, affected-topic, evidence-alias, sparse-output, and semantic-floor shape cases through production parser/node/validator seams. @impl EVH-006, EVH-010
  - Shared corpus: `wave2.json` owns eight minimized synthetic parser cases for provider field aliases, gap severity/priority, finding descriptions plus string gaps, sparse findings, relation endpoint aliases, singular affected topic, alternate relation keys, and derived gap identity. One parameterized runner calls production `parse_synthesis_output` and asserts the complete normalized finding/relation/gap projection with the stable intended case ids from RELEASE-09/11/19/20/22-25.
  - Responsible seams: seven bespoke node/store tests that only inspected parser normalization were removed after equivalent corpus cases passed. `shape-wave2-evidence-alias-binding` remains parameterized at the real node/store seam because only accepted evidence authority can prove that provider `source_id` aliases become the accepted submission ref. `release-discovery-synthesis-semantic-floor` remains the existing full node regression and still proves that accepted evidence cannot yield an empty repaired synthesis.
  - Provenance/claims: all eight intended uniform cases plus the complementary evidence-binding case have exact collected fast claims. The regression-descent selectors now point to the shared stable parameter ids; no historical discovery points at a removed bespoke selector.
  - Validation: combined Wave0/Wave1/Wave2 provider, real Wave2 node, and provider-contract suites passed `37`; exact Wave2 collection exposed all nine stable case ids. Asset governance passed with `85` claims and `1440` deterministic tests (`fast=1338`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 4.8 Enable real-catalog provenance closure: every replayable discovery resolves to its collected existing-regression or shape-case claim and every provider-only discovery has bounded live rationale; reject unknown discoveries, unresolved intended case ids, missing claims, duplicate/unowned cases, oversize payloads, credentials, raw host paths, and non-synthetic external URLs, require recorded human review that free text is minimized/synthetic without claiming arbitrary raw prose is mechanically detectable, and directly test each provenance/sensitivity rule with a minimal invalid fixture plus an archive-scan smoke case that contains one known violation. @impl EVH-006, EVH-010
  - Real closure: the asset checker loads the non-empty sorted `provider_shapes/*.json` archive under aggregate byte/case bounds and joins all 31 LIVE/RELEASE dispositions to the loaded cases, central claim index, actual five-lane pytest collection, and bounded live case ids. Existing regressions require reverse discovery provenance plus a collected exact selector; provider-only rows require the exact collected live parameter id; uniform shapes require a resolved intended case, one uniquely selected claim containing the stable case id, and equal case/claim discovery provenance.
  - Sensitivity: loader validation walks structured payload and normalized expectation values, rejects credential-shaped keys/values, raw macOS/Linux/Windows user paths, and HTTP(S) hosts other than the case's reviewed synthetic domain. Every fixture still requires `minimized_reviewed=true` and explicitly records whether free text remains; no claim is made that arbitrary copied prose is mechanically recognizable.
  - Negative evidence: direct minimal tests cover unknown discoveries, unresolved intended ids, duplicate cases, missing/duplicate/uncollected claims, reverse-provenance mismatch, uncollected existing regressions, mismatched provider-only live ids, oversize/unreviewed inputs, credentials, host paths, and external URLs. The archive smoke rejects both an empty scan and a non-empty archive containing a known external-URL violation.
  - Validation: provider/Wave2 focused suites passed `46`; asset governance passed with `85` claims and `1449` deterministic tests (`fast=1347`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.

## 5. Validated Metric Semantics

- [x] 5.1 Define and test the pure `ValidatedEvaluationOutcome` plus typed metric results where an unselected case metric is `not_applicable` with null value, a selected metric with missing/unlabeled authority or a ratio's empty authoritative denominator is `insufficient_authority` with null value, a validated empty source collection permits measured zero only for applicable count metrics, and sufficient input is numeric `measured`, using already-read ledger/topic/synthesis/citation-map/labeled-expectation inputs. @impl EVH-002
  - Pure contracts: frozen `ValidatedEvaluationOutcome` accepts only already-read accepted-ledger, topic/question, synthesis finding/gap, final citation-map, and labeled-expectation projections. Each authority preserves `None` (missing) versus `()` (validated empty) versus immutable present records, rejects mutable/unfreezable values, and performs no I/O, model, network, clock, or filesystem access.
  - Typed results: closed metric ids map to `count` or `ratio`; `MetricResult` is exactly `not_applicable`, `insufficient_authority`, or numeric `measured`, always with bounded structured evidence basis. Unavailable states require null value. Counts require non-negative integers and may measure validated zero; ratios require `0..1` plus an explicit positive authoritative denominator, so an empty denominator cannot become vacuous zero/one.
  - Resolver: `evaluate_metric_result` derives unselected, missing/unlabeled authority, and empty-denominator states from the selected metric ids and outcome authority presence before accepting a caller-computed pure value. Existing placeholder metric/report callers remain behavior-compatible until tasks 5.2-5.5 migrate each formula/schema vertically.
  - Validation: typed outcome plus legacy metric/live-report compatibility suites passed `38`. Asset governance passed with `85` claims and `1464` deterministic tests (`fast=1362`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 5.2 Replace ref-prefix precision with structural citation-binding rate and labeled semantic citation precision; prove accepted-but-unlabeled bindings return `insufficient_authority` for semantic precision. @impl EVH-002
  - Structural binding: `compute_citation_binding_rate` expands authoritative final citation-map claim/ref pairs and measures membership against accepted ledger `submission_ref` values. Ref-shaped text has no special status; a syntactically plausible but unaccepted ref is unbound, missing authority is insufficient, and an empty authoritative citation denominator is insufficient rather than perfect.
  - Semantic precision: `compute_labeled_citation_precision` joins cited claim/ref pairs to explicit replay judgments and counts only `supports` as semantically supported. Accepted refs without labels, an empty label collection, or partial label coverage return `insufficient_authority`; duplicate/malformed labels and malformed citation refs fail closed.
  - Replacement: the old `compute_citation_precision` prefix proxy and its aggregate `citation_precision` output were removed. Scenario/live declarations now use canonical `citation-binding-rate`/`must-answer-coverage` ids. Typed live-report integration remains owned by task 5.5 rather than fabricating citation authority from the current legacy `values` dictionary.
  - Validation: eval/live/scenario compatibility suites passed `94`. Asset governance passed with `85` claims and `1470` deterministic tests (`fast=1368`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 5.3 Project must-answer coverage through existing question-to-topic and finding/gap-to-topic mappings; count a question covered only when every known bound topic has a finding or typed honest gap, report partial questions and unknown/unmapped topics separately, and avoid direct question-to-finding claims. @impl EVH-002
  - Authority join: typed `compute_must_answer_coverage` reads planner-owned `question_id -> topic_ids` bindings and synthesis-owned finding/gap `affected_topics`; it never links a question directly to a finding or infers coverage from submission counts. A typed honest gap represents its topic as legitimately as a finding for coverage purposes.
  - Semantics: a question is covered only when every bound topic is represented, partial when a non-empty proper subset is represented, and uncovered otherwise. The frozen evaluation reports covered/partial/uncovered question ids, all missing planned topics, synthesis topics unknown to the planner, and planned topics unmapped by any finding/gap separately; evidence basis exposes bounded counts rather than question text.
  - Authority states: unselected returns `not_applicable`; missing finding/gap/binding projections and a validated empty question denominator return `insufficient_authority`. Empty/duplicate topic bindings, duplicate questions, and malformed affected-topic authority fail closed. The former one-submission-per-question proxy remains only inside the legacy aggregate pending task 5.5 and is no longer the public must-answer function.
  - Validation: eval/live/scenario compatibility suites passed `102`. Asset governance passed with `85` claims and `1478` deterministic tests (`fast=1376`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 5.4 Replace submission-ref diversity with separate numeric distinct-canonical-URL and normalized-host metrics derived only from validated accepted `SourceRef` values, prove provider-local `source_id` changes cannot inflate diversity, compute final claim-map citation completeness separately from synthesis finding-support coverage, and separate missing-backing-ref counts from labeled semantic unsupported-claim counts and contradiction recall. @impl EVH-002
  - Source identity: distinct canonical-URL and normalized-host counts walk only `source_refs` inside validated accepted ledger records, structurally parse HTTP(S) URLs, normalize scheme/host/default port/fragment, and ignore provider-local `source_id`. Two ids for one canonical URL remain one URL/host; a validated empty source collection measures numeric zero, while missing source authority or invalid URLs fail closed.
  - Separate denominators: final citation completeness divides cited final claim-map claims by all authoritative final claims. Synthesis finding-support coverage independently divides findings with at least one accepted backing ref by all synthesis findings. Empty ratio populations are insufficient rather than perfect and neither result substitutes for structural citation binding.
  - Separate failure meanings: structural missing-backing-ref count measures absent/unknown refs on synthesis findings; semantic unsupported-major-claim count requires labeled major-claim judgments; contradiction recall joins labeled expected contradiction pairs to observed contradiction pairs. Missing/empty semantic labels are insufficient, not measured zero. Provider/source structure cannot manufacture semantic support.
  - Replacement: submission-ref diversity, legacy citation completeness, legacy contradiction recall, and unlabeled unsupported-claim proxies were removed from the aggregate shim; it now emits no placeholder quality values until task 5.5 supplies typed versioned reporting.
  - Validation: eval/live/scenario compatibility suites passed `106`. Asset governance passed with `85` claims and `1482` deterministic tests (`fast=1380`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 5.5 Extend live reports compatibly with `report_schema_version=1`, `metrics_schema=evidence-v1`, typed metric statuses/evidence bases, and legacy-report classification; start a new non-blocking baseline. @impl EVH-002, EVH-005
  - Versioned reporting: every newly generated success or failure `LiveScenarioReport` carries `report_schema_version=1` and `metrics_schema=evidence-v1`. Scenario-selected metric ids dispatch through the typed evaluators over an explicit `LiveOutcome.values` authority projection; every metric serializes id, status, value kind, null/numeric value, evidence basis, and optional authoritative denominator.
  - Validation/classification: the report contract reconstructs and validates every `MetricResult`, rejecting arbitrary partial metric dictionaries, mismatched identities, invalid status/value combinations, and unknown/partial schema versions. Reports lacking both version fields are explicitly `legacy`; versioned reports must be exact evidence-v1. Current early-prefix canaries honestly report missing late authority as `insufficient_authority` instead of reviving checkpoint proxies.
  - Baseline: the committed 2026-07-17 baseline is now marked legacy and its historical proxy values are declared non-comparable. A new evidence-v1 non-blocking observation window is open with no threshold; it explicitly records that no credentialed evidence-v1 observation exists yet, so future task 6.7 measurements cannot be confused with migrated old numbers.
  - Validation: eval/live/scenario compatibility suites passed `111`. Asset governance passed with `85` claims and `1487` deterministic tests (`fast=1385`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.

## 6. Focused Late-Node Live Cases

> Slice contract: tasks 6.2-6.4 each add one stable live case id, exact collected live claim/selector, deterministic setup/report contract tests, and focused-coverage disclaimers before any credentialed execution in task 6.7.

- [x] 6.1 Add test-owned seed builders that create compact valid checkpoint projections and publish minimum Wave0/Wave1 ledger/content authority through existing store interfaces, with hash/schema/containment tests and no direct ledger-line fabrication. @impl EVH-005, EVH-007
  - Test-owned builder: `build_live_seed_bundle` creates one compact synthetic topic and fixed isolated identity, materializes production Wave0/Wave1 work specs and attempts, writes real source/result contracts through the resolver's contained artifact writer, builds validated candidates, and publishes only through `submit_candidate_if_active` into `WorkUnitStore`.
  - Authority split: checkpoint output is revalidated by `validate_research_state` and contains only schema/identity, short topic/question projections, execution trace, and accepted record hashes. Source/result bodies remain contained sandbox artifacts and submission metadata remains in the validated hash-chained ledger; branch-result slots are not abused to duplicate ledger authority.
  - Proof: tests reparse canonical ledger bytes from `evidence_ledger_path`, recompute every record hash, validate Wave0/Wave1 result schemas, read every contained source/result ref, prove checkpoint serialization excludes source bodies, and prove identical seeding replays idempotently. An AST smoke requires exactly two real submit-boundary calls and forbids ledger encoder/parser, direct file-write calls, or the ledger filename in the builder.
  - Validation: seed/store/submit/ledger/state suites passed `53`. Asset governance passed with `85` claims and `1491` deterministic tests (`fast=1389`, `integration=89`, `workflow=13`, `live=5`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 6.2 Add a one-topic Wave1 live case invoking the real `NODE_SPEC` with seeded Wave0 authority and `_build_wave1_capabilities` through the bounded live bridge factory; separate the reserved gate view, project `WORK_UNIT_GATE_PREVIEW_FIELDS` with `preview_work_unit_update`, validate the view, then call `evaluate_gate_for_node(..., real_wave1_gate_def())`, asserting real model/web-tool submit/gate authority plus focused-claim disclaimers. @impl EVH-005, EVH-009
  - Focused case: stable `live-one-topic-wave1` is a direct `wave1.NODE_SPEC` live case with one unique research identity, validated Wave0 ledger seed, production `_build_wave1_capabilities`, and the existing bounded live bridge factory. Its metadata explicitly excludes public-entry, predecessor-lifecycle, production-recipe, and full-pipeline coverage instead of inheriting the early-prefix claim.
  - Real authority path: the focused core reuses the seed's real `WorkUnitStore`, constructs production runtime/work-unit resolvers, invokes `NODE_SPEC.real_factory`, removes the reserved gate view before state projection, previews exactly `WORK_UNIT_GATE_PREVIEW_FIELDS`, validates the wrapper view, and only then evaluates `real_wave1_gate_def()`. It requires one new accepted Wave1 record beside the seeded Wave0 record and a real `pass` gate route.
  - Live dependency proof: the credentialed shell requires a tracked real model response and at least one Tavily web-tool call, reports only the new Wave1 result/ref and isolated identity, and keeps response diagnostics shape-only/redacted. The central `live-wave1-focused-provider` claim selects the exact collected live parameter at the runtime-integration seam with `LIVE_REAL_DEPENDENCIES` authenticity.
  - Deterministic contract: a scripted external dependency drives the same focused core without credentials and records the exact `preview -> validate -> evaluate` order plus production policy construction. The seed builder now accepts an optional clock so live attempts and submission terminal times remain monotonic while existing fixed-clock replay tests remain deterministic.
  - Validation: focused live/report/seed/fixture/lane/evidence suites passed `71`. Asset governance passed with `86` claims and `1492` deterministic tests (`fast=1390`, `integration=89`, `workflow=13`, `live=6`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 6.3 Add a Wave2 synthesis live case invoking the real `NODE_SPEC` with seeded accepted evidence and the shared zero-tool `_build_hitl1_capabilities` through the bounded live bridge factory; assert node-owned binding/semantic-floor behavior, then separately call `evaluate_gate_for_node(..., real_wave2_gate_def())` on the node-updated state and record redacted response-shape diagnostics. @impl EVH-005, EVH-009
  - Focused case: stable `live-wave2-synthesis` directly invokes `wave2_synthesis.NODE_SPEC` with one unique research identity and real Wave0+Wave1 accepted ledger/content authority from the test-owned seed builder. Metadata declares model-only preflight, at most two model calls for one repair, zero tool calls, and explicit exclusions for public entry, predecessor lifecycle, production recipe, and full pipeline.
  - Production dependency path: the core constructs capabilities through the shared `_build_hitl1_capabilities` and bounded live bridge factory, reuses the real `WorkUnitStore` as `SynthesisBundleStoreProtocol`, resolves real node dependencies, and invokes `NODE_SPEC.real_factory`. Zero-tool authority is asserted at the production policy/tool-resolver boundary rather than incorrectly treating the node request's tool hint as the enforcement boundary.
  - Node-owned authority: the deterministic contract supplies a provider-local Wave1 `source_id` as a synthesis backing alias; the real node resolves it to the accepted Wave1 ledger hash, enforces the non-empty finding/gap semantic floor, and canonically publishes `synthesis/findings.json`. Only after the node update exists does the core evaluate `real_wave2_gate_def()` and require `pass`.
  - Reporting and claim: the credentialed shell requires a tracked real model response, records zero tool calls, and emits bounded redacted response-shape diagnostics plus validated finding/gap/binding authority. Exact claim `live-wave2-focused-provider` selects the collected live parameter at the runtime-integration seam with `LIVE_REAL_DEPENDENCIES` authenticity.
  - Validation: focused live/report/seed/fixture/lane/evidence/Wave2/zero-tool suites passed `85`. Asset governance passed with `87` claims and `1493` deterministic tests (`fast=1391`, `integration=89`, `workflow=13`, `live=7`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 6.4 Add a one-gap targeted-evidence live case invoking the real `NODE_SPEC` with seeded synthesis authority and `_build_wave0_capabilities` through the bounded live bridge factory/work-unit controller; assert subgraph-owned validated submit/ledger publication without claiming a nonexistent targeted phase gate or public-entry/recipe/full-prefix coverage. @impl EVH-005, EVH-009
  - Focused case: stable `live-one-gap-targeted-evidence` directly invokes `targeted_evidence.NODE_SPEC` under one unique identity, model+web strict preflight, and bounded Wave0-derived bridge policy. Its metadata declares `none-submit-only` and excludes public entry, predecessor lifecycle, production recipe, and full pipeline.
  - Seed authority split: the test-owned builder publishes a schema-valid one-gap `SynthesisResult` through `WorkUnitStore.write_synthesis`; the narrow canonical `GapRecord` remains sandbox authority, while a separate invocation projection derived from it adds the existing dispatch-only `search_required=true` field. The compact validated checkpoint correctly rejects and therefore never stores `synthesis_gaps` bodies.
  - Real submit path: the focused core constructs `_build_wave0_capabilities`, runtime/work-unit resolvers, and the shared controller, invokes `NODE_SPEC.real_factory`, and requires exactly one new `targeted_evidence` ledger record with `targeted.source-intake` result authority beside the seeded Wave0/Wave1 records. The deterministic contract turns any gate-adapter call into a hard failure, mechanically proving no nonexistent targeted phase gate is claimed.
  - Live dependency proof: the credentialed shell requires both a tracked real model response and at least one Tavily web-tool call, reports only the new targeted result/ref plus the bounded gap projection and isolated identity, and registers exact runtime-integration claim `live-targeted-focused-provider` with `LIVE_REAL_DEPENDENCIES` authenticity.
  - Validation: focused live/report/seed/fixture/lane/evidence/targeted/worker-bridge suites passed `83`. Asset governance passed with `88` claims and `1495` deterministic tests (`fast=1393`, `integration=89`, `workflow=13`, `live=8`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 6.5 Update live selection, strict preflight, versioned reports, and archive scans so all six cases use unique identity, one outer attempt, scenario-specific token/tool/time bounds, honest `LIVE_REAL_DEPENDENCIES` focused claims, and bounded redacted identity/invariant/metric/attempt/resource diagnostics; directly test malformed/sensitive reports and prove the archive scanner catches one known sensitive fixture instead of passing an empty scan. @impl EVH-002, EVH-005, EVH-009, EVH-010
  - Selection/preflight: the live selector now collects exactly six stable canary parameters plus the model/web preflight tests; all cases declare unique-per-invocation identity and one outer attempt. Model-only and web-dependent cases use strict scenario-specific preflight, and each canary has an exact central live claim at its honest seam/authenticity rather than borrowing public-entry or full-pipeline proof.
  - Bound enforcement: `_BridgeFactory` now requires the selected `LiveScenario` and derives hard model-call, tool-call, token, and wall-time budgets from that case's metadata. This closed a real mismatch where the two-call Wave2 repair allowance had previously been reduced to one call by a generic zero-tool branch. Zero-tool policies retain the production budget model's positive internal minimum while exposing no allowed/resolved tools and reporting zero calls.
  - Versioned diagnostics: evidence-v1 reports now carry a fixed structural diagnostic summary for redacted identity presence/distinctness, invariant counts, typed metric status counts, outer/workflow attempts and retries, and model/tool/token/cost availability. Raw thread/run/research ids never enter the report. The report constructor validates this schema and all cross-field counts against top-level authority.
  - Sensitivity/bounds: attempt diagnostics redact credentials, host paths, and URLs and are capped at 4096 characters; direct malformed, sensitive-URL, oversize, and cross-field mismatch fixtures fail closed. The bounded archive scanner requires a non-empty current evidence-v1 JSON set, reconstructs each typed report, rejects duplicate cases and aggregate/per-file oversize input, and its smoke test catches a known sensitive URL fixture rather than succeeding on an empty scan.
  - Validation: focused live/report/seed/fixture/lane/evidence/workflow suites passed `87`. Asset governance passed with `88` claims and `1507` deterministic tests (`fast=1405`, `integration=89`, `workflow=13`, `live=8`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed. Existing gitignored pre-change reports were not used as evidence-v1 execution proof.
- [x] 6.6 Reassign the existing three 300-second deadlines and add the new case deadlines under one deterministic budget contract enforcing the six-case sum `<=900s`, each web case `<=240s`, each zero-tool case `<=120s`, and the unchanged 20-minute job timeout; directly test one per-case and one aggregate deadline violation. @impl EVH-005, EVH-009
  - Deadlines: `live-start-to-hitl1=90s`, `live-hitl1-to-topic-planning=120s`, `live-one-topic-wave0=210s`, `live-one-topic-wave1=180s`, `live-wave2-synthesis=120s`, and `live-one-gap-targeted-evidence=180s`. The exact declared sum is `900s`.
  - Deterministic contract: `validate_live_canary_deadlines` requires exactly six unique cases, the unchanged `1200s` job timeout, each web case at most `240s`, each zero-tool case at most `120s`, aggregate at most `900s`, and at least `300s` setup/preflight/report/cleanup margin. The first green attempt caught and rejected an accidental `960s` total before targeted evidence was corrected to `180s`.
  - Negative proof: parameterized per-case fixtures independently reject a `241s` web deadline and a `121s` zero-tool deadline; a separate fixture keeps all individual cases legal while exceeding the aggregate and is rejected as `live_aggregate_deadline_invalid`. Bridge policies continue to derive their wall budgets from these same declarations.
  - Validation: focused deadline/live-report/lane/workflow suites passed `45`. Asset governance passed with `88` claims and `1511` deterministic tests (`fast=1409`, `integration=89`, `workflow=13`, `live=8`, `release=1`). Ruff/format, strict OpenSpec, `git diff --check`, the unchanged workflow `timeout-minutes: 20`, and the empty protected-tree diff passed.
- [x] 6.7 Preserve the complete six-case live attempts with fresh identities and schema-valid redacted per-case evidence, then explicitly defer the still-red current-provider green aggregate to `_backlog/plans/deep-research-six-case-live-closure.md`; do not use partial or gitignored pre-change evidence to claim live success or enable nightly. @impl EVH-002, EVH-005, EVH-009
  - Attempt 1 (`2026-07-18`, fresh report root `.reports/live-evidence-v1-20260718`): strict preflight passed with model provider `deepseek` and web provider `tavily`; the complete selector collected eight items and finished in `207.16s`. Start-to-HITL1, HITL1-to-topic-planning, one-topic Wave0, and one-topic Wave1 passed; Wave2 synthesis failed with typed `live_wave2_failed:ValidationError`; one-gap targeted evidence failed with typed `live_targeted_failed:AssertionError`; both strict preflight tests passed. Six schema-valid per-case reports were preserved. At that point the original live-acceptance task remained incomplete pending focused diagnosis, a fresh complete six-case pass, and aggregate validation.
  - Diagnosis: Wave2 made two model calls and returned the same structurally valid JSON shape twice, but each gap included `search_required`; the production prompt describes a `search_required` flag without limiting it to findings, while the canonical `GapRecord` forbids the field. Raw missing `finding_id` is normalizer-supported and is not the blocking error. Targeted evidence initially spent all three tool calls without a final response; a test-owned two-call tool window produced a third no-tool response, but it was prose rather than JSON. The production targeted worker has no bounded structured-output repair, so the subgraph correctly published no ledger authority.
  - Attempt 2 (targeted-only, fresh report root `.reports/live-evidence-v1-20260718-targeted-repro`): the focused two-tool submit window was enforced (`2` tool calls, `14.01s`), and response shapes were two tool-only turns followed by one `230`-character prose turn; the case remained a typed failure with no accepted targeted record. The responsible fixes are production Wave2 prompt/schema alignment and targeted structured-output repair, both under protected `agent/src`; test-side coercion would fabricate provider compatibility. Per the reviewed protection boundary, implementation must return to proposal/design review before changing production.
  - Production-review handoff (`2026-07-18`): the reviewed planning and completed implementation are archived at `openspec/changes/archive/2026-07-18-harden-late-node-structured-output`. Review expanded the initially observed field mismatch into the complete responsible authority path: canonical gap flag -> private typed Wave2 gate preview -> semantic `evidence_needed`/bounded `exhausted` gate routes -> gate-owned searchable gap ids -> targeted worker; it also records the later exactly-one-search request contract and one separate zero-tool structured repair. The production change is complete, while this task remains open for its independently owned six-case live aggregate.
  - Production-change attempt (`2026-07-18`, `harden-late-node-structured-output`, fresh report root `.reports/live-evidence-v1-20260718-173821`): deterministic remediation was green before the one-shot run. External strict preflight passed (`deepseek`/`tavily`); the exact selector collected eight items and finished in `238.46s`. Start-to-HITL1, HITL1-to-topic-planning, Wave0, and targeted evidence passed; Wave1 and Wave2 failed; both strict-preflight tests passed. The targeted fix was confirmed live with two Tavily calls, one attempt, no retry, valid authority, and `32.678s`. Wave1 instead consumed three tool-only turns with empty final content and failed closed; Wave2 produced one schema-valid response containing zero findings plus one searchable gap and then failed a downstream assertion. No case was selectively rerun.
  - Fresh aggregate evidence: `scan_live_report_archive` accepted all six exact evidence-v1 case reports as schema-valid, bounded, unique, and redacted. Their declared wall-time sum was `234.158s`, preserving the `900s` bound and five-minute job margin. All cases used one outer/workflow attempt and zero retries; tokens were available, costs unavailable, and the four passing cases recorded four of four invariants with their selected metrics honestly unavailable for insufficient authority. The two failed reports recorded typed error/shape diagnostics and zero passed invariants without raw model content. Because six passing reports were then mandatory, the original task stayed unchecked.
  - Final dependent attempt (`2026-07-18`, `.reports/live-evidence-v1-20260718-185139`): after request-window, cumulative parallel-quota, Wave2 prompt-schema, semantic-floor, and live-report projection corrections, the complete selector passed seven cases and recorded one typed targeted authority failure. Wave1 and Wave2 passed with one tool call/zero tools respectively; targeted's first response proposed three parallel calls and was correctly denied before dispatch. A separate focused one-search diagnostic later reached provider timeout, but is not part of the complete aggregate. Archive scan accepted all six complete-run reports as schema-valid, bounded, unique, and redacted. The original task remained unchecked at that point; this was not a six-case green lane.
  - Deferral handoff (`2026-07-18`): further credentialed retries moved to `_backlog/plans/deep-research-six-case-live-closure.md`, which preserves the evidence roots, re-entry triggers, exact one-shot selector, archive scan, deadline/margin, and six-pass completion contract. Deferral is not live success.
  - Completion meaning: this task is now checked because its bounded evidence preservation and explicit risk handoff are complete. The aggregate itself remains red; no six-case provider-compatibility claim is made.
- [x] 6.8 Keep all six live cases manual-only after the red aggregate and defer nightly restoration until `_backlog/plans/deep-research-six-case-live-closure.md` closes with one measured six-case pass preserving the five-minute margin without raising the job timeout. @impl EVH-005, EVH-009
  - Scheduling decision after the dependent production attempt: elapsed time preserved the margin, but correctness did not pass all six cases. `.github/workflows/agent-live-evaluation.yml` therefore remains manual-only (`workflow_dispatch`) and retains `timeout-minutes: 20`; no nightly schedule was restored.
  - Deferred cadence: the schedule may be restored only after the linked backlog plan closes with one fresh six-case green aggregate and the existing margin; archiving the production-hardening change does not satisfy this task.
  - Completion meaning: the reviewed cadence decision is complete and `.github/workflows/agent-live-evaluation.yml` remains intentionally manual-only. This checkbox does not mean nightly is enabled.

## 7. Requirement Governance, Documentation, And Final Gates

- [x] 7.1 Add a small requirement-evidence policy mapping cross-lane requirements to required independent asset classes/authenticity (including workflow for `EVH-004/008` and deterministic/live/release for `EVH-005`), retain the collected deterministic test-side `@impl` requirement for every alive requirement, and add missing-class, missing-`@impl`, unknown-id, and ordinary-requirement acceptance tests. @impl EVH-008, EVH-009, EVH-010
  - Policy: a small test-owned closed registry requires `EVH-004/008` to have collected deterministic workflow evidence with `SCRIPTED_REAL_WORKFLOW`, and requires `EVH-005` to have independent deterministic correctness, live `LIVE_REAL_DEPENDENCIES`, and release `FULL_REAL_PIPELINE` evidence. Asset classes are joined independently; no ordered authenticity minimum substitutes one class for another.
  - Deterministic ownership: alive requirement ids load only from main OpenSpec `> req:` headers, while deterministic `@impl` ids load only from modules that contribute an actually collected deterministic selector. Every alive requirement retains this test-side ownership proof; active-delta-only ids and uncollected test modules cannot satisfy it.
  - Missing central evidence closed: exact release selector `tests/e2e/test_release_acceptance.py::test_full_real_release_acceptance` now has one `RELEASE_ACCEPTANCE`/`PUBLIC_ENTRY`/`FULL_REAL_PIPELINE` claim, and the versioned live report/archive contract supplies EVH-005 deterministic correctness. The asset checker joins the policy to all five focused collections after ordinary claim selection validation.
  - Direct proof: synthetic contracts cover a valid cross-lane set, missing release class, missing deterministic `@impl`, unknown policy id, ordinary-requirement acceptance, main-spec-only alive loading, and collected-module-only `@impl` loading. Existing root requirement and asset-checker contracts remain complementary rather than duplicated.
  - Validation: requirement-policy/root-coverage/asset-checker focused suites passed `17` before final formatting and the policy suite passed `7` after formatting. Asset governance passed with `90` claims and `1518` deterministic tests (`fast=1416`, `integration=89`, `workflow=13`, `live=8`, `release=1`); root requirement coverage, Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 7.2 Generate and commit a minimal redacted release attestation from the design-frozen accepted evidence with source date/hash, attestation base revision explicitly distinct from the unknown run revision, counts/bounds, all eight frozen named invariant results, separately scoped source-run/source-archive-scan/attestation-scan evidence, an exact committed source locator plus original live/release target scope for the historical scan, and no unsupported metadata; add direct deterministic schema/provenance/sensitivity tests with minimal invalid attestations, prove the archive/sensitivity scan detects a known invalid fixture, and update or explicitly supersede the contradictory `NOT READY / full-real not executed` parity report. @impl EVH-005, EVH-010
  - Committed artifact: `agent/docs/release-attestation-2026-07-17.json` records schema/scenario, source observation date, the design-frozen source SHA-256, attestation base revision, explicit `run_revision=unknown`, one attempt/zero retry, all eight named true invariants, accepted/citation counts, token/tool counts, and bounded duration. It contains no invocation ids, provider shapes, model text, URLs, credentials, host paths, execution timestamp, or invented run revision.
  - Provenance split: `source_run` contains only facts joined to the hashed local source JSON; `source_archive_scan` cites exact committed locator `openspec/changes/archive/2026-07-17-evaluate-harden-deep-research-graph/tasks.md#task-6.6` and preserves the original `agent/.reports/live` plus `agent/.reports/release` target scope; `attestation_scan` describes only deterministic scans of the committed JSON. The local source SHA exactly matched `36f41ca7...8146`; every source field joined exactly except the reviewed duration's sub-microsecond decimal truncation, validated with an explicit `1e-6s` absolute-only tolerance.
  - Validation/sensitivity: exact-field and numeric bounds reject unsupported metadata, missing invariant, non-unknown/equal run revision, bad archive locator, and source-run mismatch. A known JSON fixture containing a private HTTP URL is detected as sensitive; the committed attestation scans clean for credential values and raw host paths.
  - Contradiction closure: the pre-acceptance DPT parity report is retained as historical analysis but now begins with an explicit `Superseded` status and points to the committed attestation, so its embedded `NOT READY / full-real not executed` snapshot cannot be read as the current release-evidence verdict.
  - Validation: attestation/parity suites passed `11`. Asset governance passed with `90` claims and `1528` deterministic tests (`fast=1426`, `integration=89`, `workflow=13`, `live=8`, `release=1`); all three root governance checks, JSON parse, Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed. No fresh full-real release run was executed.
- [x] 7.3 Align `agent/README.md`, the human-authored test-policy section of `agent/AGENTS.md`, `agent/docs/regression-descent.md`, the live baseline, and the governance/OpenSpec bootstrap pointers established in task 1.2 with the final disjoint selections, central evidence claims, family/case semantics, provider provenance, scripted-case versus shape-fixture versus persisted-trace terminology, focused-live disclaimers, typed metrics, committed-attestation policy, and the continued single-E2E policy without copying enumerable catalogs into prose. @impl EVH-001, EVH-002, EVH-005, EVH-009, EVH-010
  - Selection/authority guidance: README and AGENTS now describe the exact fast/integration/workflow deterministic union, central `TestEvidenceClaim` authority, six focused live canaries, strict preflight, and the single manual release E2E without copying the claim/case catalogs into prose. Late-node live cases explicitly disclaim public-entry, predecessor-lifecycle, production-recipe, and full-pipeline coverage.
  - Terminology/metrics: both human entry points distinguish a short scripted case through the real loop, a minimized provider-shape fixture at a normalization/validation seam, and persisted trace replay reserved for reviewed temporal needs. Evidence-v1 typed statuses/evidence bases, unavailable authority, and the non-blocking threshold policy are stated consistently.
  - Durable/current evidence: both guides point to `docs/release-attestation-2026-07-17.json` as one isolated accepted full-real proof, while the baseline now records the 2026-07-18 four-pass/two-fail evidence-v1 observation honestly. Historical release attestation does not substitute for current provider distribution or six-case live success.
  - Open findings/cadence: regression descent retains exact collected historical selectors and adds a separate current-open section for the Wave2 gap-field and targeted prose-output production findings without inventing discovery ids or test-side closure. Because the measured six-case lane is not green, `agent-live-evaluation.yml` is now manual-only; its schedule can return only after `_backlog/plans/deep-research-six-case-live-closure.md` records a fresh pass with margin. The release workflow remains the sole full-pipeline lane.
  - Bootstrap alignment: the already-established OpenSpec config, governance README, policy file, and AGENTS pointer remain concise and aligned; no enumerable policy was duplicated there. Documentation contracts also caught and corrected the missing stable parameter id for the Wave1 source-order regression selector.
  - Validation: documentation/regression/workflow/attestation suites passed `17`; the final documentation-focused subset passed `6`. Asset governance passed with `90` claims and `1532` deterministic tests (`fast=1430`, `integration=89`, `workflow=13`, `live=8`, `release=1`); all four root governance checks, workflow YAML parse, Ruff/format, strict OpenSpec, `git diff --check`, and the empty protected-tree diff passed.
- [x] 7.4 Run strict OpenSpec, all three root requirement/spec/architecture governance checks, asset and requirement coverage, lock, Ruff, deterministic fast/integration/workflow/full suites, viability, durability, blocking-I/O and all new collector/discovery/archive detector smoke cases, live report/redaction/aggregate-deadline invalid-fixture tests, committed release-attestation schema/redaction/eight-invariant validation and scan smoke, and repository-boundary checks; preserve the explicit live/nightly deferral, do not rerun credentialed live or full-real unless its reviewed trigger is met, and compare `agent/src`, `backend`, `frontend`, release runner, and public entry with the exact protected tree hashes recorded by task 1.1 while preserving any documented pre-existing user changes. @impl EVH-001..EVH-010
  - Deterministic suites: `make test-fast` passed `1431` with `2` deselected; `make test-integration` passed `85`, skipped `4` Gateway-unavailable cases, and deselected `12`; `make test-workflow` passed all `13`; the aggregate `make test` passed `1529`, skipped the same `4`, and deselected `10`. Viability passed `5`; durability passed `9` with the explicitly deferred Postgres case skipped; blocking-I/O passed `9`.
  - Focused governance/sensitivity proof: the new collector, discovery, invalid-fixture, and archive-detector set passed `39`; live report/redaction/deadline fixtures, release-attestation schema/provenance/redaction/eight-invariant validation, and their known-sensitive scan smokes passed in their focused suites. The post-format live/release control-plane compatibility subset passed `53`.
  - Static and repository gates: strict OpenSpec; requirement, spec, architecture, and requirement-to-test governance; asset governance; lock check; Ruff check; Ruff format check across `332` files; and `git diff --check` all passed. The final asset collection contains `90` central claims and `1533` deterministic tests partitioned exactly as `fast=1431`, `integration=89`, `workflow=13`, with `live=8` and `release=1` outside that union.
  - Protected boundaries: current Git objects exactly match task 1.1 for `agent/src=32f747773f31de7d767e90ad52015e06011316b7`, `backend=e033d4751b5f9a517b9fb249d25eceb8092ef1be`, `frontend=b5e77197a08602fe194ded9d8045468d7ec9de3a`, release E2E/workflow/preflight, and all three public-entry blobs. `agent/tests/scenarios/release.py` is the sole protected-list blob difference (`bb2667f...` to `32c7743...`): its only diff replaces the removed legacy `tests.scenarios.model.AuthenticityLevel` import with the central `tests.assets.evidence.AuthenticityLevel`; focused release compatibility is green and no release/public-entry behavior changed.
  - Rerun decision and former open gate: no fresh full-real release run was triggered because production/public entry and the release control plane remain unchanged, and the reviewed policy requires proposal/design review before production repair. Before the explicit deferral decision, this task stayed unchecked while the six-case pass and nightly enablement were treated as prerequisites; the active warning was the pre-existing `VIRTUAL_ENV=backend/.venv` mismatch, plus the documented Pydantic serializer warnings and deferred/unavailable integration skips above.
  - Dependent production final-gate refresh (`2026-07-18`): strict validation for both changes; all four root governance checks; lock, Ruff, format, asset and requirement governance; fast `1454`, integration `85 + 4` Gateway skips, workflow `13`, aggregate `1552 + 4` Gateway skips; viability `5`, durability `9 + 1` deferred Postgres skip, blocking-I/O `9`, and focused invalid/authority smoke `141` all passed. Zero-API demo and infra probes passed. Protected upstream/release/public objects remain exact, and no release E2E was triggered because the two fresh failures are diagnosable below the public pipeline while the separate HITL2 content-projection design finding remains open. At that point task 7.4 stayed unchecked solely because tasks 6.7/6.8 still required a six-case green lane and nightly enablement.
  - Deferral status (`2026-07-18`): `_backlog/plans/deep-research-six-case-live-closure.md` now owns the future credentialed closure. Task 7.4 remains independently responsible for final deterministic/governance/boundary proof and cannot claim live success from those gates.
  - Archive/sync closeout (`2026-07-18`): `harden-late-node-structured-output` was synced into the five owning main specs and archived at `openspec/changes/archive/2026-07-18-harden-late-node-structured-output`; the active OpenSpec list now contains only this change. No product/test implementation changed after the recorded fast `1457`, integration `85 + 4` Gateway skips, workflow `13`, aggregate deterministic, viability, durability, blocking-I/O, and focused `141` results above, so those complete-suite executions remain the implementation-state proof.
  - Fresh affected-surface gates: strict validation passed; requirement consistency reported `128` registered ids and `154` current main/active-delta occurrences; all `23` main specs, architecture governance, and requirement-to-test coverage passed. Asset governance passed with `90` central claims and `1559` deterministic tests partitioned as `fast=1457`, `integration=89`, `workflow=13`, plus `live=8` and `release=1`. Lock, Ruff, format check across `332` files, and `git diff --check` passed.
  - Fresh focused/boundary proof: architecture, requirement coverage, regression descent, evidence documentation, release attestation/evidence report, live workflow, provider-shape archive, and live report/redaction/deadline contracts passed `101`. `backend`, `frontend`, the release E2E/workflow/preflight, and all three public-entry surfaces have empty diffs; the reviewed dependent production paths remain owned by the archived hardening change. No credentialed live or release E2E was run, and `.github/workflows/agent-live-evaluation.yml` remains manual-only with the unchanged 20-minute timeout.
