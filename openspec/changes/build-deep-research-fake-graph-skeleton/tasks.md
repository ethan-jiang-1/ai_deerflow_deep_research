Execution order is a hard dependency chain: groups `1 -> 2 -> ... -> 12`, and
tasks within each group run in numeric order. Group 1 is a hard viability gate;
do not create the business topology if reflected `Command`/artifact propagation
does not work on the pinned stack.

## 1. Reflected HITL And Host Viability Gates

- [ ] 1.1 Add a dedicated reflected test fixture tool that returns `Command(goto=END)` with one version-1 human-input `ToolMessage`, then add a red integration test that resolves that fixture through DeerFlow's public configured-tool loader and invokes the pinned async ToolNode path, asserting the returned command list preserves `goto`, active `tool_call_id`, normalized real tool name, stable message id, and `artifact.human_input` (`RUI-006`, `REG-003`).
- [ ] 1.2 Make the fixture viability test green using only public LangChain/LangGraph APIs; if `Command` or `ToolMessage.artifact` is lost, stop the change and revise the design without editing production code, `backend/`, or `frontend/` (`RUI-006`, `REG-003`).
- [ ] 1.3 Add a red current-production dispatch test proving two ordinary reflected `infra_probe` invocations in one process must resolve the same lazily created GraphHost/memory saver while two explicitly injected test hosts remain independent (`RUI-001`, `RUI-006`, `REG-005`).
- [ ] 1.4 Implement the process-local default-host resolver initially registering only `InfraProbeHandler`, without retaining request envelopes or SQL providers; keep test reset/injection deterministic and make the existing `infra_probe` same-process revisit work through default dispatch. Later task 8.5 adds research handlers to this same host factory; annotate the resolver with `@impl RUI-006` (`RUI-001`, `RUI-006`).

## 2. Skeleton State And Pure Lifecycle Contracts

- [ ] 2.1 Add red unit tests for the versioned minimal skeleton state and pure lifecycle contracts: opaque start-message correlation/request digest, bounded exact fake request, validated persisted fixture plan with closed typed route/decision enums, bounded execution trace, lifecycle/phase/terminal enums, typed accepted-human-response/internal-cancel resume union, closed version-1 lifecycle result-code enum plus preserved registered change-00 infrastructure codes, `full_fake` control-result schema/4,096-character serialization bound/research-content exclusion, generation and independent repair/rerun counters, consumed response ids, and deterministic Wave branch reducers. Explicitly reject duplicate mutable pending-HITL/cursor state fields and cover arbitrary free-form result codes, duplicate branch ids, invalid fixture routes, unbounded inputs, raw authority fields, and unknown schema versions (`REG-002`, `REG-003`, `REG-004`, `REG-005`).
- [ ] 2.2 Implement shared pure lifecycle/route-decision/control-result contracts and deterministic bounded serializer under `domain/`, and the temporary checkpoint schema/reducers plus validated closed fixture plan under `graph/skeleton_state.py`, with no IO or runtime authority fields. Do not create canonical `domain/state.py` before change 02; document that change 02 must migrate the builder and delete the graph-owned skeleton schema so only one state authority remains. Annotate owning surfaces with `@impl REG-002`, `@impl REG-003`, `@impl REG-004`, and `@impl REG-005` (`REG-002`..`REG-005`).
- [ ] 2.3 Add red tests for a frozen non-checkpointed invocation-context contract and domain dependency-resolver protocol, including missing node, wrong logical name/policy, any duplicated fixture-plan field, authority leakage, same uncommitted visit retaining its attempt id after retry, and two committed executions of the same node receiving distinct deterministic attempt ids derived from prior completed trace visits (`REG-001`, `RUI-006`).
- [ ] 2.4 Implement the domain-owned invocation context containing only `GraphContextView` plus the `NodeDependencyResolver` protocol used by LangGraph `context_schema`; keep concrete runtime binding outside graph state, reject fixture duplication in context, and preserve the existing NodeBuildDependencies factory boundary (`REG-001`, `RUI-006`).

## 3. Explicit Topology And Implementation Selection

- [ ] 3.1 Add red graph-contract tests for the eleven stable logical node names, normalized edge/route representation, START/terminal reachability, no duplicate/unreachable node, no implicit filesystem discovery, and no internal component exposed as a top-level phase (`REG-001`, `REG-005`).
- [ ] 3.2 Implement `graph/topology.py` as the single normalized node/edge truth and `graph/routing.py` as pure typed-verdict routers; annotate the topology owner with `@impl REG-001` and snapshot owner with `@impl REG-005` (`REG-001`, `REG-005`).
- [ ] 3.3 Add red implementation-map tests for full-fake resolution, the canonical pure `UNAVAILABLE_REAL_FACTORY` sentinel, explicit mixed test overrides, unknown node/mode, incomplete map, unavailable real implementation, duplicate logical NodeSpec, and prohibition on silent real-to-fake fallback (`REG-001`).
- [ ] 3.4 Add the canonical sentinel to the pure NodeSpec contract, implement `graph/implementation_map.py`, and extend the explicit registry/builder surface so every logical node resolves once from its package-root `NODE_SPEC`; reject sentinel-selected real behavior before checkpoint invocation and annotate the owner with `@impl REG-001` (`REG-001`).

## 4. Canonical Node Packages

- [ ] 4.1 Add red package-shape and contract tests for `bootstrap` and `topic_planning`, including package-root-only `NODE_SPEC`, private contracts, deterministic fake factory, canonical unavailable-real sentinel, and forbidden imports (`REG-001`, `REG-002`).
- [ ] 4.2 Implement the two packages with typed fixture inputs/results and bounded logical trace updates; do not call agent capabilities or IO (`REG-001`, `REG-002`).
- [ ] 4.3 Add red package-shape and contract tests for `wave0` and `wave1`, including their optional phase-local subgraph ownership and no sibling-node imports (`REG-001`, `REG-002`).
- [ ] 4.4 Implement the Wave package roots, typed contracts, fake entry factories, canonical unavailable-real sentinel, and subgraph modules without exposing dispatch/join workers as top-level nodes (`REG-001`, `REG-002`).
- [ ] 4.5 Add red package-shape and contract tests for `wave2_synthesis` and `targeted_evidence`, including typed targeted-repair/convergence outputs (`REG-001`, `REG-002`).
- [ ] 4.6 Implement those two packages with fixture-only fake behavior and no sandbox/model access (`REG-001`, `REG-002`).
- [ ] 4.7 Add red package-shape and contract tests for `rerun`, `readiness`, and `final_delivery`, including generation increment, bounded readiness repair, typed completion/blocked outputs, and a fake final marker that contains no finding/evidence/citation/report fields; stop/cancel decisions remain owned by the HITL packages and graph routing (`REG-001`, `REG-002`, `REG-004`).
- [ ] 4.8 Implement the final three packages with deterministic fake behavior and the canonical unavailable-real sentinel (`REG-001`, `REG-002`, `REG-004`).

## 5. Deterministic Send Fan-Out And Fan-In

- [ ] 5.1 Add red Wave0 phase-subgraph tests that dispatch exactly three public LangGraph `Send` tasks, complete them in permuted order, reduce each unique branch once, reject duplicate/unknown branch ids, and join into one normalized parent result (`REG-002`).
- [ ] 5.2 Implement the Wave0 dispatcher/worker/join subgraph with deterministic fixture results and no persistent subgraph checkpointer; annotate the owner with `@impl REG-002` (`REG-002`).
- [ ] 5.3 Add the equivalent red tests for Wave1, including stable output across scheduler ordering and no advance before all three branches join (`REG-002`).
- [ ] 5.4 Implement the Wave1 phase-local Send/reducer/join subgraph and keep every internal component out of the top-level topology (`REG-002`).
- [ ] 5.5 Add spy-backed tests proving full-fake Wave execution never invokes a model, web/MCP/ACP tool, DeerFlow `task` subagent, node-agent capability, or sandbox research tool and writes no research artifact (`REG-002`).

## 6. HITL Node Packages, Interrupt, And HumanMessage Correlation

- [ ] 6.1 Add red package-shape/contract and focused graph tests for `hitl1` and `hitl2`: package-root `NODE_SPEC`, canonical unavailable-real sentinel, request ids derived from research/phase/generation/prior-HITL ordinal, stable retry, distinct same-generation repeated HITL2 ids, exactly one checkpoint interrupt as pending authority carrying the suspension-time outer HumanMessage cursor, checkpoint-before-return ordering, version-1 request payloads, source `deep_research`, visibly `full_fake` title/context, and free-text/choice modes (`REG-001`, `REG-003`, `REG-004`).
- [ ] 6.2 Implement the two HITL node packages and graph-owned `interrupt()` behavior, plus runtime projection to the outer `Command`/`ToolMessage` contract using `ToolRuntime.tool_call_id` and the group-2 control-result contract/serializer; annotate the bridge/node owners with `@impl REG-003` (`REG-001`, `REG-003`, `REG-004`, `RUI-006`).
- [ ] 6.3 Add red latest-HumanMessage and resume-union tests for matching structured card response (including UI-hidden response metadata), plain visible client response, ordered text-only content blocks with exact no-separator concatenation, non-text/malformed block rejection, valid HITL2 option id/value, tampered or unknown option pairs, plain closed-decision normalization, post-suspension cursor ordering, empty/oversized value, wrong source/request id, hidden summary/dynamic context, AI/ToolMessage, pre-suspension message, no fallback past a failing newest candidate, exact `response_mismatch` versus `response_invalid` classification, consumed message classified for read-only reprojection rather than a new answer, model-supplied answer argument, and internal cancel never parsing as user text (`REG-003`, `REG-004`).
- [ ] 6.4 Implement runtime-owned resume classification after `aget_state()`: first match the latest actual trusted-state HumanMessage against the exact checkpointed consumed request/message pair for read-only durable-result reprojection; otherwise require exactly one pending interrupt, extract only the latest eligible post-suspension HumanMessage, require structured correlation when metadata exists, build the pure accepted-response envelope, and pass it through `Command(resume=...)`. Make the HITL node record consumed request/message ids and its completed logical visit while LangGraph consumes the interrupt, with no duplicate pending state or out-of-graph checkpoint patch. Annotate the owner with `@impl REG-003` (`REG-003`, `REG-004`).
- [ ] 6.5 Add focused fault-injection tests proving the nested interrupt checkpoint exists before an injected outer-projector failure and the same snapshot can be reprojected with a stable request id plus current tool-call id without rerunning the HITL node (`REG-003`, `REG-005`).

## 7. Research Graph Recipe And Routing

- [ ] 7.1 Add red graph tests for default bootstrap -> HITL1 and profile-complete bootstrap bypass, the happy route with real change-01 interrupts, independent Wave0/Wave1 repair loops, Wave2 -> targeted -> Wave2 convergence, every HITL2 decision (`proceed | revise_view | repair | rerun | stop`), each readiness repair target (`targeted_evidence | wave2_synthesis | hitl2`), final-delivery self-repair and evidence-blocked return to readiness, second-generation rerun, and bounded-counter exhaustion; assert routers read typed fields rather than free-form text (`REG-001`, `REG-002`, `REG-004`).
- [ ] 7.2 Implement the request-independent StateGraph recipe with the reduced `context_schema`, node wrappers that derive the current deterministic attempt id and call the runtime-owned dependency resolver before binding the selected NodeSpec factory, all approved edges, and no runtime authority in state; annotate the builder with `@impl REG-001` and route enforcement with `@impl REG-002` (`REG-001`, `REG-002`, `RUI-006`).
- [ ] 7.3 Add red mixed-map tests that replace one fake with an explicit test real implementation while preserving the identical topology/snapshot and refusing a production real selection whose owner is absent (`REG-001`).

## 8. Lifecycle Handlers And Namespace Semantics

- [ ] 8.1 Add a red dispatch-precedence matrix plus action-input, outer-call-correlation, interaction/transport-capability, and start-message tests. Pin the order schema/id grammar -> registered action -> sole latest `deep_research` call -> RuntimeAdapter/capability reduction -> start-message selection when applicable -> provider/lock/checkpoint inspection and resume correlation -> graph/result; assert rejected earlier layers never call later spies. Cover forbidden start question/answer/authority fields, redacted unknown actions, missing/mismatched/duplicate/sibling calls while infra-probe behavior is preserved, `non_interactive`/`disable_clarification` refusal, reduced `channel_user_id`/`channel_name` IM refusal without retaining raw platform identity or appending synthetic AI fallback, status/cancel retained, newest visible genuine HumanMessage selection after ignoring synthetic context, no fallback to an older request when that candidate is invalid, exact `start_message_invalid`, required stable message id, bounded exact string/text-only content blocks with non-text rejection, human-input-response rejection, and no namespace/checkpoint access on invalid input (`RUI-001`, `RUI-006`, `REG-004`).
- [ ] 8.2 Add red deterministic-id and lifecycle-result integration tests proving the canonical compact-JSON tuple `['deep-research/thread/v1', user, thread]` yields exactly `^r_[A-Za-z0-9_-]{43}$`, ambiguous concatenations cannot collide, the same trusted thread is stable, different user/thread scope differs, start message ids are correlation rather than namespace authority, probe/research id grammars cannot be confused, raw identity/request text is not directly encoded or echoed and the id is never treated as authorization, every post-schema registered lifecycle outcome (including sole-call denial) uses the group-2 versioned `full_fake` envelope and closed code set, pre-lifecycle denials omit lifecycle fields/use unavailable durability rather than inventing state, and secret/internal/research-content fields are absent (`RUI-006`, `REG-004`).
- [ ] 8.3 Extend the strict reflected tool schema, reduce interaction/transport signals, implement the deterministic research-id helper and separate start/resume HumanMessage selectors, and normalize dispatch/Command results for `infra_probe | start | resume | status | cancel` through the group-2 contract and group-6 outer projection without changing the reflection path; annotate the tool/runtime owners with `@impl RUI-006` and `@impl REG-004` (`RUI-001`, `RUI-006`, `REG-003`, `REG-004`).
- [ ] 8.4 Add red namespace and handler tests proving all four lifecycle handlers share the identical versioned graph recipe, plus start-new, injected failure after committed HITL checkpoint but before result delivery followed by same-message idempotent reprojection with no repeated node visit, resume delivery failure followed by consumed-message reprojection of the next interrupt/terminal with no graph invoke, malformed stored start correlation or multiple pending tasks returning `checkpoint_inconsistent`, different-message same-thread `thread_research_exists` without mutation, new-thread new lifecycle, read-only `status_ok`, fresh-response pending-only resume, fresh-message terminal/no-pending `invalid_transition`, wrong user/thread and absent namespace both returning indistinguishable `research_not_found`, unsupported `schema_unsupported`, terminal cancel idempotency, and infra-probe/research namespace separation (`RUI-003`, `RUI-004`, `RUI-006`, `REG-004`, `REG-005`).
- [ ] 8.5 Implement the concrete runtime dependency resolver, four research ActionHandlers, deterministic start derivation/correlation, shared trusted namespace derivation, runtime projection, state inspection, typed bounded results, and registration on the process-local default host while keeping SQL provider lifetime per action; annotate owners with `@impl REG-004`, `@impl REG-005`, and `@impl RUI-006` (`RUI-003`, `RUI-004`, `RUI-006`, `REG-001`, `REG-004`, `REG-005`).
- [ ] 8.6 Add red cancel tests for suspended cancellation through an internal resume decision, repeated terminal idempotency, stopped/completed behavior, same-namespace lock serialization, and explicit non-claim of active cross-worker preemption (`REG-004`, `RUI-006`).
- [ ] 8.7 Implement cancel as a normal graph transition rather than raw checkpoint rewriting and preserve ordinary outer asyncio cancellation propagation/cleanup for actively executing actions (`REG-004`, `RUI-004`).

## 9. Topology Snapshot And Structural Governance

- [ ] 9.1 Add a red semantic snapshot/diagram test covering normalized logical nodes, labelled edges, terminal reachability, and exclusion of phase-local Wave components; make formatting-only ordering changes deterministic (`REG-001`, `REG-005`).
- [ ] 9.2 Generate and commit the topology snapshot/diagram from the normalized topology and document the regeneration command; annotate the generator or snapshot owner with `@impl REG-005` (`REG-005`).
- [ ] 9.3 Update `openspec/governance/project-structure.toml` with the new current topology/routing/implementation-map, graph-owned skeleton schema, pure lifecycle contracts, runtime control modules, and eleven node-package paths; explicitly keep canonical `domain/state.py` absent until change 02, regenerate the bounded `agent/AGENTS.md` structure block, update governance fixtures, and keep the existing node-package grammar unchanged (`PRS-001`, `PRS-003`, `PRS-004`, `REG-001`).
- [ ] 9.4 Run the architecture checker against valid and malformed new-node fixtures and require no unowned structural drift or forbidden dependency edge (`PRS-002`, `PRS-003`, `PRS-004`).

## 10. Zero-API End-To-End And Restart Recovery

- [ ] 10.1 Add a full public-surface memory E2E for start -> HITL1 response -> HITL2 proceed -> readiness -> final, asserting the start request comes from the actual latest user HumanMessage, two stable human-input messages, same-process host reuse, versioned `full_fake` control results, a terminal fixture marker with no research content, and no external calls (`REG-002`..`REG-005`, `RUI-006`).
- [ ] 10.2 Add public-surface E2E fixtures for Wave0/Wave1 repair, targeted convergence, HITL2 revise-view/repair/rerun/stop, all readiness repair targets, final-delivery self-repair/evidence-blocked return, cancel, non-interactive and known-IM-transport start/resume refusal with status/cancel retained, different-message same-thread start conflict/new-thread independence, wrong-thread access, stale request denial, consumed-response idempotent reprojection, fresh-message completed resume rejection, and status read-only behavior (`REG-002`..`REG-005`, `RUI-006`).
- [ ] 10.3 Add a subprocess fixture that starts to HITL on file-SQLite, exits after provider closure, then resumes in a fresh process from the matching HumanMessage; verify the same research namespace is recovered and outer/probe checkpoints remain untouched (`REG-003`, `REG-005`, `RUI-003`, `RUI-004`).
- [ ] 10.4 Run the environment-appropriate infra probe before and after fake lifecycle actions on the retained memory host and file-SQLite provider, proving probe visit semantics and research isolation remain green (`RUI-001`, `RUI-004`, `RUI-005`, `RUI-006`).
- [ ] 10.5 Assert memory and SQLite memory mode report same-process only, file-SQLite reports restart durable, and no test or documentation claims Postgres, Docker live smoke, multi-worker coordination, or cross-worker cancellation (`REG-005`).
- [ ] 10.6 Add agent-owned Blockbuster runtime tests covering the new async lifecycle action, nested graph, Send fan-in, provider cleanup, and outer Command projection paths, plus a `make -C agent test-blocking-io` target that runs only those deterministic tests (`RUI-004`, `RUI-006`, `REG-002`, `REG-005`).

## 11. Entry Guidance, Diagnostics, And Documentation

- [ ] 11.1 Add red public-skill and Agent/SOUL replay tests showing a research request calls `start` as the sole tool call without copying the question/id into tool arguments, the returned suspended result preserves the research id for a matching reply, that reply calls `resume` as the sole tool call with only the research id, every lifecycle response visibly preserves `implementation_mode=full_fake`, and the lead never presents the terminal fixture as findings/report/completed research. Also assert status/cancel accuracy and absence of topology, phase prompts, fixture controls, exclusive-tool-surface claims, authority claims, or answer payload instructions (`RUI-006`, `REG-003`, `REG-004`).
- [ ] 11.2 Update the committed public skill and Agent/SOUL guidance to label the lifecycle as a development full-fake skeleton and prohibit research-output claims, materialize/check it through existing configurator/provisioning/rollback fixtures, and preserve the same config tool/group/reflection, public path, per-user Agent path, API provisioning boundary, and extensions enabled-state entries; changes take effect on next agent build (`DEC-003`, `DEC-004`, `RUI-006`).
- [ ] 11.3 Extend doctor/reflection tests only as needed to recognize the expanded action description and combined host while proving no startup-only config, mount, provider, sandbox, worker-count, launcher, or restart rule changes (`RUI-006`, `DEC-005`).
- [ ] 11.4 Synchronize root `README.md` and `AGENTS.md`, `agent/README.md`, human-authored `agent/AGENTS.md`, the 01/master plans, topology regeneration docs, and OpenSpec traceability. Document the `full_fake`/Web-UI-or-generic-client boundary and known IM/non-interactive refusal, and keep deferred launcher/Docker/Postgres work out of this change (`REG-001`..`REG-005`, `RUI-006`, `PRS-004`).

## 12. Final Verification And Archive Gates

- [ ] 12.1 Run `make -C agent lock-check`, `make -C agent format`, `make -C agent lint`, `make -C agent test`, `make -C agent test-viability`, `make -C agent test-durability`, the file-SQLite lifecycle restart E2E, and `make -C agent test-blocking-io`; require no real LLM/network call and record skips honestly (`RUI-006`, `REG-001`..`REG-005`).
- [ ] 12.2 Run isolated `configure.py --check`/materialization parity and doctor checks, verifying the public skill/Agent updates are next-agent-build only and the existing startup fingerprint/provider/mount contract is unchanged (`DEC-002`..`DEC-005`, `RUI-006`).
- [ ] 12.3 Run `python3 openspec/governance/check_project_architecture.py` and require the registry, generated guide block, current paths, import boundaries, and all node packages to conform (`PRS-001`..`PRS-004`, `REG-001`).
- [ ] 12.4 Run `python3 openspec/governance/check_project_reqs.py` and require zero duplicate, unregistered, orphan, and reused-retired IDs (`RUI-006`, `REG-001`..`REG-005`).
- [ ] 12.5 Run `python3 openspec/governance/check_project_specs.py` and require zero main-spec structure violations (`RUI-006`, `REG-001`..`REG-005`).
- [ ] 12.6 Run `openspec validate build-deep-research-fake-graph-skeleton --type change --strict`, resolve every schema/scenario error, verify every new requirement has task coverage plus an owning `@impl` annotation, and confirm no files under `backend/` or `frontend/` changed (`RUI-006`, `REG-001`..`REG-005`).
