Execution order is a hard dependency chain: groups `1 -> 2 -> ... -> 15`, and tasks within a group run in numeric order. A production module owned by a later group cannot be created early merely to make an earlier test pass; earlier groups use explicit test fixtures at those boundaries.

## 1. Package And Test Foundation

- [x] 1.1 Create the independent `agent/pyproject.toml`, committed `agent/uv.lock`, single-source `__about__.py` version usable with or without installed distribution metadata, editable repository source for the compatible DeerFlow 2.1 harness, operations extra with the pinned round-trip YAML dependency, Postgres-test extra forwarding the harness Postgres dependencies, lazy package exports, and agent-owned format/lint/test commands; annotate the package owner for `PRS-001`.
- [x] 1.2 Add red zero-dependency governance fixtures for a valid structure manifest plus missing/malformed TOML, absolute/traversing/duplicate paths, unknown requirement owners, forbidden upstream ownership, missing/ambiguous lifecycle-appropriate owning-spec reference, improper archived-delta authority after a main spec exists, missing/duplicated/stale `agent/AGENTS.md` controlled-block markers, second source roots, and current-required-path drift (`PRS-001`, `PRS-004`).
- [x] 1.3 Add `openspec/governance/architecture-policy.md` without an independent path enumeration, add the normalized requirement-owned `project-structure.toml`, update the governance README, and implement the Python 3.12 standard-library `check_project_architecture.py` schema/reference/render/path contract against fixtures; annotate its owner with `@impl PRS-004` and keep the incomplete live checkout failing until tasks 1.4-1.5 (`PRS-001`, `PRS-004`).
- [x] 1.4 Scaffold only the manifest-required ownership package/test/config/tooling roots under `runtime/domain/engine/agents/graph/resources`; behavior modules are created by their later red-before-green tasks. Make the live path portion of the `PRS-001` contract green without pre-creating 01+ topology, routing, engine-kernel, or business-node packages.
- [x] 1.5 Add `agent/AGENTS.md` and `agent/README.md` with the checker-rendered actual-structure block, human-authored annotated future tree, stable node shape, shared-code ownership, dependency direction, commands, authority warning, and strict `00 -> 18` order; make the live manifest schema, owning-spec reference, controlled-block, and current-path portions green while group 3 remains responsible for import and node-package enforcement (`PRS-001`..`PRS-004`).

## 2. Runtime Viability Gates

- [x] 2.1 Write a red integration test that loads an async `BaseTool` through DeerFlow's current `tools[].use` resolution and proves `ToolRuntime.context/state` reach the coroutine under async invocation (`RUI-001`, `RUI-002`).
- [x] 2.2 Add only the reflected test fixture needed to make runtime injection green; if the public loader loses `ToolRuntime`, stop this change and revise the design without editing upstream (`RUI-001`, `RUI-002`).
- [x] 2.3 Write a red integration test that cancels an outer asyncio tool task while a minimal nested graph is active and asserts nested work terminates (`RUI-004`, `NOA-005`).
- [x] 2.4 Make cancellation cleanup green using public asyncio/LangGraph behavior only; prove no access to Gateway `RunManager.abort_event`, private run-journal fields, or upstream edits (`RUI-004`, `NOA-005`).
- [x] 2.5 Record both probes as hard viability gates; do not start group 3 or later unless reflected async runtime injection and outer cancellation propagation pass on the pinned stack (`RUI-001`, `RUI-002`, `RUI-004`, `NOA-005`).

## 3. Structure And Import Contracts

- [x] 3.1 Add red manifest-backed AST fixtures covering valid imports plus domain-to-outer, engine-to-outer, agents-to-runtime/graph/nodes, node-to-agents/runtime/graph/sibling, production downstream-to-`app.*`, generic helper module, upstream-to-downstream violations, and manifest/import-policy disagreement (`PRS-002`, `PRS-004`).
- [x] 3.2 Extend the architecture checker and agent-owned contract wrapper with the AST import/folder enforcement and `@impl PRS-002` owner annotation, then make every allow/deny fixture deterministic and green; allow `app.*` only in explicitly marked real-Gateway integration test fixtures and keep the manifest as the exact rule enumeration (`PRS-002`, `PRS-004`).
- [x] 3.3 Add red valid/invalid `NODE_SPEC` fixture packages for missing fake/contracts, unstable logical name, internal export leakage, node-to-registry reverse import, implicit filesystem discovery, private-module registry import, and component/topology confusion (`PRS-003`).
- [x] 3.4 Implement the pure domain context/request/result/capability protocols needed by NodeBuildDependencies, then implement `domain/node_spec.py` NodeSpec/PolicyRef/NodeBuildDependencies plus explicit-package graph registry validation without adding research topology; make discovery read only package-root `NODE_SPEC`, constrain factories to reduced dependencies, keep the dependency DAG acyclic, and make the manifest-backed node-package shape check green (`PRS-003`, `PRS-004`).

## 4. Structured Configuration Materialization

- [x] 4.1 Add committed DeerFlow/extensions fragments plus red tests for effective AppConfig/extensions target resolution under root `.env`, root/backend/explicit-path conflicts, canonical effective skills root, configurable `DEER_FLOW_HOME`, blocking runtime-config versus nonblocking entry-status check axes, fresh merge, duplicate run, reordered input, YAML comment/anchor/quote preservation, JSON unknown-key/key-order and detected indentation/newline preservation, check/dry-run, restricted backup, redacted diff, and rollback manifest (`DEC-002`).
- [x] 4.2 Add red collision/concurrent-change/rollback fixtures for same-name foreign tool/group ownership, malformed YAML/JSON, content-signature drift, exact-hash whole-file restore, later operator edits, ambiguous owned-entry changes, and detected-online mutation refusal with online check/dry-run allowed (`DEC-002`).
- [x] 4.3 Implement `agent/scripts/configure.py` against the effective Gateway config/extensions targets and canonical repo skills root, with round-trip YAML and structured JSON semantic-key merge, project lock, explicit stopped-Gateway precondition, configured/known-local health plus project-owned process/container guards, atomic compare-before-replace, permission-restricted backup, before/after manifest, `--check`, `--dry-run`, hash-guarded `--rollback`, and secret/path redaction; refuse inert fallback targets and do not claim local detectors prove absence of remote writers; annotate `@impl DEC-002` (`DEC-002`).
- [x] 4.4 Verify the second configure run is byte-stable, unrelated YAML text and unknown JSON data survive, rollback never overwrites later edits, and `database`, `checkpointer`, `sandbox`, `agents_api`, MCP, ACP, subagent, and unrelated tool settings never change (`DEC-002`).

## 5. Public Skill And Per-User Agent

- [x] 5.1 Add red tests for committed-to-materialized public skill parity, enabled-state behavior, thin entry content, same-name drift, and rejection of `skills/custom/deep-research-controller/` (`DEC-003`).
- [x] 5.2 Implement the committed public skill and its idempotent materialization under `skills/public/deep-research-controller/`, with `@impl DEC-003` at the materializer owner (`DEC-003`).
- [x] 5.3 Add red provisioning tests for non-production `DEER_FLOW_AUTH_DISABLED=1` default, production refusal, rejection of offline `--user-id`, authenticated current-user API request/response shape, offline rollback preserving API-created Agent state, disabled `agents_api` diagnosis without auto-enable, absent dedicated Agent as a nonblocking entry status, pre-existing foreign/legacy Agent, invalid name, and zero writes to the shared legacy Agent root (`DEC-004`).
- [x] 5.4 Implement the dedicated Agent template, no-auth `default` filesystem materialization, and exact authenticated current-user `POST /api/agents` request documentation/fixture, including public skill/tool-group references and fail-closed ownership checks; annotate `@impl DEC-004` (`DEC-004`).
- [x] 5.5 Add a zero-API Replay model contract with a fake `deep_research` tool proving the public skill/SOUL routes a research request, surfaces typed unavailability, and does not claim lifecycle/HITL availability, exclusive tools, or phase prompts; defer real reflection availability to group 12 (`DEC-003`, `DEC-004`).

## 6. Startup Snapshot And Source Assembly

- [x] 6.1 Add a red effective-provider/startup-snapshot matrix covering direct `AppConfig` default memory versus raw-file database omission applying the current SQLite defaults, explicit unified memory/file-SQLite/Postgres, legacy memory/file-SQLite/in-memory-SQLite/Postgres, missing Postgres URLs, every conflicting legacy-plus-database pair, missing/empty/`01`/zero/negative/malformed/greater worker inputs normalized against `${GATEWAY_WORKERS:-1}`, secret-free deterministic canonical hashing, strict `v1:<64 lowercase hex>` parsing, missing/malformed/unknown-version fingerprints, and post-start drift; assert provider classification matches actual `make_checkpointer(app_config)` selection and durability never exceeds the resolved connection form (`DEC-001`, `RUI-004`, `RUI-005`).
- [x] 6.2 Implement `runtime/startup_snapshot.py` plus the single effective-provider/durability classifier in `runtime/checkpoint.py`, with legacy `checkpointer` always preceding `database`; expose canonical fingerprint capture/verification for launchers, GraphHost, RuntimeAdapter, and doctor, and annotate the durability owner with `@impl RUI-005` (`DEC-001`, `RUI-004`, `RUI-005`).
- [x] 6.3 Add red preparation-core tests that pin the current upstream exact backend sync/detected-extras/frontend-install tokens and inline `PYTHONPATH=.`, prove root `.env`, `DEER_FLOW_PROJECT_ROOT`/`DEER_FLOW_HOME`, and Gateway `backend/` cwd parity, refuse missing/invalid/older/newer config versions and AppConfig/config-upgrade target disagreement before shared backend-environment mutation or fingerprinting, preserve sync-before-editable-install, verify compatible harness/module origin, and emit a deterministic secret-free candidate fingerprint with relative SQLite paths resolved exactly as the Gateway resolves them; use a stopped-environment fixture and do not test service orchestration here (`DEC-001`).
- [x] 6.4 Implement `agent/scripts/prepare.py` as the agent-owned no-orchestration preparation core: run upstream-equivalent backend sync plus detected extras and frontend install only after its caller establishes quiescence, verify the harness, install `agent/` editable into the backend environment with `--no-deps`, compute the canonical versioned startup candidate, and expose a structured machine-readable field that wrappers assign directly without shell evaluation; pin current upstream install tokens (`DEC-001`).
- [x] 6.5 Add red rendered-Compose tests for base-first/override-second ordering, `<repo>/agent/src` resolution, read-only `/app/agent/src`, Gateway-only `PYTHONPATH=/app/agent/src:.`, container-effective startup-candidate export in the Gateway prelude, no sandbox source mount, a negative fixture proving reversed file order violates the rendered source-origin contract, and token-level upstream Gateway-command parity; defer the pre-uvicorn doctor gate and operator-facing diagnosis to group 13 (`DEC-001`).
- [x] 6.6 Implement `agent/docker/docker-compose.deep-research.yaml` and its documented invocation without modifying upstream compose/Dockerfiles; annotate the loading owner with `@impl DEC-001` (`DEC-001`).

## 7. Trusted Runtime Adapter

- [x] 7.1 Define red tests separating RuntimeAdapter's explicit server-injected user/thread/run/AppConfig, thread-data host/virtual validation, supported progress writer, and runtime-only TrustedRuntimeEnvelope from `runtime/projection.py` fixtures that require a registered-handler-owned validated research scope before producing pure GraphContextView/NodeExecutionCapabilities and minimal NodeAgentContext (`RUI-002`).
- [x] 7.2 Add red fail-closed cases for missing explicit `runtime.context.user_id` despite generic default fallback, missing thread/run/AppConfig/startup fingerprint, changed startup-only sandbox fingerprint, inconsistent workspace/uploads/outputs, traversal/symlink escape, stale/mismatched sandbox, initializer failure, cross-user/thread access, and real Gateway override of spoofed body-context/body-config user ids in authenticated, auth-disabled, and trusted-internal owner modes (`RUI-002`).
- [x] 7.3 Add a fresh-thread test proving the public async sandbox initializer obtains the existing parent sandbox once and that a pre-initialized parent sandbox is reused without another lifecycle (`RUI-002`, `NOA-001`).
- [x] 7.4 Implement runtime-owned TrustedRuntimeEnvelope in `runtime_adapter.py` and the separately owned `runtime/projection.py` projection onto group-3 pure domain contracts. Require a registered handler's validated research-scope id before research-root/NodeAgent projection; keep strict identity/path/startup-snapshot validation, blocking host-path work off the event loop, async lazy parent-sandbox initialization only after fingerprint match, and a supported stream-writer emitter without serializing runtime handles or reading private Gateway fields; annotate `@impl RUI-002` (`RUI-002`).
- [x] 7.5 Prove domain/graph/agents do not import runtime, and raw AppConfig, identity, host paths, sandbox internals, and checkpoint keys never enter research state/events/prompts/tool output while model-facing data contains only canonical virtual roots and opaque refs (`RUI-002`).

## 8. Namespace And GraphHost

- [x] 8.1 Add red namespace tests for deterministic same-scope identity, different user/thread/probe separation, domain/version separation, delimiter ambiguity, malformed probe ids, caller-supplied digest refusal, outer lead-agent isolation, unsupported schema version, and same-key concurrent mutation (`RUI-003`).
- [x] 8.2 Implement versioned length-prefixed SHA-256 namespace derivation, distinct infrastructure-probe graph namespace constants, schema-version refusal, and fixed-size cancellation-safe process-local lock striping; annotate `@impl RUI-003` (`RUI-003`).
- [x] 8.3 Add red GraphHost tests with a test-only handler/builder fixture for generic action-handler registration, request-independent recipe-only caching, fresh per-action NodeBuildDependencies/capability binding across different user/thread sentinels, unregistered-action refusal, startup-fingerprint drift refusal before provider entry, compile-inside-provider, per-action SQL context enter/exit, effective memory-saver reuse, exception/cancellation cleanup, same-key serialization, and different-key concurrency (`RUI-004`).
- [x] 8.4 Implement only the generic GraphHost action registry, fingerprint gate, provider wrapper, builder cache, and process-local memory saver without `InfraProbeState`, a production probe handler, named research lifecycle methods, or a Gateway lifespan assumption; annotate `@impl RUI-004` (`RUI-004`).
- [x] 8.5 Add test-fixture memory/file-SQLite/in-memory-SQLite/Postgres graphs that distinguish same-process behavior from provider-reopen/restart durability and reject unsupported, invalid-connection, or precedence-inconsistent durability claims (`RUI-005`).
- [x] 8.6 Make the fixture-based real SQLite provider-reopen/subprocess-restart tests green; add an isolated committed Postgres test Compose profile plus `agent/Makefile test-postgres`, and require its fixture-based provider-reopen and subprocess-restart run before 00 completion (`RUI-005`).

## 9. Runtime-Owned Node-Agent Bridge And Factory

- [x] 9.1 Add red bridge/factory tests for pure NodeAgentContext input, node-visible capability protocol, resolved model/tools, parent sandbox/thread capability reuse, ephemeral child state/runtime context, per-request separate-runnable construction with no cross-user/thread/attempt child cache, `checkpointer=None`, and refusal when parent isolation is missing (`NOA-001`).
- [x] 9.2 Add leakage tests proving raw AppConfig/identity/host/checkpoint/sandbox data appears only in the ephemeral child binding required by tools and never in node arguments, model requests, graph checkpoints, events, or results (`NOA-001`).
- [x] 9.3 Implement `runtime/node_agent_bridge.py` plus the full-takeover project wrapper around `create_deerflow_agent()` and immutable execution policy; nodes call only `NodeExecutionCapabilities.run_agent`, the agents layer never imports runtime, and neither layer creates another sandbox/thread lifecycle; annotate `@impl NOA-001` (`NOA-001`).
- [x] 9.4 Add red budget tests using Replay/Fake models for normal completion, deterministic no-network UTF-8-byte upper-bound admission over text messages/tool schemas, non-text refusal without an explicit conservative modality estimator, per-call model output cap, missing usage metadata, maximum model calls, total/per-response/parallel tool calls, per-tool-result cap, structured-result cap, wall-time timeout, child cancellation, and typed finish reasons (`NOA-002`).
- [x] 9.5 Implement and pin model-call/tool-call/parallelism/token/model-output/tool-result/structured-result/wall-time enforcement, terminal `usage_unavailable`, and the budget/cancellation middleware's relative ordering; defer the complete assembled-chain order assertion until group 11 and annotate the policy owner with `@impl NOA-002` (`NOA-002`).
- [x] 9.6 Add red inventory tests proving memory, title, uploads, subagents, skill evolution, todo, clarification, sandbox/thread lifecycle middleware, and independent persistence are absent (`NOA-001`, `NOA-006`).
- [x] 9.7 Implement the no-clarification contract and reject a fabricated `ask_clarification` call without creating a graph interrupt or human-input artifact; annotate `@impl NOA-006` (`NOA-006`).

## 10. Node-Agent Security Policy

- [x] 10.1 Add red allow/deny tool tests with typed ToolPolicySpec fixtures, unknown argument shapes, sync-only/non-cancellable tools, and built-in/MCP/ACP-like fake names to prove schema filtering plus pre-dispatch revalidation both apply (`NOA-003`).
- [x] 10.2 Add red path tests for canonical allowed reads/writes, traversal, provider-verifiable symlink containment, final-boundary revalidation, unverifiable-provider write refusal, package/host path, cross-attempt/sibling write, and phase/gate/ledger mutation (`NOA-003`).
- [x] 10.3 Implement deny-by-default ToolPolicySpec registry plus tool/path/attempt middleware, native-async/cancellation eligibility, default bash denial, final provider-containment requirement, off-event-loop blocking path checks, and immutable decisions before execution; annotate `@impl NOA-003` (`NOA-003`).
- [x] 10.4 Add red benign and adversarial external-source Replay fixtures covering ignore-rules, forbidden tool, phase advance, fake gate pass, and ledger mutation instructions (`NOA-004`).
- [x] 10.5 Implement package-resource prompt loading plus separate delimited untrusted-data/artifact projection, and prove system/policy prompts are byte-stable across source fixtures; annotate `@impl NOA-004` (`NOA-004`).

## 11. Structured Results, Events, And Cancellation

- [x] 11.1 Add red structured-output tests for valid result, malformed/missing fields, unknown artifact refs, oversized/redaction-sensitive errors, partial model output, and bounded progress detail (`NOA-005`).
- [x] 11.2 Implement result validation/normalization and stable redacted progress events through the supported stream writer with a deterministic test collector fallback; annotate `@impl NOA-005` (`NOA-005`).
- [x] 11.3 Add a nested fake-agent cancellation integration test that asserts `CancelledError` is not converted to success, queued/in-flight child tasks are cancelled and awaited, same-key locks are released, and provider contexts close (`NOA-005`, `RUI-004`).
- [x] 11.4 Make cancellation and cleanup tests green without background task leakage, private Gateway state, or independent node-agent checkpoints, then pin the complete assembled middleware type order after the group-9 budget and group-10 policy layers exist (`NOA-001`, `NOA-002`, `NOA-003`, `NOA-005`, `RUI-004`).

## 12. Reflected Infrastructure Probe

- [x] 12.1 Add red reflected-tool input tests for valid `infra_probe`, server-generated bounded URL-safe probe ids with at least 128 bits of CSPRNG entropy and no scope-derived text, optional validated caller-supplied opaque ids, dispatch-level typed `action_unavailable` for `start|resume|status|cancel` and unknown actions before adapter/sandbox access without echoing the rejected action, and schema-level `extra="forbid"` rejection for authority/unknown fields whose normalized errors expose field/code but never Pydantic input values, rejected payloads, host identities, or injected secret markers (`RUI-001`, `RUI-002`).
- [x] 12.2 Implement thin `tool.py` schema -> registry-presence -> RuntimeAdapter -> GraphHost dispatch plus the separately namespaced, versioned `InfraProbeState`, one-node probe builder, and production invoke/inspect handler registration; keep research business logic/state absent and annotate `@impl RUI-001` (`RUI-001`).
- [x] 12.3 Prove first invocation initializes/validates the parent sandbox and writes/reads a checkpoint without calling research projection, creating a research workspace, or invoking a node agent; same-id reinvocation observes the previous marker and advances the visit count without claiming resume; same-id concurrent calls serialize; cross-scope reuse remains isolated; results expose only opaque probe/marker/provider/durability data without user/thread/host/internal checkpoint values; and failure before adaptation produces no checkpoint mutation (`RUI-001`, `RUI-002`, `RUI-003`).

## 13. Readiness Diagnostics And Final Launcher Wiring

> Deferred: 13.4 and 13.5 (project-owned `serve.sh` launcher, `agent/Makefile`
> dev/prod/daemon targets, in-container doctor gate, live launch smoke) are
> intentionally deferred to deployment-environment setup — production is not
> provisioned yet, so building a launcher for it would overload change 00. The
> runtime substrate (13.1–13.3 diagnostics) is complete. Tracking:
> `_backlog/todos/deferred_deep-research-00-launcher-and-docker.md` and
> `_backlog/todos/deferred_deep-research-00-postgres-profile.md`.

- [x] 13.1 Add red doctor fixtures for explicit prelaunch-candidate versus in-process fingerprint modes, missing/mismatched fingerprint and missing-mode refusal, missing/invalid/older/newer config version, valid/missing/wrong module origin, incompatible harness, reflection/name drift, rendered Compose reversed-order/source-origin mismatch, root/backend/explicit config or extensions target conflicts, noncanonical effective skills root, config ownership conflict, missing skill/Agent, authenticated Agent unknown offline, disabled `agents_api`, legacy paths, runtime/entry tri-state classification, effective provider precedence, memory/file-SQLite/in-memory-SQLite/Postgres durability, missing Postgres URL, every worker value not normalizing to integer one, and host-source sandbox leakage (`DEC-005`).
- [x] 13.2 Add red credential fixtures for database URLs, environment references, API tokens, user ids, host paths, startup fingerprints/inputs, and checkpoint digests (`DEC-005`).
- [x] 13.3 Implement reusable package `runtime/diagnostics.py` plus thin `agent/scripts/doctor.py` with explicit prelaunch-candidate/in-process modes, mode-labelled boolean `runtime_ready`, deterministic entry precedence `known defect -> not_ready`, `unattributable-only -> unknown`, `all pass -> ready`, and `durability=same_process|restart_durable|unavailable` plus provider kind; never recompute a missing expected fingerprint silently, make blocking CLI exit status depend only on runtime readiness, use the shared classifier, correct next-build/restart classification, single-worker gate, and complete redaction; support the same diagnostic inside the Gateway container; annotate `@impl DEC-005` (`DEC-005`).
- [ ] 13.4 (DEFERRED — follow-up deployment change) Add red wrapper/Compose-command tests for direct stop delegation with no prework; dev/prod/daemon/restart application-state read-only preflight from the locked agent operations environment; missing/invalid/older/newer version and config-target-disagreement refusal before stop/shared-backend mutation; required upstream stop before backend sync/install; blocking configure-runtime failure before stop; post-stop preparation/candidate/doctor failures that remain stopped without fake rollback; candidate computation/export before prelaunch doctor; configure/doctor entry-only warning without launch refusal; editable-install origin; restart reduction to stop plus start; `UV_NO_SYNC=1`; exact upstream `--skip-install` start-argument forwarding; and Docker candidate export -> in-container prelaunch doctor -> unchanged uvicorn ordering where only runtime failure stops startup (`DEC-001`, `DEC-005`).
- [ ] 13.5 (DEFERRED — follow-up deployment change) Implement the project-owned local wrapper and `agent/Makefile` configure, doctor, dev/prod/daemon/restart/stop, and Docker checks, and finalize the group-6 Compose Gateway prelude with in-container prelaunch doctor before uvicorn. Local start/restart performs read-only preflight, blocks only on configure's runtime-config axis, delegates and verifies upstream stop, invokes the preparation core, exports its candidate fingerprint, passes prelaunch doctor, and only then delegates upstream start with no second sync; stop delegates directly, and both launch paths gate only on `runtime_ready` while entry not-ready/unknown remains visible (`DEC-001`, `DEC-005`).

## 14. Environment And Documentation Verification

> Deferred: 14.3 (local dev/prod smoke) and 14.4 (Docker/Postgres smoke) need a
> provisioned deployment environment and move to the follow-up deployment change
> (see `_backlog/todos/deferred_deep-research-00-*`). 14.1/14.2 (zero-API + config
> materialization) are covered by the green agent test suite; 14.5 is doc sync.

- [x] 14.1 Run the complete agent-owned unit/contract/graph suite with zero LLM API calls and pass uv lock check, ruff format/lint, plus agent-scoped blocking-IO runtime checks (`PRS-001`..`PRS-004`, `DEC-001`..`DEC-005`, `RUI-001`..`RUI-005`, `NOA-001`..`NOA-006`).
- [x] 14.2 From clean isolated config/DEER_FLOW_HOME fixtures, run configure dry-run/apply/check twice, exact-hash rollback, semantic rollback with unrelated edits, conflicting rollback, and doctor, verifying YAML/JSON preservation, public skill/no-auth Agent placement, no legacy writes, redaction, and exact reflection resolution (`DEC-002`..`DEC-005`).
- [ ] 14.3 (DEFERRED — follow-up deployment change) Run local dev and local production smoke paths against isolated runtime config, invoke reflected `infra_probe`, restart Gateway on SQLite, and verify same-id reinvocation recovers the previous checkpoint marker without calling it resume (`DEC-001`, `RUI-001`, `RUI-005`).
- [ ] 14.4 (DEFERRED — follow-up deployment change) Run the base-first Docker override smoke path and required Postgres persistence profile against isolated runtime data, then verify package origin, one-worker readiness, source/sandbox mount separation, per-action provider closure, and subprocess restart recovery (`DEC-001`, `DEC-005`, `RUI-004`, `RUI-005`).
- [x] 14.5 Synchronize `agent/README.md`, the generated and human-authored portions of `agent/AGENTS.md`, root `README.md`, root `AGENTS.md` module map, `openspec/governance/architecture-policy.md`, `project-structure.toml`, the governance README, the short OpenSpec bootstrap pointer/archive command, and the 00/master plans with only verified final commands and contracts; do not copy the exact structure enumeration into policy or OpenSpec config (`PRS-001`, `PRS-004`, `DEC-001`, `DEC-005`).

## 15. Final Traceability And Archive Gates

- [x] 15.1 Verify every registered requirement has one owning delta header, task coverage, and an owning `@impl` annotation, with no undocumented implementation surface (`PRS-001`..`PRS-004`, `DEC-001`..`DEC-005`, `RUI-001`..`RUI-005`, `NOA-001`..`NOA-006`).
- [x] 15.2 Run `openspec validate establish-deep-research-runtime-infrastructure --type change --strict` and resolve every schema/scenario error (`PRS-001`, `PRS-004`, `DEC-001`, `RUI-001`, `NOA-001`).
- [x] 15.3 Run `python3 openspec/governance/check_project_reqs.py` and require zero duplicate, unregistered, orphan, and reused-retired IDs (`PRS-001`, `PRS-004`, `DEC-001`, `RUI-001`, `NOA-001`).
- [x] 15.4 Run `python3 openspec/governance/check_project_specs.py` and require zero delta-header-in-main, missing-purpose, missing-requirements, and missing-requirement-header violations (`PRS-001`, `PRS-004`, `DEC-001`, `RUI-001`, `NOA-001`).
- [x] 15.5 Run `python3 openspec/governance/check_project_architecture.py` and require a valid structure registry, the unique pending owning delta before first archive or active main spec afterward, exact controlled-guide rendering, conforming current paths/imports/node packages, and no unowned structural drift (`PRS-001`..`PRS-004`).
