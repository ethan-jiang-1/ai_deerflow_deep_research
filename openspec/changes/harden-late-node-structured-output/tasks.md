# Late-Node Structured Output Hardening Tasks

> Current progress: **0/10 complete**. Apply has not started. Update this summary and append the exact red/green command and observed result beneath each task immediately when that task changes state.
>
> Coordination boundary: this change owns production fixes and their focused deterministic evidence. `rebalance-deep-research-test-assets` remains authoritative for the six-case aggregate, nightly scheduling, and final test-asset completion. Never edit `backend/` or `frontend/`; preserve all pre-existing worktree changes from that active change.

## 1. Baseline And Canonical Gap Contract

- [ ] 1.1 Before any implementation write, record the apply base, complete initial worktree status, the two retained failing evidence-v1 report roots, and Git hashes for `backend`, `frontend`, release/public-entry surfaces, plus the current `agent/src` tree; distinguish all pre-existing `rebalance-deep-research-test-assets` changes from this change and record the exact focused baseline failures without rerunning full-real. @impl WSN-001, TEL-002
- [ ] 1.2 Red-before-green: add minimal domain/provider-shape and real Wave2 node/store/gate/topology tests proving an explicit gap `search_required=true` currently fails, an omitted field remains false, prompts describe finding and gap fields unambiguously, canonical persistence yields a typed searchable-gap-id preview, the real gate routes `evidence_needed` and writes/clears only gate-owned ids, repeated unresolved gaps route `exhausted` to blocked, fabricated prior state cannot route, and targeted consumes that projection; record the failing selectors before production edits. @impl WSN-001, WSN-002, WSN-003, TEL-001, REG-001
- [ ] 1.3 Add defaulted `GapRecord.search_required`; align Wave2 prompts; add the reserved typed gate preview, deterministic real gate rule, gate-owned `unresolved_gaps` projection/clearing, targeted id consumption, and exactly one normalized/builder edge `wave2_synthesis --exhausted--> blocked/END`; replace the focused targeted seed's detached/manual gap flag with canonical synthesis -> typed preview -> real gate-adapter authority; pass task 1.2 plus existing Wave2 provider-shape, semantic-floor, gate, store, seed, state-ownership, topology, and gap-router suites without changing synthesis/state schema versions or storing complete gap bodies in checkpoint state. @impl WSN-001, WSN-002, WSN-003, TEL-001, REG-001

## 2. Targeted Worker Repair

- [ ] 2.1 Red-before-green: extend the real targeted worker resolver/store test harness with scripted initial and repair results, then prove the missing behavior for a valid first response, prose/schema-invalid/wrong-gap first response followed by valid repair, invalid/non-success/wrong-gap repair, exact request ordering, the initial `1..2` tool window, zero repair tools, and zero source/result/ledger authority on every failed path. @impl TEL-002
- [ ] 2.2 Set the targeted initial request to `minimum_tool_calls=1` and `tool_call_limit=2`; add a bounded repair prompt carrying only assigned gap identity, stable validation metadata, and the truncated untrusted draft; invoke exactly one separate `tools_enabled=false` repair only for initial structured-output or assigned-gap identity validation failure, then validate through the same parser and gap-identity boundary. @impl TEL-002
- [ ] 2.3 Make the full task 2.1 matrix green through the real worker/resolver/artifact-validator/ledger path, directly test request and draft size bounds plus no recursive repair, and re-run adjacent Wave0/Wave1 worker, bridge budget/tool-policy, partial-worker, prompt-injection, and malformed-output workflow cases to rule out shared-runtime regressions. @impl TEL-002

## 3. Integration And Live Closure

- [ ] 3.1 Run the focused late-node graph/integration/domain/provider-shape/gate/state-ownership/topology suites together; update the focused Wave2 live assertion to require `evidence_needed` iff the current validated searchable-gap preview is non-empty and `pass` otherwise; update only existing test-evidence claims or documentation whose exact selector/semantics changed; and pass the asset checker plus requirement coverage without inventing a targeted-loop/full-pipeline claim or persisted trace asset. @impl WSN-001, WSN-002, WSN-003, TEL-001, TEL-002, REG-001
- [ ] 3.2 After deterministic gates pass, run the complete six-case live selector once with fresh identities and a fresh evidence-v1 report root; require six schema-valid passing case reports and a redacted aggregate within the existing per-case and 900-second declared deadlines, record model/tool/token/cost-availability/retry/invariant/metric evidence, and do not coerce provider output or rerun selectively to hide a failure. @impl WSN-001, TEL-002
- [ ] 3.3 If and only if task 3.2 is green with the five-minute job margin preserved, update `rebalance-deep-research-test-assets/tasks.md` tasks 6.7 and 6.8, restore its reviewed six-case nightly schedule without increasing the 20-minute timeout, and record the production-change provenance there; otherwise leave scheduling manual-only and record the honest blocker in both changes. @impl TEL-001, TEL-002

## 4. Final Gates And Handoff

- [ ] 4.1 Run strict validation for both active changes; all root requirement/spec/architecture/coverage checks; lock, Ruff, asset governance, deterministic fast/integration/workflow/full, viability, durability, blocking-I/O, and focused invalid/authority smoke suites; run read-only config/doctor and the zero-API scripted lifecycle/infra probe appropriate to an unchanged entry surface; compare `backend`, `frontend`, release runner/workflow/preflight, public entry, and unrelated `agent/src` paths with task 1.1 hashes; assess the reviewed full-real trigger with the adjacent HITL2 content-projection finding explicitly in scope, without running release E2E unless the trigger is actually met; then update both changes' authoritative progress. @impl WSN-001, WSN-002, WSN-003, TEL-001, TEL-002, REG-001
