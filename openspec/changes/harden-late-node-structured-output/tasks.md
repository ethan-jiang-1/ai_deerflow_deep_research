# Late-Node Structured Output Hardening Tasks

> Current progress: **8/11 complete**. Both production remediation slices and their deterministic/governance gates are complete. Task 3.3 is the active one-shot fresh complete live lane; scheduling and final gates remain reopened, and nightly remains manual-only. Update this summary and append the exact red/green command and observed result beneath each task immediately when that task changes state.
>
> Coordination boundary: this change owns production fixes and their focused deterministic evidence. `rebalance-deep-research-test-assets` remains authoritative for the six-case aggregate, nightly scheduling, and final test-asset completion. Never edit `backend/` or `frontend/`; preserve all pre-existing worktree changes from that active change.

## 1. Baseline And Canonical Gap Contract

- [x] 1.1 Before any implementation write, record the apply base, complete initial worktree status, the two retained failing evidence-v1 report roots, and Git hashes for `backend`, `frontend`, release/public-entry surfaces, plus the current `agent/src` tree; distinguish all pre-existing `rebalance-deep-research-test-assets` changes from this change and record the exact focused baseline failures without rerunning full-real. @impl WSN-001, TEL-002
  - Apply base and ownership: `ee0ebc6fb9b6c2a9128b558d9994ca7810788101` (`feat(agent): rebalance deep research test assets and hardening`). `git status --porcelain=v1 -uall` was empty: all prior `rebalance-deep-research-test-assets` implementation plus this change's reviewed planning artifacts were already committed, so this change starts from a clean worktree with no pre-existing uncommitted user files to attribute or preserve.
  - Protected Git objects: `agent/src=32f747773f31de7d767e90ad52015e06011316b7`, `backend=e033d4751b5f9a517b9fb249d25eceb8092ef1be`, and `frontend=b5e77197a08602fe194ded9d8045468d7ec9de3a`. Release/public-entry blobs were `tests/scenarios/release.py=32c77435edcf35db14546c6105c85ba64485423a`, `tests/e2e/test_release_acceptance.py=51df58d866b899bc275dc3499818b288f6e73d87`, `.github/workflows/agent-release-e2e.yml=aea955cc0a5815ed37628c2ea6cbe2a870e3cdc6`, `scripts/release_preflight.py=b45e88e30f15433d77bafca6c81bc1af3b66eb05`, public tool `160380cfaa9c787da10df2d7c5f5978d30b81450`, committed skill `d9114d905ce4c0e2e13037280d2880cc9625a247`, and Agent SOUL `8aeccdf8dba9096c5464a347abb306ff71fb3882`.
  - Retained evidence roots: `agent/.reports/live-evidence-v1-20260718` contains the six schema-valid case reports with SHA-256 values `71fde4dc...99af5` (HITL1-to-topic-planning), `b22e88af...3831` (targeted), `42c93874...9fde` (Wave0), `b2cf4b3d...29323a` (Wave1), `7d664184...f129b` (start-to-HITL1), and `d3bb8f8a...3200b` (Wave2). `agent/.reports/live-evidence-v1-20260718-targeted-repro` contains one targeted report `82d76789...0010`. Reports remain gitignored operational evidence and were read only.
  - Exact baseline failures: the complete selector finished in `207.16s` with four case passes, two typed failures, and two strict-preflight passes. Wave2 used two model calls and returned the same JSON shape twice; normalization supported the missing finding id, but both outputs failed because `gaps.0.search_required` was extra-forbidden. Targeted initially spent three tool calls with no final content; its bounded two-tool reproduction used two tool-only turns then returned a `230`-character prose answer in `14.0115s`, failed structured validation, and published no targeted authority. No live or full-real command was rerun for this baseline.
- [x] 1.2 Red-before-green: add minimal domain/provider-shape and real Wave2 node/store/gate/topology tests proving an explicit gap `search_required=true` currently fails, an omitted field remains false, prompts describe finding and gap fields unambiguously, canonical persistence yields a typed searchable-gap-id preview, the real gate routes `evidence_needed` and writes/clears only gate-owned ids, repeated unresolved gaps route `exhausted` to blocked, fabricated prior state cannot route, and targeted consumes that projection; record the failing selectors before production edits. @impl WSN-001, WSN-002, WSN-003, TEL-001, REG-001
  - Red assets: `tests/graph/test_wave2_synthesis_real.py` now covers default-false/explicit-true `GapRecord`, prompt field separation, canonical artifact persistence, typed preview contents/bounds, and real-node preview publication. `test_gate_integration.py` covers current-preview pass/repair/blocked behavior, gate-owned id projection/clearing, no reserved-key persistence, and missing-preview fail-closed behavior. `test_topology_and_implementation.py` requires the exact Wave2 exhausted terminal edge; `test_targeted_evidence_real.py` requires canonical gate-owned id input and rejects malformed/duplicate projections.
  - Exact red command: `cd agent && uv run --extra operations pytest -q tests/graph/test_wave2_synthesis_real.py tests/graph/test_gate_integration.py tests/graph/test_topology_and_implementation.py tests/graph/test_targeted_evidence_real.py`. Collection failed in the two Wave2/gate modules with `ImportError: cannot import name 'WAVE2_GATE_PREVIEW_KEY'`, also naming missing `Wave2GatePreview`; this is the intended absent production interface. No production file had changed when the red result was captured. The active environment emitted the known non-blocking `backend/.venv` versus `agent/.venv` warning.
- [x] 1.3 Add defaulted `GapRecord.search_required`; align Wave2 prompts; add the reserved typed gate preview, deterministic real gate rule, gate-owned `unresolved_gaps` projection/clearing, targeted id consumption, and exactly one normalized/builder edge `wave2_synthesis --exhausted--> blocked/END`; replace the focused targeted seed's detached/manual gap flag with canonical synthesis -> typed preview -> real gate-adapter authority; pass task 1.2 plus existing Wave2 provider-shape, semantic-floor, gate, store, seed, state-ownership, topology, and gap-router suites without changing synthesis/state schema versions or storing complete gap bodies in checkpoint state. @impl WSN-001, WSN-002, WSN-003, TEL-001, REG-001
  - Canonical contract: schema-version-1 `GapRecord` now owns `search_required: bool = False`; old gap payloads remain valid and canonical reserialization adds false. `Wave2GatePreview` accepts at most 32 unique canonical gap ids under one reserved private key, and Wave2 initial/repair prompts explicitly distinguish `finding.search_required` from `gap.search_required`.
  - Authority and routing: the real Wave2 node persists the validated synthesis artifact then returns the typed preview. The graph wrapper pops/type-checks it, overlays it only for the current gate evaluation, and never checkpoints the reserved key. A pure `MISSING_EVIDENCE` gate rule produces `evidence_needed`; the adapter projects the complete validated id tuple through `WriterRole.GATE` to `unresolved_gaps`, clears stale ids on pass, and blocks on exhausted budget. The normalized topology and builder add only `wave2_synthesis --exhausted--> blocked/END`.
  - Targeted/seed migration: real targeted evidence consumes only canonical gate-owned ids; malformed/duplicate projections fail closed. The focused seed writes a canonical searchable `GapRecord`, builds the real preview, evaluates the real Wave2 gate adapter, and feeds only `unresolved_gaps` into targeted execution. Complete gap bodies remain sandbox/report authority, not checkpoint control state. Existing provider-shape expectations now include the canonical default false rather than production compatibility coercion.
  - Validation: the initial focused green passed `51`; Wave2/gate/topology/targeted/seed/live expanded to `95`; provider-shape, state/store, zero-tool and scripted worker compatibility first exposed five stale default-field fixtures plus one old `synthesis_gaps` workflow input, then passed `171` after their canonical migration. Final full graph plus focused late-node/state/store/live/bridge suite passed `258`. Ruff check and format check (`332` files), strict validation for both active changes, and `git diff --check` passed. No live or release API run occurred.

## 2. Targeted Worker Repair

- [x] 2.1 Red-before-green: extend the real targeted worker resolver/store test harness with scripted initial and repair results, then prove the missing behavior for a valid first response, prose/schema-invalid/wrong-gap first response followed by valid repair, invalid/non-success/wrong-gap repair, exact request ordering, the initial `1..2` tool window, zero repair tools, and zero source/result/ledger authority on every failed path. @impl TEL-002
  - Harness: the real targeted node now runs against a scripted `NodeExecutionResult` queue through `RuntimeWorkUnitDependencyResolver`, the real attempt artifact writer, validation registry, and `WorkUnitStore`. Observable assertions distinguish allowed controller work-spec files from forbidden partial cache/result/ledger authority.
  - Matrix: valid-first must use exactly one request with `minimum_tool_calls=1` and `tool_call_limit=2`; prose, malformed JSON, and wrong-gap initial outputs must use one additional zero-tool repair and publish one validated record; invalid, non-success, and wrong-gap repairs must stop after two requests with empty accepted refs, empty ledger, and no cache/result files.
  - Exact red command: `cd agent && uv run --extra operations pytest -q tests/graph/test_targeted_evidence_real.py` produced `8 passed, 7 failed`. Valid-first observed the old `minimum_tool_calls=0/tool_call_limit=None`; all six initial-invalid/repair-failure cases observed only one request instead of two. These failures precisely establish the missing production request window and repair path; production repair code had not changed when captured.
- [x] 2.2 Set the targeted initial request to `minimum_tool_calls=1` and `tool_call_limit=2`; add a bounded repair prompt carrying only assigned gap identity, stable validation metadata, and the truncated untrusted draft; invoke exactly one separate `tools_enabled=false` repair only for initial structured-output or assigned-gap identity validation failure, then validate through the same parser and gap-identity boundary. @impl TEL-002
  - Initial request: `build_targeted_worker_prompt` now requires at least one tool call and caps the invocation at two, leaving a model turn for final structured output without changing the shared Wave0 production policy.
  - Repair interface: one separate `NodeExecutionRequest` is built only after a successful initial agent result fails JSON/schema/assigned-gap validation. It contains the assigned gap, a stable bounded validation code, and at most `8192` characters of draft inside `<untrusted-source-data>`; it has `tools_enabled=false`, no minimum, and no tool-call limit. A non-success repair or a second parse/identity failure becomes a typed failed work attempt with no recursion.
  - Publication ordering: source cache, targeted result document, candidate construction, and ledger submit remain after the final selected output passes both schema and assigned-gap validation. Direct tests prove invalid/non-success/wrong-gap repair leaves accepted refs and ledger empty and creates neither cache nor result artifacts.
  - Validation: the task 2.1 matrix became `15 passed`; direct draft/error truncation, assigned-gap expected schema, zero-tool request fields, and Pydantic-error normalization extended it to `17 passed`. No provider or release run occurred.
- [x] 2.3 Make the full task 2.1 matrix green through the real worker/resolver/artifact-validator/ledger path, directly test request and draft size bounds plus no recursive repair, and re-run adjacent Wave0/Wave1 worker, bridge budget/tool-policy, partial-worker, prompt-injection, and malformed-output workflow cases to rule out shared-runtime regressions. @impl TEL-002
  - Focused repair matrix passed `17`, including valid-first, three initial-invalid/repair-success shapes, three repair-failure shapes, exact request ordering, stable bounded metadata, one-call/no-recursion behavior, and cache/result/ledger non-publication.
  - Shared-runtime regression command covered the full targeted file plus Wave0/Wave1 work units, scripted worker bridge, adversarial real-worker path, zero-tool node conformance, node-agent bridge, budget middleware, tool policy, adversarial eval, replay registry, and scenario observation contracts; all `124` tests passed. This includes partial-worker submit/gate outcomes, prompt injection/path denial, Wave1 malformed submit, Wave2 malformed structured repair, and bridge budget/tool-window behavior.
  - Governance: asset coverage passed with `90` central claims and `1556` deterministic tests (`fast=1454`, `integration=89`, `workflow=13`, `live=8`, `release=1`); requirement coverage passed. Ruff check and format check passed across `332` files after moving the async failure-artifact scan through `asyncio.to_thread`; strict OpenSpec and `git diff --check` passed. No central claim or persisted trace was added for ordinary focused correctness tests.

## 3. Integration And Live Closure

- [x] 3.1 Run the focused late-node graph/integration/domain/provider-shape/gate/state-ownership/topology suites together; update the focused Wave2 live assertion to require `evidence_needed` iff the current validated searchable-gap preview is non-empty and `pass` otherwise; update only existing test-evidence claims or documentation whose exact selector/semantics changed; and pass the asset checker plus requirement coverage without inventing a targeted-loop/full-pipeline claim or persisted trace asset. @impl WSN-001, WSN-002, WSN-003, TEL-001, TEL-002, REG-001
  - Focused semantics: Wave2 live now validates the exact current-preview relation (`searchable ids -> evidence_needed`, empty -> pass) and the complete gate-owned id projection. Targeted live seeds canonical synthesis, derives the typed preview, and evaluates the real Wave2 gate before invoking targeted; it still asserts no targeted phase gate and disclaims public entry, predecessor lifecycle, production recipe, and full pipeline.
  - Evidence scope: existing `live-wave2-focused-provider` and `live-targeted-focused-provider` claims remain honest runtime-integration/`LIVE_REAL_DEPENDENCIES` claims; no selector, seam, authenticity, scenario id, targeted-loop claim, persisted trace, or release claim changed. The topology snapshot was regenerated, `agent/AGENTS.md` records the one new Wave2 exhausted edge, and regression descent records deterministic remediation as awaiting fresh live confirmation.
  - Validation: domain, engine, graph, state/store/seed/live, zero-tool/worker bridge, topology snapshot, documentation, workflow, and asset contracts passed `533`. Asset governance passed with `90` claims and `1556` deterministic tests (`fast=1454`, `integration=89`, `workflow=13`, `live=8`, `release=1`). Requirement coverage, all four root governance checks, strict validation for both active changes, and `git diff --check` passed.
- [x] 3.2 Red-before-green: add focused Wave1 request-contract coverage requiring `minimum_tool_calls=1/tool_call_limit=2` while retaining its separate zero-tool repair, and Wave2 real-node/store coverage proving accepted evidence plus gaps-only initial output triggers one repair while a gaps-only repair publishes neither artifact nor gate preview; then make them green through only the Wave1 request builder and Wave2 semantic validator and rerun adjacent worker/bridge/provider-shape/authority suites. @impl WON-002, WSN-001
  - Second-run diagnosis: Wave1's initial request allowed `tool_call_limit=3` under a three-model-call live ceiling, so the provider could consume every turn with tools and never return a successful draft for the existing zero-tool repair. Wave2 semantic validation rejected only when both findings and gaps were empty, so accepted evidence plus one searchable gap could persist with zero findings.
  - Exact red command: `cd agent && uv run --extra operations pytest -q tests/integration/test_wave1_work_units.py tests/graph/test_wave2_synthesis_real.py` produced `3 failed, 16 passed`. The real Wave1 request observed `3 != 2`; a gaps-only Wave2 initial response made one call instead of repair; and a gaps-only repair did not raise or prevent publication. No production file for this slice had changed when the red was captured.
  - Green implementation: Wave1 retains `minimum_tool_calls=1` and its existing separate zero-tool repair while narrowing only the initial request's `tool_call_limit` from `3` to `2`. Wave2 now requires at least one finding whenever accepted refs are non-empty; gaps-only initial output enters the existing one zero-tool repair, and gaps-only repair raises `synthesis_findings_required` before artifact or preview publication.
  - Validation: the exact red selector passed `19`; adjacent Wave0/Wave1/targeted workers, shared bridge/budget, Wave2 provider-shape/store/gate, live contract/seed, and zero-tool suites passed `172`. Ruff and format passed across `332` files. Asset governance passed with `90` claims and `1558` deterministic tests (`fast=1456`, `integration=89`, `workflow=13`, `live=8`, `release=1`); requirement coverage, strict validation for both changes, all four root governance checks, documentation contracts, and `git diff --check` passed.
- [ ] 3.3 After deterministic gates pass, run the complete six-case live selector once with fresh identities and a fresh evidence-v1 report root; require six schema-valid passing case reports and a redacted aggregate within the existing per-case and 900-second declared deadlines, record model/tool/token/cost-availability/retry/invariant/metric evidence, and do not coerce provider output or rerun selectively to hide a failure. @impl WSN-001, TEL-002
  - One-shot attempt (`2026-07-18`, fresh root `.reports/live-evidence-v1-20260718-173821`): external strict preflight passed with `model=deepseek`, `web=tavily`; the complete selector collected exactly eight items and finished in `238.46s` with four case passes, two typed case failures, and both in-selector strict-preflight tests passing. All six per-case reports were preserved, and `scan_live_report_archive` accepted the complete exact case set as schema-valid, bounded, unique, and redacted. No case was rerun selectively and no provider output was coerced.
  - Passing cases: start-to-HITL1 `25.538s` (`600/955` input/output tokens, zero tools); HITL1-to-topic-planning `28.001s` (`621/949`, zero tools); one-topic Wave0 `100.712s` (`13575/4204`, six Tavily calls); one-gap targeted evidence `32.678s` (`3303/1485`, two Tavily calls). Every pass used one outer/workflow attempt, zero retries, four of four invariants, distinct present identities, unavailable cost, available token authority, and two honestly `insufficient_authority` focused metrics.
  - Failed cases: one-topic Wave1 used one outer/workflow attempt, zero retries, `19.963s`, `4456/485` tokens, and three Tavily calls, but all three model response shapes were tool-only with empty content, so it failed closed as `live_wave1_failed:AssertionError` with no accepted identity/authority. Wave2 used one outer/workflow attempt, zero retries, `27.266s`, `1460/1063` tokens, and zero tools; its single response normalized and schema-validated with zero findings plus one canonical searchable gap, then failed a downstream live assertion as `live_wave2_failed:AssertionError`. Both failure reports record zero of four case invariants and unavailable cost without raw provider content.
  - Result: declared case wall-time sum was `234.158s`, well below the `900s` bound and preserving the five-minute job margin, but the task remains unchecked because six passing reports are mandatory. Report SHA-256 values are `ab133c32...93b1`, `f33ba567...0040`, `dd1a8327...16be`, `dc465687...bfbe`, `cd3a4436...aee`, and `862570ef...4a6` in sorted case order. The retained red result is the evidence; another live run requires a separately reviewed remediation rather than a selective retry.
- [ ] 3.4 If and only if task 3.3 is green with the five-minute job margin preserved, update `rebalance-deep-research-test-assets/tasks.md` tasks 6.7 and 6.8, restore its reviewed six-case nightly schedule without increasing the 20-minute timeout, and record the production-change provenance there; otherwise leave scheduling manual-only and record the honest blocker in both changes. @impl TEL-001, TEL-002
  - The measured duration preserved the margin, but task 3.2 was red. Per the conditional rule, `.github/workflows/agent-live-evaluation.yml` remains `workflow_dispatch` only with its unchanged `timeout-minutes: 20`; no schedule was restored. The owning test-assets tasks 6.7/6.8 remain unchecked and now carry this production-change provenance and the exact fresh-run blocker.

## 4. Final Gates And Handoff

- [ ] 4.1 Run strict validation for both active changes; all root requirement/spec/architecture/coverage checks; lock, Ruff, asset governance, deterministic fast/integration/workflow/full, viability, durability, blocking-I/O, and focused invalid/authority smoke suites; run read-only config/doctor and the zero-API scripted lifecycle/infra probe appropriate to an unchanged entry surface; compare `backend`, `frontend`, release runner/workflow/preflight, public entry, and unrelated `agent/src` paths with task 1.1 hashes; assess the reviewed full-real trigger with the adjacent HITL2 content-projection finding explicitly in scope, without running release E2E unless the trigger is actually met; then update both changes' authoritative progress. @impl WSN-001, WSN-002, WSN-003, TEL-001, TEL-002, REG-001
  - Governance/static gates: strict validation passed for both active changes. Root requirement consistency passed with `128` registered ids and `160` occurrences; all `23` main specs, architecture, and requirement-to-test coverage passed. Lock, Ruff check, Ruff format check across `332` files, asset governance, requirement coverage, and `git diff --check` passed. Asset collection remained `90` central claims and `1556` deterministic tests partitioned as `fast=1454`, `integration=89`, `workflow=13`, with `live=8` and `release=1` outside the deterministic union.
  - Deterministic/runtime gates: fast passed `1454`; integration passed `85` with four explicitly unavailable Gateway-stack skips; workflow passed `13`; aggregate passed `1552` with the same four skips. Viability passed `5`; durability passed `9` with the explicitly deferred Postgres case skipped; blocking-I/O passed `9`; the focused invalid/report/redaction/release-attestation/evidence-policy/scenario-authority/gate/targeted smoke set passed `141`. Known Pydantic serializer warnings remained non-blocking.
  - Read-only environment proof: zero-API `demo-scripted` reached the full-fake completed fixture terminal, and focused infra/lifecycle/durability probes passed `44` with only deferred Postgres skipped. Root `make doctor` did not load `agent/.env` and therefore reported the unresolved `$TAVILY_API_KEY`; rerunning the same read-only doctor with `--env-file agent/.env` was Ready with only the existing web-capture/image-search/LocalSandbox warnings. `configure.py --check` made no writes and honestly returned `would_change=true/not_ready` for the existing unmaterialized public skill/Agent surfaces.
  - Protected boundaries: `backend`, `frontend`, release runner, release E2E, release workflow/preflight, public tool, committed skill, and Agent SOUL have empty diffs and exactly match task 1.1 objects. The only `agent/src` differences are the eleven reviewed production paths named by this change; no unrelated production path changed. Workflow timeout remains `20` minutes and manual-only.
  - Full-real decision: no release E2E was run. The release runner/public entry/control plane are unchanged, and both fresh discoveries are diagnosable below the public pipeline: Wave1 is bounded provider-only tool-selection/convergence behavior; Wave2 is a deterministic node semantic-floor gap requiring a newly reviewed red test and production rule. The adjacent HITL2 brief still reads nonexistent `synthesis_gaps`; gate-owned searchable ids are not an honest substitute for complete synthesis content, so a full-real run cannot close that design finding or be claimed as new Wave2-to-HITL2 proof. The explicit rerun trigger is therefore not met.
  - Post-record governance correction: the first regression-descent update used two invented disposition labels and a pending selector; its focused contract correctly failed the closed vocabulary. The log was corrected to use `deterministic-regression` only for the two collected selectors, retain Wave1 as `provider-only-live`, and keep the unreviewed zero-findings semantic-floor discovery in prose until a real red selector exists.
  - Selector correction: the next focused documentation run correctly rejected two mistyped deterministic node ids. Collection was inspected directly and the log now names exact collected selectors `test_real_synthesis_persists_searchable_gap_and_returns_typed_preview` and `test_targeted_invalid_first_response_repairs_once_without_tools[prose]`.
