## 1. Domain contracts: profile model, parser, and state fields

- [ ] 1.1 Add red deterministic tests in `agent/tests/domain/test_profile.py` for a frozen extra-forbid `ResearchProfile` with closed-enum dimensions (`ResearchDepth`, `TargetAudience`, `OutputFormat`, `CostTolerance`, `TimeBudget`), a `must_answer` tuple (1–8 items, each ≤ 256 chars), `scope_boundaries` (≤ 2,048 chars), `custom_notes` (≤ 1,024 chars), and `schema_version=1`; a `StructuredBrief` model for the LLM output with proposed dimensions and a human-readable summary; and a pure `parse_profile_response(text: str) -> ResearchProfile` function that extracts dimension values from free text and returns a partial profile with unset fields as `None`. Prove rejection of invalid enum values, oversized fields, empty must-answer, and malformed JSON. @impl HIN-001, HIN-003
- [ ] 1.2 Implement `agent/src/deerflow_deep_research/domain/profile.py` with the frozen `ResearchProfile`, `StructuredBrief`, `ResearchDepth`/`TargetAudience`/`OutputFormat`/`CostTolerance`/`TimeBudget` enums, and `parse_profile_response` until task 1.1 is green. @impl HIN-001, HIN-003
- [ ] 1.3 Add red tests in `agent/tests/domain/test_state.py` for new checkpoint fields: `profile_ref: ContentRef | None`, `research_depth: str` (default `""`), `target_audience: str`, `output_format: str`, `cost_tolerance: str`, `time_budget: str`, `must_answer_questions: tuple[str, ...]`, and `degraded_profile: bool`; prove they appear in `ResearchState`, `ResearchCheckpoint`, `OWNERSHIP_TABLE` with `WriterRole.CONTROLLER`, and `GATED_FIELDS`. Prove a version-2 checkpoint without these fields validates successfully (backward compat, no schema version bump). @impl HIN-004
- [ ] 1.4 Extend `domain/state.py` (`ResearchState` TypedDict, `ResearchCheckpoint` dataclass, `OWNERSHIP_TABLE`, `GATED_FIELDS`) with the new profile fields until task 1.3 is green. Do NOT bump `RESEARCH_STATE_SCHEMA_VERSION`. @impl HIN-004

## 2. LLM prompt template and brief generation

- [ ] 2.1 Add red tests in `agent/tests/graph/test_hitl1_prompts.py` for a `build_brief_prompt(question: str) -> str` function that produces a constrained system prompt including the original question text and instructions to output a `StructuredBrief` JSON; a `parse_brief_output(text: str) -> StructuredBrief` that validates the model's JSON output; and rejection of missing required dimensions, invalid enum values, and non-JSON output. Use `FakeToolCallingModel` returning valid/invalid JSON to prove the parse path. @impl HIN-001
- [ ] 2.2 Implement `agent/src/deerflow_deep_research/graph/nodes/hitl1/prompts.py` with `build_brief_prompt` and `parse_brief_output` until task 2.1 is green. @impl HIN-001

## 3. Real HITL1 node: interrupt, parse, validate, and store

- [ ] 3.1 Add red node tests in `agent/tests/graph/test_hitl1_node.py` for `build_real`: on first visit (ordinal=1) it calls `capabilities.run_agent()` once with the brief prompt, validates the `StructuredBrief` output, calls `interrupt()` with a `PendingResearchInterrupt` containing the brief in `HumanInputRequest.context`, and suspends; on resume with a complete valid `AcceptedHumanResponse` it parses the profile, writes `profile_ref` and dimension fields to state, and routes `accepted`; on resume with `InternalCancelDecision` it routes `cancel` with `terminal_status=CANCELLED`; and on `request_id` mismatch it raises `ValueError("response_mismatch")`. Use `FakeToolCallingModel` for the agent call and a test double for `interrupt` that captures the payload. @impl HIN-001, HIN-002, HIN-003
- [ ] 3.2 Implement `graph/nodes/hitl1/node.py` `build_real` consuming `dependencies.capabilities` for the agent call and performing the first-visit brief→interrupt cycle until task 3.1 first-visit tests are green; the fake factory in `fake.py` is unchanged. @impl HIN-001, HIN-002
- [ ] 3.3 Add red tests for re-entrant HITL1: on resume with an incomplete response (missing `format`, `cost_tolerance`, and `time_budget`), the node SHALL construct a partial profile, generate a follow-up prompt asking only for the missing dimensions, call `interrupt()` again with ordinal=2; on resume with the remaining dimensions filled, it SHALL merge them into the partial profile and route `accepted`. On the 3rd re-entrant visit (ordinal=3, total 4 visits including initial), if still incomplete, it SHALL accept the best-effort partial profile with `degraded_profile=True` and route `accepted`. @impl HIN-003
- [ ] 3.4 Implement re-entrant follow-up logic in `build_real` (store partial profile in a local closure variable across interrupts, track ordinal via `completed_visits`, bounded at 3 total visits) until task 3.3 is green. @impl HIN-003
- [ ] 3.5 Add red tests for error paths: model output fails schema validation once → retry; fails twice → `route=exhausted`, `terminal_status=BLOCKED`; `run_agent` raises → propagate error without writing partial profile or calling `interrupt()`; cancel interrupt during re-entrant visit → route `cancel`; response with unrecognizable enum value → treated as unset with follow-up. @impl HIN-001, HIN-003
- [ ] 3.6 Implement error/cancel/retry paths in `build_real` until task 3.5 is green. @impl HIN-001, HIN-003
- [ ] 3.7 Add red tests for profile storage: on `accepted` route, the node SHALL serialize the `ResearchProfile` to JSON and write a `ContentRef` to `state["profile_ref"]` with a deterministic `sandbox_path` under the bundle's `request/` subtree, plus set all short dimension fields in the state update; on `cancel` route, no profile fields are written. Verify the content_ref path matches `workspace/deep-research/<rid>/request/profile.json`. @impl HIN-004
- [ ] 3.8 Implement profile storage (ContentRef construction + dimension field population) in `build_real` until task 3.7 is green. Note: the actual sandbox file write requires a runtime capability; for the initial implementation, the ContentRef is constructed and returned in the state update, and the actual file write is deferred to a later change or handled by a follow-up task. @impl HIN-004

## 4. Mixed-graph integration and E2E

- [ ] 4.1 Add red tests in `agent/tests/graph/test_implementation_map.py` that the mixed map (`bootstrap=real`, `hitl1=real`, every other phase `fake`) selects both real factories and compiles, the full-fake map is unchanged, and the normalized topology is identical (no new edges, no removed nodes). @impl HIN-005
- [ ] 4.2 Extend `runtime/research.py:ResearchGraphRecipe` and the graph builder if needed to support `hitl1=real` in the implementation modes override until task 4.1 is green. @impl HIN-005
- [ ] 4.3 Add a red mixed-graph E2E test (through the handlers using the `implementation_modes` recipe override) that runs real bootstrap → real HITL1 (with `FakeToolCallingModel` + captured interrupt) → resume with valid response → remaining fake phases to completion; asserts the checkpoint contains `profile_ref` and dimension fields, the lifecycle result is `implementation_mode=full_fake`, and no research model/web/subagent/synthesis/report side effect occurs beyond the HITL1 brief generation. @impl HIN-005
- [ ] 4.4 Add a red full-fake regression test proving the fake HITL1 path (and the existing lifecycle E2E) is unchanged when all phases are `fake`. @impl HIN-005

## 5. Structure, documentation, and governance

- [ ] 5.1 Add the exact new production paths (`domain/profile.py` and `graph/nodes/hitl1/prompts.py`) to `openspec/governance/project-structure.toml` required-current paths; add the new state fields to the typed-state documentation; render the generated `agent/AGENTS.md` block with `python3 openspec/governance/check_project_architecture.py --render-guide`; and pass `check_project_architecture.py`. @impl PRS-001, PRS-004
- [ ] 5.2 Update human-authored `agent/AGENTS.md` (Current Status / Node Packages), `agent/README.md`, and the numbered/master backlog plan status (`_backlog/plans/deep-research-06-hitl1-node.md`) with the real HITL1, profile model, LLM brief generation, re-entrant follow-up, and the unchanged mixed-graph boundary. @impl HIN-001, HIN-005
- [ ] 5.3 Register the `HIN` prefix and HIN-001 through HIN-005 descriptions in `openspec/governance/req-registry.yaml`; verify the active delta `specs/hitl1-node/spec.md` owns `> req: HIN-001, HIN-002, HIN-003, HIN-004, HIN-005` with no modified-capability delta. @impl HIN-001, HIN-002, HIN-003, HIN-004, HIN-005
- [ ] 5.4 Run the existing configure/doctor contract tests and project doctor; verify HITL1 has no new config.yaml, extensions_config.json, skill, Agent/SOUL, mount, dependency, or startup-only field changes; confirm no `backend/` or `frontend/` edit is needed. @impl DEC-005

## 6. Verification and hard done conditions

- [ ] 6.1 Run focused domain/graph/HITL1 tests, then `cd agent && make test-unit && make test-contract`; fix all failures.
- [ ] 6.2 Run `cd agent && make test-viability && make test-durability && make test-blocking-io`; verify the mixed-graph E2E path with the existing infrastructure probe.
- [ ] 6.3 Run `cd agent && make test` and verify the complete agent-owned suite, including full-fake and mixed-HITL1 lifecycle paths, is green.
- [ ] 6.4 Run `cd agent && make format && make lint && make lock-check`; fix formatting, lint, or lock drift without adding an unused dependency.
- [ ] 6.5 Run `python3 openspec/governance/check_project_architecture.py`, `python3 openspec/governance/check_project_reqs.py`, and `python3 openspec/governance/check_project_specs.py`; all three must pass with no structural drift, duplicate/unregistered/orphan/reused-retired requirement IDs, delta headers in main specs, or missing purpose/requirements/req headers.
- [ ] 6.6 Inspect `git diff -- backend frontend config.example.yaml extensions_config.example.json` and verify it is empty; inspect the final change diff for one profile authority, one brief-generation path, one interrupt/parse/validate/store cycle, and complete `@impl` traceability before archive.
