## 1. Domain contracts: topic plan, materializer, and planner-owned state

- [x] 1.1 Add red deterministic tests in `agent/tests/domain/test_topics.py` for frozen extra-forbid `ResearchTopic` and `TopicPlan` contracts with `schema_version=1`; bounded topics (1-8), bounded title/scope/must-answer bindings/search dimensions/exclusions; rejection of unknown fields, malformed JSON, missing required fields, oversized text, and out-of-range counts; canonical JSON serialization; and a deterministic `parse_plan_output(text) -> TopicPlan` for JSON that rejects invalid/non-JSON output. @impl TOP-001, TOP-002
- [x] 1.2 Implement `agent/src/deerflow_deep_research/domain/topics.py` with the topic contracts, bounds, canonical serialization, and deterministic parser until task 1.1 is green. @impl TOP-001, TOP-002
- [x] 1.3 Add red tests for a pure deterministic materializer `materialize_topic_plan(plan, must_answer) -> MaterializedTopics`: stable model-independent ids/slugs derived from validated title/scope (identical across two materializations of the same plan, never the raw model string), deduplication by slug, overlap rejection, a coverage map binding every root `must_answer` question to at least one topic, and rejection of empty coverage, uncovered root questions, and over-expansion beyond the topic bound. @impl TOP-002, TOP-003
- [x] 1.4 Implement the materializer as a pure function (under `domain/topics.py` or `engine/`) until task 1.3 is green; it performs no I/O and no model calls. @impl TOP-002, TOP-003
- [x] 1.5 Add red state tests in `agent/tests/domain/test_state.py` for new bounded planner-owned topic fields (`topic_refs` and `topic_registry`); prove defaults preserve version-2 checkpoint compatibility, fields appear in `ResearchState`, `ResearchCheckpoint`, and `OWNERSHIP_TABLE` under `WriterRole.PLANNER`, the local authority-writer guard accepts planner writes and rejects non-planner writers, and `RESEARCH_STATE_SCHEMA_VERSION` is not bumped. @impl TOP-004, REG-006, REG-007
- [x] 1.6 Extend `domain/state.py` with the bounded planner-owned topic fields, ownership entries, default validation, and serialization support until task 1.5 is green. @impl TOP-004, REG-006, REG-007

## 2. Runtime capabilities: real node-agent injection for real topic planning

- [x] 2.1 Add red tests in `agent/tests/unit/test_research_runtime_capabilities.py` proving `ResearchGraphRecipe` detects `topic_planning=real`, rejects `topic_planning=real` unless `hitl1=real` (which itself requires `bootstrap=real`) with a typed dependency error, constructs a real `RuntimeNodeAgentBridge` with zero tools and one model call per bridge invocation for real topic planning, does NOT attach a request-bundle writer or work-unit controller for topic planning, and leaves full-fake recipes on unavailable capabilities. @impl TOP-005, NOA-001, NOA-002
- [x] 2.2 Wire `runtime/research.py` to detect `topic_planning=real`, enforce the real-HITL1 dependency, and construct/attach the real node-agent bridge only for real topic planning without extending request-bundle, bootstrap, or work-unit authority until task 2.1 is green. @impl TOP-005, NOA-001, NOA-002

## 3. Planner prompt and node behavior

- [x] 3.1 Add red tests in `agent/tests/graph/test_topic_planning_prompts.py` for `build_planner_prompt(request_text, research_depth, target_audience, output_format, cost_tolerance, time_budget, must_answer_questions, degraded_profile) -> NodeExecutionRequest` and `parse_plan_output(text) -> TopicPlan`; verify the objective carries all profile constraints plus request_text, the expected output demands exactly one `TopicPlan` JSON object, the prompt includes the degraded-profile instruction when degraded_profile is True, and invalid/missing/non-JSON output is rejected. @impl TOP-001
- [x] 3.2 Implement `agent/src/deerflow_deep_research/graph/nodes/topic_planning/prompts.py` until task 3.1 is green. @impl TOP-001
- [x] 3.3 Add red node tests in `agent/tests/graph/test_topic_planning_node.py` for `build_real`: it calls `dependencies.capabilities.run_agent()` with the planner request carrying all profile constraints plus request_text, validates a `TopicPlan`, re-prompts once on invalid output, materializes a stable registry on success, records bounded planner-owned topic state, and routes `next`; on a second invalid result or `run_agent` failure it writes no topic state and routes `exhausted` with `terminal_status=BLOCKED`/`terminal_reason=GATE_BLOCKED`; with `must_answer_questions=()` and `degraded_profile=True`, it falls back to `request_text` as the synthetic must-answer question and produces a valid covering plan. Use fake capabilities returning `NodeExecutionResult.summary`, not `FakeToolCallingModel` directly. @impl TOP-001, TOP-002, TOP-003, TOP-004
- [x] 3.4 Implement the real planning factory in `graph/nodes/topic_planning/node.py`, replacing the `UNAVAILABLE_REAL_FACTORY` sentinel; keep `fake.py` unchanged. Until task 3.3 is green. @impl TOP-001, TOP-002, TOP-003, TOP-004
- [x] 3.5 Add red tests for the coverage/materializer integration and ownership: an empty/uncovered/overlapping/over-expanded plan is rejected and retried once, then fails closed to `exhausted`; a valid covering plan is accepted; a controller/worker/model-derived update to topic fields is rejected by the reducer. @impl TOP-002, TOP-003, TOP-004

## 4. Topology, implementation map, and E2E

- [x] 4.1 Add red topology/contract tests that the topic planning result permits `next` and `exhausted`; `NORMALIZED_EDGES` and `build_research_graph()` include `topic_planning --exhausted--> blocked/END` and a conditional edge `{next: wave0, exhausted: END}` replacing the direct edge; the topology snapshot changes only by that edge and conditional conversion; and full-fake route behavior remains unchanged. @impl TOP-005, REG-001, REG-005
- [x] 4.2 Extend `graph/topology.py`, `graph/builder.py` (convert the direct `topic_planning -> wave0` edge to conditional), and the committed topology snapshot/tests until task 4.1 is green. @impl TOP-005, REG-001, REG-005
- [x] 4.3 Add red implementation-map tests that mixed mode (`bootstrap=real`, `hitl1=real`, `topic_planning=real`, every other phase `fake`) selects all three real factories and compiles; `topic_planning=real` without `hitl1=real` fails closed before graph invocation; the full-fake map is unchanged; real topic planning declares no `NodeCapability`. @impl TOP-005, PRS-003
- [x] 4.4 Extend the topic_planning `NODE_SPEC` routes/contracts and implementation-map/recipe override support until task 4.3 is green. @impl TOP-005, PRS-003
- [x] 4.5 Add a red mixed-graph lifecycle E2E through handlers that runs real bootstrap -> real HITL1 -> real topic planning -> valid plan -> remaining fake phases to completion using fake node-agent capabilities; assert the bounded topic registry and `topic_refs` in the checkpoint, `implementation_mode=full_fake`, no findings/report side effects, no sandbox topic file, and profile read from checkpoint state. @impl TOP-001, TOP-004, TOP-005
- [x] 4.6 Add red E2E coverage for invalid plan -> repair -> completion, plus double-invalid plan -> terminal `blocked` with no recorded topic state and no advance to Wave0. @impl TOP-003, TOP-005, REG-005
- [x] 4.7 Add a full-fake regression test proving fake topic planning and existing lifecycle E2E remain unchanged and do not construct `RuntimeNodeAgentBridge`. @impl TOP-005, REG-002

## 5. Structure, documentation, and governance

- [x] 5.1 Verify the active delta specs for `topic-planning-node`, `research-graph-lifecycle`, and `project-structure` still own the topic-planning `exhausted` route, planner-owned topic checkpoint fields, version-2 compatibility, and the new production paths after implementation edits. @impl TOP-001, TOP-002, TOP-003, TOP-004, TOP-005, REG-001, REG-005, PRS-001, PRS-002, PRS-003, PRS-004
- [x] 5.2 Add exact new production paths (`domain/topics.py` and `graph/nodes/topic_planning/prompts.py`) to `openspec/governance/project-structure.toml`; render the generated `agent/AGENTS.md` block with `python3 openspec/governance/check_project_architecture.py --render-guide`; pass `check_project_architecture.py`. @impl PRS-001, PRS-002, PRS-003, PRS-004
- [x] 5.3 Update human-authored `agent/AGENTS.md` (Current Status / Node Packages / state authority), `agent/README.md`, and `_backlog/plans/deep-research-07-topic-planning-node.md` with real topic planning, the topic contracts and deterministic materializer, the real-HITL1 dependency, the explicit `exhausted` route, and the unchanged `backend/`/`frontend/` boundary. @impl TOP-001, TOP-002, TOP-003, TOP-004, TOP-005
- [x] 5.4 Verify `openspec/governance/req-registry.yaml` contains the `TOP` prefix and TOP-001 through TOP-005 descriptions, and that active delta specs own all TOP ids plus modified REG-001/005/006/007 and PRS ids with no duplicate/unregistered/orphan/reused-retired requirements. @impl TOP-001, TOP-002, TOP-003, TOP-004, TOP-005, PRS-004
- [x] 5.5 Run configure/doctor contract tests and project doctor; verify topic planning adds no `config.yaml`, `extensions_config.json`, public/custom skill, Agent/SOUL, mount, dependency, startup-only field, `backend/`, or `frontend/` changes. @impl DEC-005

## 6. Verification and hard done conditions

- [x] 6.1 Run focused domain/runtime/graph/topic-planning tests, then `cd agent && make test-unit && make test-contract`; fix all failures.
- [x] 6.2 Run `cd agent && make test-viability && make test-durability && make test-blocking-io`; verify mixed real-topic-planning E2E and blocked paths use existing infrastructure boundaries.
- [x] 6.3 Run `cd agent && make test` and verify the complete agent-owned suite, including full-fake, mixed-topic-planning, blocked, and restart lifecycle paths, is green.
- [x] 6.4 Run `cd agent && make format && make lint && make lock-check`; fix formatting, lint, or lock drift without adding unused dependencies.
- [x] 6.5 Run `python3 openspec/governance/check_project_architecture.py`, `python3 openspec/governance/check_project_reqs.py`, and `python3 openspec/governance/check_project_specs.py`; all three must pass.
- [x] 6.6 Run `openspec validate implement-deep-research-topic-planning-node --strict`; fix any artifact/schema issue.
- [x] 6.7 Inspect `git diff -- backend frontend config.example.yaml extensions_config.example.json` and verify it is empty; inspect the final diff for one topic authority, one deterministic materializer, no LLM-authored registry, no `profile.json` read, one explicit `exhausted` route, no new `NodeCapability`, and complete `@impl` traceability before archive.
