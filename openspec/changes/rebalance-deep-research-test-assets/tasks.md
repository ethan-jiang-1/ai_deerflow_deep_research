# Test Asset Rebalancing Tasks

> Current progress: **0/50 complete**. Review rounds 1-8 have corrected the evidence vocabulary, scenario shape, lane partition, focused-live claim/reporting, metric semantics, provider-fixture guarantees, cross-lane requirement policy, preserved-requirement semantics, release rerun policy, persisted release evidence, incremental governance order, model/tool-node discovery, DeerFlow-derived scripted-loop/replay/isolation/gate-self-test policy, durable governance placement, and task granularity; implementation has not started.
>
> Progress rule: update this summary and the active task after every focused red/green slice. Before checking a task, record its focused selector or deterministic governance command, observed result, and any honest scope reduction directly below that task; manual live tasks additionally record the bounded report evidence named by the task.
>
> Hard boundary: this change modifies test-owned `agent/` assets, OpenSpec, documentation, Make targets, and agent workflows only. `backend/` and `frontend/` remain unchanged. Any required change to production behavior or interfaces under `agent/src/` returns to proposal/design review before implementation and triggers a fresh release-proof policy assessment.

## 1. Canonical Evidence And Lane Contract

- [ ] 1.1 Record `apply_base_revision`, initial worktree status, protected tree hashes for `agent/src`, `backend`, `frontend`, release runner, and public entry, plus deterministic baseline probes for the current empty `workflow` collection and placeholder metrics; then add synthetic invalid-fixture tests proving focused-lane overlap, descriptive-only families, contradictory evidence vocabulary, and invalid metric authority fail with stable diagnostics without snapshotting raw test counts or leaving the repository gate red. @impl EVH-001, EVH-002, EVH-008, EVH-009
- [ ] 1.2 Define the central frozen `TestEvidenceClaim` contract with stable claim id, exact selector, expected focused selection, requirement ids, one of four closed asset classes, one of five canonical stable seams, optional one of five authenticity levels, at most one scenario-case id, and discovery ids; use synthetic case registries to prove each supplied case resolves to exactly one claim/node id containing its stable case id, reject aliases, unknown vocabulary, selector globs/prefixes, and add bounds, validation, and duplicate tests without requiring the not-yet-migrated real scenario catalog. @impl EVH-006, EVH-007, EVH-008
- [ ] 1.3 Migrate incident, real-node, and critical-fault inventories to reference central claim ids and canonical seams, preserve replaced/duplicate incident-recommendation status and the complete historical incident-category inventory, remove duplicated selector/authenticity declarations, and add contradictory-reference failure tests. @impl EVH-006, EVH-008
- [ ] 1.4 Use Python syntax-tree inspection across production node packages to freeze and verify loaded `run_agent` calls or method-reference owners (`hitl1`, `topic_planning`, `wave0`, `wave1`, `wave2_synthesis`, `targeted_evidence`); add `workflow` only to the existing qualifying focused selectors, register their real-bridge deterministic workflow claims, and make ownership drift or missing/downgraded coverage fail with the node id; do not infer model use from `NODE_SPEC.capabilities`, require a real bridge loop for `SCRIPTED_REAL_WORKFLOW`, and add positive/negative classification tests. @impl EVH-008, EVH-009
- [ ] 1.5 Make `make test-fast`, `make test-integration`, and `make test-workflow` pairwise disjoint, keep live/release/Postgres excluded, add collection tests proving their exact union equals `make test` deterministic collection, set live to `requires_llm and not release_e2e`, set release to `requires_llm and release_e2e`, and reject any `release_e2e` test missing `requires_llm`. @impl EVH-008, EVH-009
- [ ] 1.6 Update the asset checker to collect deterministic aggregate plus fast/integration/workflow/live/release focused selectors, reject overlap among all five focused selections, validate every central claim in its expected selection, and add focused meta-tests that inject one minimal violation into every new or materially changed selector/claim/node-discovery gate and assert its stable diagnostic. @impl EVH-006, EVH-009
- [ ] 1.7 Update PR CI to run all three focused deterministic selections and fail when workflow collection is empty or any selector overlaps. @impl EVH-009

## 2. Scenario Families, Cases, And Assertions

- [ ] 2.1 Define frozen `ScenarioFamily` and `ScenarioCase` contracts with stable ids, family risk/requirements/permitted-degradation policy, and case lane/entrypoint/asset class/seam/authenticity/bounds/expectations/hard invariants/applicable metrics; reject case degradation outside its family policy using synthetic fixtures, and keep legacy `Scenario`/`ScenarioRunner` only as the unchanged migration source until task 3.11 rather than weakening the new schema for string pseudo-scripts. @impl EVH-001, EVH-007
- [ ] 2.2 Define bounded typed deterministic inputs for model turns, tool results/failures, lifecycle actions, and supported fault points; extend the shared `ScriptedChatModel` fixture with observable calls/tool bindings plus exhausted/unconsumed-turn failure checks, and reject string pseudo-scripts, credentials, raw host paths, external URLs where forbidden, and oversize inputs without adding persisted replay unless a reviewed case proves scripts insufficient. @impl EVH-001, EVH-007
- [ ] 2.3 Define the test-owned `ScenarioObservation` projection and closed invariant registry over observable calls plus checkpoint, validated-ledger, and contained-sandbox facts; add unknown-invariant and redaction tests. @impl EVH-007
- [ ] 2.4 Implement one shared assertion interface for family/case route, terminal, artifacts, support/citations, every applicable invariant, and permitted degradation without adding a universal execution dispatcher. @impl EVH-001, EVH-007
- [ ] 2.5 Build reusable local runtime, scripted model/tool, checkpoint, ledger, and contained-artifact fixtures that focused pytest cases can compose while continuing to call existing production interfaces explicitly; isolate effective home/config paths and filesystem state, reset or restore sandbox-provider and singleton caches on success/failure, prove cross-case cleanup, and consolidate the public-entry replay fake only if its explicit behavior and selector proof remain intact. @impl EVH-001, EVH-008
- [ ] 2.6 Add synthetic scenario-governance fixtures requiring stable case ids as pytest parameter ids and rejecting unbound cases, cross-asset/seam authenticity substitution, insufficient same-scope authenticity, missing authority observations, or deterministic credentials/network access; defer enforcement that every real family has a collected deterministic case/claim to task 3.11. @impl EVH-001, EVH-007, EVH-009

## 3. First-Wave Replay Cases

> Slice contract: each conversion task records its stable case id, exact central claim/node id, focused selector, and observed result before it is checked; task 3.11 enables completeness only after those slices are individually bound.

- [ ] 3.1 Convert `quick-factual` into a focused real worker/ledger workflow case that proves accepted evidence and structurally bound support at the lowest applicable seam. @impl EVH-001, EVH-008
- [ ] 3.2 Convert `claim-verification` into a labeled replay case with supported, contradicted, and uncertain evidence states and authoritative finding/ref observations. @impl EVH-001, EVH-002
- [ ] 3.3 Convert `insufficient-evidence` into a real validation/ledger/artifact workflow case that reaches typed honest degradation without accepted unsupported claims or fabricated citations. @impl EVH-001, EVH-002, EVH-008
- [ ] 3.4 Convert `prompt-injection` into a real tool/worker/validator/ledger/gate workflow case proving route, gate, ledger, checkpoint, and path authority cannot be forged. @impl EVH-001, EVH-004, EVH-008
- [ ] 3.5 Convert `malformed-output` into a real node/worker workflow case that consumes bounded repair responses and fails closed without partial ledger or artifact authority. @impl EVH-001, EVH-008
- [ ] 3.6 Convert `tool-unavailable-timeout` into a real bridge/policy workflow case with typed timeout/unavailable results, cancellation semantics, call bounds, and no partial publication. @impl EVH-001, EVH-003, EVH-008
- [ ] 3.7 Convert `budget-exhaustion` into a real bridge/policy workflow case with typed exhaustion, exact model/tool call bounds, and no accepted partial publication. @impl EVH-001, EVH-003, EVH-008
- [ ] 3.8 Convert `partial-worker-success` into a real fan-out/submit/gate workflow case distinguishing accepted, failed, repair, fatigue, and exhausted outcomes from authoritative work state. @impl EVH-001, EVH-003, EVH-008
- [ ] 3.9 Convert `checkpoint-control` into a real lifecycle workflow case covering duplicate resume, cancel idempotency, terminal monotonicity, and restart-safe checkpoint identity without claiming an agent loop. @impl EVH-001, EVH-003
- [ ] 3.10 Convert `sandbox-filesystem-failure` into code-correctness cases over real store atomic-publication/replay seams with containment and prior-or-single-new authority guarantees, without falsely marking them workflow. @impl EVH-001, EVH-003
- [ ] 3.11 Remove the legacy `Scenario`/`ScenarioRunner` and catalog only after all ten migrations, then enable real-catalog governance proving every family id has a collected deterministic case/claim at its declared lowest seam and only qualifying cases enter the workflow selection. @impl EVH-001, EVH-007, EVH-008, EVH-009
- [ ] 3.12 Re-run the syntax-discovered six-node workflow audit after the first-wave migration, add or upgrade the smallest explicit production-interface case for any newly exposed gap, and make the combined node/scenario/workflow inventory pass without weakening a required seam or authenticity. @impl EVH-008, EVH-009

## 4. Provider-Shape Provenance

- [ ] 4.1 Define the minimized provider-shape case schema/loader plus a closed `DiscoveryDisposition` contract for exactly one of `uniform_shape` with intended case id, `existing_regression` with claim id, or `provider_only` with bounded live case/rationale; reject mixed/unknown disposition fields and validate focused node, input schema version, bounded synthetic payload, expected normalized/fail-closed result, and sensitivity metadata. @impl EVH-010
- [ ] 4.2 Classify `LIVE-20260717-01..06` as uniform shape, existing focused regression, or provider-only behavior; bind existing-regression claims immediately and record a stable intended shape-case id or bounded live rationale for the other dispositions. @impl EVH-006, EVH-010
- [ ] 4.3 Classify `RELEASE-20260717-01..14` as uniform shape or existing focused regression; bind existing-regression claims immediately and record a stable intended shape-case id for cases migrated in tasks 4.5-4.7. @impl EVH-006, EVH-010
- [ ] 4.4 Classify `RELEASE-20260717-15..25` as uniform shape or existing focused regression; bind existing-regression claims immediately and record a stable intended shape-case id for cases migrated in tasks 4.5-4.7. @impl EVH-006, EVH-010
- [ ] 4.5 Add or migrate Wave0 URL, fetch-status, limitations, partial/duplicate-source, and direct-answer shape cases through production normalization/validation seams without duplicating non-uniform focused tests. @impl EVH-006, EVH-010
- [ ] 4.6 Add or migrate Wave1 provider/source ids, ref rewrites, question states, source ordering, and malformed-submit shape cases through production contract/materializer/submit seams. @impl EVH-006, EVH-010
- [ ] 4.7 Add or migrate Wave2 finding, gap, relation, endpoint, priority, affected-topic, evidence-alias, sparse-output, and semantic-floor shape cases through production parser/node/validator seams. @impl EVH-006, EVH-010
- [ ] 4.8 Enable real-catalog provenance closure: every replayable discovery resolves to its collected existing-regression or shape-case claim and every provider-only discovery has bounded live rationale; reject unknown discoveries, unresolved intended case ids, missing claims, duplicate/unowned cases, oversize payloads, credentials, raw host paths, and non-synthetic external URLs, require recorded human review that free text is minimized/synthetic without claiming arbitrary raw prose is mechanically detectable, and meta-test each new provenance/sensitivity gate with a minimal intentional violation. @impl EVH-006, EVH-010

## 5. Validated Metric Semantics

- [ ] 5.1 Define and test the pure `ValidatedEvaluationOutcome` plus typed metric results where an unselected case metric is `not_applicable` with null value, a selected metric with missing/unlabeled authority or a ratio's empty authoritative denominator is `insufficient_authority` with null value, a validated empty source collection permits measured zero only for applicable count metrics, and sufficient input is numeric `measured`, using already-read ledger/topic/synthesis/citation-map/labeled-expectation inputs. @impl EVH-002
- [ ] 5.2 Replace ref-prefix precision with structural citation-binding rate and labeled semantic citation precision; prove accepted-but-unlabeled bindings return `insufficient_authority` for semantic precision. @impl EVH-002
- [ ] 5.3 Project must-answer coverage through existing question-to-topic and finding/gap-to-topic mappings; count a question covered only when every known bound topic has a finding or typed honest gap, report partial questions and unknown/unmapped topics separately, and avoid direct question-to-finding claims. @impl EVH-002
- [ ] 5.4 Replace submission-ref diversity with separate numeric distinct-canonical-URL and normalized-host metrics derived only from validated accepted `SourceRef` values, prove provider-local `source_id` changes cannot inflate diversity, compute final claim-map citation completeness separately from synthesis finding-support coverage, and separate missing-backing-ref counts from labeled semantic unsupported-claim counts and contradiction recall. @impl EVH-002
- [ ] 5.5 Extend live reports compatibly with `report_schema_version=1`, `metrics_schema=evidence-v1`, typed metric statuses/evidence bases, and legacy-report classification; start a new non-blocking baseline. @impl EVH-002, EVH-005

## 6. Focused Late-Node Live Cases

> Slice contract: tasks 6.2-6.4 each add one stable live case id, exact collected live claim/selector, deterministic setup/report contract tests, and focused-coverage disclaimers before any credentialed execution in task 6.7.

- [ ] 6.1 Add test-owned seed builders that create compact valid checkpoint projections and publish minimum Wave0/Wave1 ledger/content authority through existing store interfaces, with hash/schema/containment tests and no direct ledger-line fabrication. @impl EVH-005, EVH-007
- [ ] 6.2 Add a one-topic Wave1 live case invoking the real `NODE_SPEC` with seeded Wave0 authority and `_build_wave1_capabilities` through the bounded live bridge factory; separate the reserved gate view, project `WORK_UNIT_GATE_PREVIEW_FIELDS` with `preview_work_unit_update`, validate the view, then call `evaluate_gate_for_node(..., real_wave1_gate_def())`, asserting real model/web-tool submit/gate authority plus focused-claim disclaimers. @impl EVH-005, EVH-009
- [ ] 6.3 Add a Wave2 synthesis live case invoking the real `NODE_SPEC` with seeded accepted evidence and the shared zero-tool `_build_hitl1_capabilities` through the bounded live bridge factory; assert node-owned binding/semantic-floor behavior, then separately call `evaluate_gate_for_node(..., real_wave2_gate_def())` on the node-updated state and record redacted response-shape diagnostics. @impl EVH-005, EVH-009
- [ ] 6.4 Add a one-gap targeted-evidence live case invoking the real `NODE_SPEC` with seeded synthesis authority and `_build_wave0_capabilities` through the bounded live bridge factory/work-unit controller; assert subgraph-owned validated submit/ledger publication without claiming a nonexistent targeted phase gate or public-entry/recipe/full-prefix coverage. @impl EVH-005, EVH-009
- [ ] 6.5 Update live selection, strict preflight, versioned reports, and archive scans so all six cases use unique identity, one outer attempt, scenario-specific token/tool/time bounds, honest `LIVE_REAL_DEPENDENCIES` focused claims, and bounded redacted identity/invariant/metric/attempt/resource diagnostics; meta-test report/redaction/archive enforcement with minimal malformed and sensitive fixtures. @impl EVH-002, EVH-005, EVH-009, EVH-010
- [ ] 6.6 Reassign the existing three 300-second deadlines and add the new case deadlines under one deterministic budget contract enforcing the six-case sum `<=900s`, each web case `<=240s`, each zero-tool case `<=120s`, and the unchanged 20-minute job timeout; meta-test one per-case and one aggregate deadline violation. @impl EVH-005, EVH-009
- [ ] 6.7 Run the complete six-case live lane manually with fresh identities after all additions and record one schema-valid redacted aggregate plus per-case duration, token/tool, cost-availability, retry, invariant, and metric evidence; do not rely on gitignored pre-change reports to enable nightly. @impl EVH-002, EVH-005, EVH-009
- [ ] 6.8 Enable all six nightly cases only after the measured evidence plus declared deadlines preserves the five-minute setup/preflight/report/cleanup margin without raising the job timeout. @impl EVH-005, EVH-009

## 7. Requirement Governance, Documentation, And Final Gates

- [ ] 7.1 Add a small requirement-evidence policy mapping cross-lane requirements to required independent asset classes/authenticity (including workflow for `EVH-004/008` and deterministic/live/release for `EVH-005`), retain the collected deterministic test-side `@impl` requirement for every alive requirement, and add missing-class, missing-`@impl`, unknown-id, and ordinary-requirement acceptance tests. @impl EVH-008, EVH-009, EVH-010
- [ ] 7.2 Generate and commit a minimal redacted release attestation from the design-frozen accepted evidence with source date/hash, attestation base revision explicitly distinct from the unknown run revision, counts/bounds, all eight frozen named invariant results, separately scoped source-run/source-archive-scan/attestation-scan evidence, an exact committed source locator plus original live/release target scope for the historical scan, and no unsupported metadata; add deterministic schema/provenance/sensitivity tests plus minimal invalid-attestation meta-tests, and update or explicitly supersede the contradictory `NOT READY / full-real not executed` parity report. @impl EVH-005, EVH-010
- [ ] 7.3 Add `openspec/governance/test-evidence-policy.md` as the concise cross-change authority for deterministic-first/lowest-seam evidence, real-loop scripted adapters, wiring-only patches, replay/live/release escalation, stable-shape assertions, fixture isolation, and gate self-tests; list it as a normative fifth pillar in the governance README while keeping pytest collection executable and agent-owned; update `openspec/config.yaml` with short proposal/spec/design/task authoring rules and a policy pointer, correcting its deterministic-scenario and three-check archive wording; then align `agent/README.md`, `agent/AGENTS.md`, `agent/docs/regression-descent.md`, and the live baseline with disjoint selections, central evidence claims, family/case semantics, provider provenance, focused-live disclaimers, typed metrics, committed-attestation policy, and the continued single-E2E policy. @impl EVH-001, EVH-002, EVH-005, EVH-009, EVH-010
- [ ] 7.4 Run strict OpenSpec, all three root requirement/spec/architecture governance checks, asset and requirement coverage, lock, Ruff, deterministic fast/integration/workflow/full suites, viability, durability, the blocking-I/O detector smoke test, live report/redaction/aggregate-deadline gate meta-tests, committed release-attestation schema/redaction/eight-invariant validation and gate meta-tests, and repository-boundary checks; do not rerun full-real unless the reviewed rerun policy triggers, and compare `agent/src`, `backend`, `frontend`, release runner, and public entry with the exact protected tree hashes recorded by task 1.1 while preserving any documented pre-existing user changes. @impl EVH-001..EVH-010
